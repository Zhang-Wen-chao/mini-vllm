# MTP 投机解码 sweep：Qwen3.8-27B 单卡（2026-09-11）

官方 vLLM 0.28.0 上对 Qwen3.8-27B（48 GDN + 16 full attention 混合架构，自带单层
MTP 头权重）做投机解码实测。**与 mini-vllm 仓库的关系**：本项目不涉及 mini-vllm
代码；本目录只作为该轮实验的证据归档位（官方框架实验统一收在 `experiments/`，
与 2026-09-10 的 `experiments/*_2026-09-10*` 同一惯例）。内部路径已脱敏：
`<model-dir>`（模型目录）、`<venv>`（Python venv）、`<out-dir>`（输出目录，
跨 sweep 引用带编号 `<out-dir-2/4/5>`）、`<nvme-root>`（NVMe 根目录，
含其下的 `cache/` 与 `tmp-run/`）、`<lc-venv>`（标定 venv）、
`<chain-log>`/`<calib-log>`（编排/标定日志）。

**脱敏清单（每轮归档前逐条过，别只扫路径）：**

| 类别 | 模式 | 替换为 | 首次发现 |
|---|---|---|---|
| 路径 | `<nvme-root>/<sweep 目录>` | `<out-dir-N>` | sweep1 |
| 路径 | `/root/models/...`、`/root/vllm-env` | `<model-dir>` / `<venv>` | sweep1 |
| **主机地址** | **`mq_connect_ip=<私网 IP>`** | **`mq_connect_ip=<host-ip>`** | **sweep8 归档时回头扫出（2026-09-12）** |

最后一条是补上一个真实疏漏：sweep7 的 12 个 `server_*.log` 里带着
vLLM `multiproc_executor.py:150` 打出的 `mq_connect_ip=10.82.0.xx (local)`——
**宿主机私网地址**（此处本身也已打码），随 sweep7 一起推上去了。归档流程此前只扫路径，
**没有扫过地址**，所以它一路过了好几轮扫描。已于 2026-09-12 在**工作区**替换为
`<host-ip>` 并留证据完整性说明（见下）；**但它仍在已推送的历史提交里**，
彻底清除需要 force-push 重写历史，**未经用户授权不做**。

## 问题

两个问题：
1. **性能**：MTP（`--speculative-config '{"method":"mtp","num_speculative_tokens":N}'`）
   在低并发/高并发下各值多少吞吐？和 fp8 KV 能不能叠加？
2. **正确性**：投机解码理论上必须无损（verify 用完整模型前向，贪心输出应与
   非 spec 路径逐 token 一致）——实测是否成立？

## 环境（锁档）

- 见 `sweep2/env_2026-09-11.txt`（nvidia-smi + pip freeze 全量）。
- Python 3.12.3 / vllm 0.28.0 / torch 2.13.0+cu130 / transformers 5.16.1 / triton 3.7.1；
  单卡 L20（48GB），与 2026-09-10 对比实验**同一 venv**（未变更）。
- 跑在容器内；时钟为 UTC（log 时间戳 01:xx-02:xx = 北京时间 09:xx-10:xx）。

## 协议

- 基线 = 单卡 fp8 权重 + **KV bf16** + 默认调度（8192）+ prefix caching ON
  （部署实验的"性能基线②"）。spec 臂只加 `--speculative-config`，其余零改动。
- 压测：`vllm bench serve --backend openai-chat`，1024 进 / 256 出，96 prompts，
  temperature 0，并发 16 与 48 两档；每臂独立起服务（冷缓存）。
- 臂序 = A → M1 → M2 → A2（sweep1）、K2 → C2 → A3（sweep2），
  首尾基线互为**锚点**（同类 ABBA 纪律：首尾基线复现则中间臂的差异归于旋钮）。
- sweep2 的 K2 = fp8 KV（无 spec）补测 @16（历史只有 @48）并回放 @48 作跨轮锚点；
  C2 = fp8 KV + MTP k=2（阶梯终点配置，boot 兼容性此前无人验证）。

### 公平清单（逐项）

| 维度 | 各臂取值 |
|---|---|
| venv / torch / vllm | 同一 `<venv>`（0.28.0 / 2.13.0） |
| 权重 / 精度 | 同一 fp8 权重目录；KV dtype 是臂变量（bf16 vs fp8） |
| 调度 | `--max-model-len 8192 --max-num-seqs 128`，其余默认 |
| workload | 同一 bench 命令、同 prompt 数/长度、temperature 0（双方都不设 top-k/top-p/seed） |
| 指标 | vLLM 自带 serve bench 输出，公式同源 |
| 优化默认项 | 双方都开 CUDA Graph、prefix caching（各自默认） |
| **已知 side-effect** | spec 臂被 vLLM 自动改写 `max_num_scheduled_tokens=2048`（启动 WARNING 原话见 `server_startup_lines.txt`），且 draft 权重吃掉 0.8-2.9 GiB → KV 池缩小（见下表）。二者都是"MTP 实现的一部分"，不是配置失误，但 @48 的 TTFT/吞吐劣势主要来自它们——已单独归因，不与 verify 收益混算 |

## 结果（输出吞吐 tok/s；A2/A3 为锚点）

### sweep1（KV bf16，spec 旋钮）

| 臂 | @16 | vs A | @48 | vs A |
|---|---:|---:|---:|---:|
| A 基线 | 219.37 | — | 307.14 | — |
| M1（k=1） | 269.66 | **+23%** | 287.62 | -6% |
| M2（k=2） | 278.11 | **+27%** | 282.31 | -8% |
| A2 锚点 | 219.55 (+0.08%) | | 307.58 (+0.14%) | |

TPOT @16：A 64.7ms → M1 53.0ms（-18%）→ M2 47.6ms（-26%）。
@48 TTFT：A 9.5s → M1 20.1s / M2 23.6s（≈2-2.5 倍，`max_num_scheduled_tokens`
被强制 2048 所致）。

### sweep2（fp8 KV + spec 叠加，阶梯终点）

| 臂 | 配置 | KV 池 tokens | @16 | @48 | @16 TPOT |
|---|---|---:|---:|---:|---:|
| A3 锚点 | fp8 权重，KV bf16 | 99,267 | 219.36 | 307.11 | 64.8ms |
| K2 | + fp8 KV | 136,533 | 219.53 | **351.99** | 67.3ms |
| C2 | + fp8 KV + MTP k=2 | 81,920 | **293.55** | 291.49 | **48.2ms** |

- **K2@48 = 351.99**，复现五天前同配置（fp8 权重+fp8 KV）的 352.9（±0.26%），
  且 KV 池 136,533 tokens / 16.67x 与历史完全一致——跨日复测通过；
- **fp8 KV 在 @16 零收益**（219.53 vs 219.37）：16 路并发无容量压力，
  KV 量化只在容量受限的 @48 兑现（+14.6%，351.99/307.14）；
- **C2@16 = 293.55**：单卡全栈比 M2（278.11）再 +5.5%——fp8 KV 单独无用，
  但与 MTP 叠加有正交互用（假设：verify 每步读 k+1 个位置的 KV，KV 读流量
  占比上升，fp8 KV 开始回本；未单独验证，记录为假设）；
- **阶梯（@16）**：219.4（fp8 权重）→ +27%（MTP）→ 278.1 → +5.5%（fp8 KV）
  → **293.6**；对照双卡 TP2 bf16 同窗 ABBA 值 284.9±6.5——超出均值 3.0%
  （TP2 自身波动 ±2.3%，属打平偏赢），TPOT 48.2 vs 49.5ms、TTFT 1.28s vs
  1.7-2.0s，省一半卡；
- **@48 最优仍是"fp8 KV 不加 MTP"**（351.99）：批量饱和后 verify 的多算
  换不回来（C2 291.49，比 K2 低 17%）。

### 接受率（分母显式）

- k=1（M1@16）：accepted 21,959 / drafts 27,093 = **81.0%**；
- k=2（M2@16）：drafts 20,647，draft tokens 41,294（每 draft 2 个位置），
  accepted 28,485；分位置 pos0 16,200/20,647 = **78.6%**、pos1 12,285/20,647 = **59.5%**；
- C2（fp8 KV + k=2）接受率与 M2 几乎不变（pos0 78.6% / pos1 59.9%）——
  C2 相对 M2 的 +5.5% 不是接受率贡献。

### sweep3（C2b：显式 sched 8192 排除法 + 容量墙实锤）

C2b 与 C2 唯一差异 = 显式 `--max-num-batched-tokens 8192`（消掉 spec 强制
`scheduled=2048` 的 prefill 节流假设）：

| 臂 | @48 | @48 TTFT | @16 | @16 TPOT |
|---|---:|---:|---:|---:|
| C2（sched 默认） | 291.49 | ~23s | 293.55 | 48.2ms |
| C2b（sched 8192） | 293.25 | 22.0s | 301.37 | 46.3ms |

- **@48 不动**（293.25 vs 291.49，+0.6%）：调度假设被排除；
  @16 +2.7%（301.37）为该臂最好成绩（超 TP2 284.9 达 +5.8%，TP2 波动带 ±2.3% 之外）；
- **容量墙实锤**（`sweep3/engine_stats_*.txt`，@48 采样）：spec 臂 Running 钉死
  **16-17 / Waiting 31 / KV usage 90-96%**，而 K2 同窗 Running 满载 48；
- C2b 的 ITL 114.8ms vs TPOT 48.9ms：每 verify 轮产出 ~2.35 token
  （118.5/50.4≈2.35 ≈ 接受率推算 2.38），verify 步仅 +4.6% 时间换 2.35 倍 token
  ——**decode 侧是赚的，@48 输在并发装不下，不在算力**。

### 容量墙机制（源码级，2026-09-11 补）

对 vLLM 0.28 源码的最终归因（两层修正后的定稿，见文末"修正轨迹"）：

- 池的块粒度被 mamba page 抬高：`platforms/interface.py:912`
  （`_align_hybrid_block_size`；**2026-09-12 更正**：旧文写的是
  `kv_cache_interface.py:911`，文件名是错的，行号 911/912 差一行，
  padding 那行在 `:936`）把 attention block size 抬到「刚好盖住 mamba page」为止。
  **这个块长不是常数，是 `f(KV dtype, k)`**：bf16/k=0 = 784、fp8/k=0 = 1568、
  fp8/k=1 = **1584（本线终态配置）**、fp8/k=2 = 1600 ——四档实测见
  [sweep9 结果](#sweep9-结果回填2026-09-12-055826071316-utc)。
  混合架构里全注意力 KV 块与 GDN 状态槽共享同一块池；
- spec 请求的 mamba 运行时准入门（`single_type_kv_cache_manager.py` align 分支）：
  `cdiv(num_tokens_main_model, block_size) + num_speculative_blocks`——按**实际
  token 数**记账。1,280 token 的请求 = 1 + 2 = **3 块**（1 当前状态 + 2 个 spec
  回滚快照，`kv_cache_interface.py:694`：align = page×(2+spec)）；
- 对账：C2 池 81,920 tokens ÷ 1,600 ≈ **52 块**；52 ÷ 3 ≈ 17 路 ←→ 实测
  Running 16-17、usage 90-96% **逐位吻合**；
- `max_model_len` 只影响块表行长（元数据）与启动时 "Maximum concurrency"
  打印（81,920/8,192 = 10.00x），**不影响运行时每请求驻留**；
- 池从 136,533（K2）缩到 81,920（C2）的 40% 是 **draft 权重吃掉的显存**，
  叠加每请求 +2 快照块的准入税——两笔账都指向同一副药：**压目标模型权重，
  把省出的显存灌回块池**（int4 路线，见 sweep5 预注册）。

## sweep4 / sweep5 预注册（2026-09-11 12:5x 北京时间，C4 开跑前）

用户拍板新目标：**明确超越 TP2 基线**（@16 284.9±6.5 / @48 350.2±3.3 双卡），
"打平偏赢"不够。两条路线，预测与判据先落字：

- **C4 臂（sweep4，`mtp_bench4.sh`）**：fp8 KV + MTP k=2 + `max-model-len 4096`
  （+ sched 8192）。**预注册预测：阴性**——源码读出准入门按实际 token 记账
  （见上节），len 4096 不减每请求 3 块的驻留，Running 应仍 ~17、@48 应仍
  ~290。跑了三小时前的初始预测（"len 减半 → 每请求记账减半 → ~30 路"）在
  读源码后**于开跑前撤销**，保留于此作修正轨迹。若实测意外翻正，则说明
  还有未读到的 len 依赖，机制节需重写。判据：@48 ≥ 340 才算翻正。
- **W4 臂（sweep5，`int4_calib.py` + `mtp_bench5.sh`）**：GPTQ **W4A16**
  （int4 / group 128 / symmetric）混合精度——FFN+全注意力 QKV/O（~18B 参数）
  进 int4，**GDN 层（`*linear_attn*`）留 bf16**（递归状态误差累积风险，
  SGLang #22935 同源判断）、`mtp.*` 留 bf16（草稿精度即接受率）、lm_head/
  visual 留 bf16。产物 `<model-dir>-int4`，serving 自动识别 compressed-tensors，
  其余协议与 C2 逐项一致（fp8 KV + k=2 + len 8192 + sched 8192）——单旋钮 =
  权重精度。**预注册预测**：权重 ~30GB→~20GB，池 52 块→~150+ 块，@48
  Running 升到 ≥48，吞吐 **≥420 tok/s**（判据：超 TP2 350.2 达 +20% 才算
  "明确超越"；420 以下算部分兑现，需查块池实际增量）。风险预记：Marlin
  sm89 内核兼容、GDN bf16 与 int4 混载加载、贪心分歧进一步扩大（预期内，
  记录用）。
- ngram 投机解码**不跑**：本 bench 是 random token 数据集，prompt 无重复子串，
  ngram 接受率趋零，测出来只有纯开销——换 workload 才有意义，不破坏本轮
  单旋钮纪律（记录为弃选理由）。

### sweep4 结果（C4，2026-09-11 13:3x 北京时间）：**预注册阴性兑现**

| 臂 | @48 | @48 TTFT | @16 | @16 TPOT | 接受率 pos0/pos1 |
|---|---:|---:|---:|---:|---|
| C2b（len 8192） | 293.25 | 22.0s | 301.37 | 46.3ms | 78.6%/59.9% |
| C4（len 4096） | **298.21** | 21.9s | 297.57 | 46.7ms | 79.8%/61.7% |

- **判据裁决：@48 = 298.21 < 340 → 阴性确认**（vs C2b +1.7%，波动量级）；
  引擎统计与 C2b 完全同相：Running **16-17 / Waiting 31 / usage 90-96%**
  （`sweep4/engine_stats_C4_c48.txt`）——并发墙纹丝不动，机制定稿成立；
- **KV 显存字节两臂完全相同（9.28 GiB）**，但"token 容量"打印从 81,009
  （10.00x）变 48,605（11.87x）——group-aware 容量记账随 len 变，而实际
  Running 不随打印动：**"Maximum concurrency" 打印不是运行时闸门**的直接
  证据（打印 = 池 tokens ÷ max_model_len 的元数据算术）；
- **数值对照通过**：C2 vs C4 贪心探针 **8/8 逐 token 一致**（`greedy_diff_c2c4.py`），
  len/块表行长不改任何贪心 token——A vs C4 的 7/8 分歧全部来自 spec+fp8KV
  （与既有签名一致），进一步把贪心不稳定的归因收窄到量化+verify 前向，
  排除分页布局一类通用因素；
- 结论：**缩 max-model-len 不是解锁键**，与源码推演一致；W4（压权重）成为
  唯一在跑的超基线主牌。

### sweep5 结果（W4，2026-09-11 14:5x 北京时间）：**预注册判据未达，int4 负结果**

| 臂 | @48 | @48 TTFT | @16 | @16 TPOT | KV 池 | KV 显存 |
|---|---:|---:|---:|---:|---:|---:|
| C2b（fp8 全栈） | 293.25 | 22.0s | 301.37 | 46.3ms | 81,920 | 9.28 GiB |
| W4（int4 全栈） | **164.01** | 34.4s | 155.30 | 72.6ms | **124,700** | **14.23 GiB** |

- **判据裁决：@48 = 164.01 ≪ 420 → 大幅未达**，@16 也从 301.4 跌到 155.3
  （-48%）。int4 这张牌在本机型/内核栈上**打输**；
- **容量墙确实松了但没解**：池 81,920→124,700 tokens（KV 显存 9.28→14.23 GiB，
  +53%——省出的 ~5GB 显存兑现了），Running 从 16-17 升到 **27/48**，usage
  波动 54.9%↔98.9%——但 27 路仍不满 48，且每路更慢（见下）；
- **真正的死因是 decode 变慢，不是容量**：@16 TPOT 72.6ms vs C2b 46.3ms
  （+57%）。Marlin kernel 确认在用（启动日志原文
  `Using MarlinLinearKernel for CompressedTensorsWNA16`，无 fallback 警告），
  也就是说 **Marlin int4 GEMM 在 sm89/L20 这套 batch 形状下就是比 fp8 GEMM 慢**：
  低 batch decode 时 weight-only int4 的 dequant 开销吃掉了权重带宽红利
  （27B 模型 16 路 batch 的 GEMM 形状对 Marlin 的分块不友好）。@16 的 -48%
  全部来自这 +57% TPOT：155.3 ≈ 301.4 ÷ 1.57 × (48.2/46.3)；
- **贪心分歧谱：0/8 一致，但语义未崩**：全部 8 题与基线 A 分歧（分歧位置
  0-81，最早第 0 个 token 就翻）——比 fp8 KV 的 7/8 更宽；但输出检查是
  **语义保留的同义改写级**（数学题算到 340+51=391 对、翻译三语全对、riddle
  正确列方程）——int4 误差翻 logits 排序但不摧毁生成质量。这是数值不稳定
  谱系的又一档（bf16 基线 8/8 → fp8 KV 7/8 → spec 6/8 → int4 全翻但语义在）；
- **C-Eval 回归（补跑，坑 10/11 双踩后修通）**：**W4 = 0.7550**（151/200）——
  vs bf16 0.7950 净 -4.0 分、vs fp8 0.7750 净 -2.0 分；配对 McNemar
  w4 vs bf16 = 5:13（不显著，p≈0.096），与 fp8 轮同款结论。与贪心 0/8
  并看是本轮最反直觉的组合：**96-token 生成轨迹全翻，200 题单步 MCQ 只净漂
  4 题**（agree 179/200）——分歧是 logits 排序级的近并列翻转，不是知识损坏。
  W4 质量结论与性能结论合流：int4 在本机型的代价在速度，不在语义；
- **定稿结论**：W4 阴性。@48 的超基线路径在这套内核栈上断在 Marlin decode
  速度，不在容量——容量路线（int4→池+53%、Running 16→27）方向本身被证实，
  但每路 TPOT +57% 的代价抵消并发收益还倒贴。**@16 的「明确超越」答案保持
  C2b（301.4，fp8 全栈 + 显式 sched）**；@48 维持 fp8 KV 无 spec（352.0）。
  下一步若要攻 @48，方向是找比 Marlin 更快的 int4 执行路径（Machete/
  CUTLASS sm89 路径、或 GDN 层也压但换 W8A8），不是再压精度。

## 正确性发现：贪心等价性破了

探针 `mtp_greedy.py`（8 题固定双语 prompt，temperature 0，logprobs 逐 token），
比较脚本 `greedy_diff2.py` / `greedy_diff3.py`：

| 对照 | same / diverge | 解读 |
|---|---|---|
| A vs A2（同轮重启） | 8 / 0 | 探针零噪声 |
| A(sweep1) vs A3(sweep2) | 8 / 0 | 跨轮、跨日、跨服务重启，仍逐 token 一致 |
| A vs M1（spec k=1） | 4 / 4 | **投机解码改变贪心输出** |
| A vs M2（spec k=2） | 2 / 6 | 同上，更深 |
| A3 vs K2（纯 fp8 KV） | 1 / 7 | **KV 量化本身也改变贪心输出** |
| K2 vs C2 | 2 / 6 | 两种扰动互相也不同 |

- 分歧特征：**近并列 logits 的 argmax 翻转**——同一 prompt 在 M1/M2 翻在同一
  位置，翻后输出语义等价（两版都算对 17×23=391、代码都对）；K2 的翻转点与
  M 臂不同但特征相同（"total"→"sum"、标点级改写）；
- 与 C-Eval 口径自洽：fp8 单步 argmax 一致率 95.5%（191/200）→ 96 token
  贪心轨迹上至少一次翻转的概率趋近 1（7/8 实测）；
- 与 vLLM issue [#54928](https://github.com/vllm-project/vllm/issues/54928)
  （open）交叉印证：DFlash2 draft 在同模型上同样改变贪心输出（含 K=1 与
  enforce-eager）——两种不同 draft 机制都翻，共同因子是 verify 前向的
  batch 形状数值不变性（同类问题：vLLM #55238 的 GEMM padding 不变性）；
- **工程含义**：贪心输出对配置扰动不是不变量（量化 / spec / batch 形状各翻各的）。
  回归测试必须用轨迹相似度或 logprob 级比较，不能用精确匹配；生产上
  "temperature=0 保证可复现"只在同配置内成立。

## sweep6 预注册（2026-09-11 15:2x 北京时间，开跑前落字）

用户成功判据（原话意）：**同两卡预算** vs TP2 双卡——单请求更快、总吞吐更大、
精度掉不多 = 一次成功的部署。

**先修正一个口头汇报口误**（详见文末修正轨迹第 5 条）：W8A8 不是未测牌——
A 臂 `--quantization fp8` 即 W8A8 dynamic，fp8 GEMM 是整条阶梯的底座，每路
速度已在 fp8 顶。剩下唯一没打的牌是**部署架构：TP2 vs 数据并行副本**。
且 bf16 52GB 装不进单卡 48GB——**量化是副本架构的前置使能，不是可选加速器**。

臂设计（同一窗口顺序跑；GPU1/2/3——GPU0 被邻座 qserver 占 39GB 不用，
GPU1-3 另有 284MB 邻座足迹）：
- **TP2 锚点**（GPU2,3，历史原配：bf16/len 8192/seqs 128/sched 默认；
  served-model-name 统一成 qwen38-27b 使 bench 命令各臂逐字一致）：
  @48（96 prompts）+ @96（192）——邻座条件下同窗重测；历史值 350.2±3.3
  作对照，锚点偏离历史 >5% 判污染窗，只报同窗相对值；
- **R2b** = 2×（fp8 权重+fp8 KV 无 spec + sched 8192 = K2 配置）：
  @48total（2×[48p, c24]）+ @96total（2×[96p, c48]）；
- **R2c** = 2×（同上 + MTP k=1）：@48total——**新配置**（k=1 快照税减半：
  52 块÷2 = 26 路 ≥ 24，预期无墙）；
- **R2a** = 2×（同上 + MTP k=2 = C2b 配置）：@48total（每副本墙 17 路，
  c24 下 7 路排队）。

**预注册预测**：
- TP2 @48 ≈ 350±10（含邻座条件）；@96 ≈ 350-390（近饱和）；
- R2b @48total ≈ 540-620；@96total ≈ 680-720（2×K2@48=352）；
- R2c @48total ≈ 560-640（M1@16 269.7 基数 + 8 路增量，无墙假设下）；
- R2a @48total ≈ 560-600（每副本墙内饱和 ~290-300）；
- TPOT：TP2 @48 按吞吐推算 ~137ms（350.2/48）；副本对 46-90ms——
  预测单请求延迟也占优。TTFT：R2a 因 7 路排队会差（~5-12s），R2b/R2c 好。
- **判据**：@48total ≥ 420（超 TP2 +20%，与 W4 同口径）= 明确超越；
  精度沿用 C-Eval（副本 = 同引擎配置无新数值漂移；fp8 0.775 vs bf16
  0.795，McNemar n.s.），不新跑。

**公平性**：同总 prompt 数（@48total=96、@96total=192）、同 bench 工具/
workload/指标、TP2 历史原配复现、副本对内两卡同配。本对比是**部署层合成**
（架构+量化双变量）——单旋钮归因已在 sweep1-3 完成，此处回答「给定两张卡
怎么部署最好」；bf16 单卡装不下 → 2×bf16 副本对照组物理不存在，量化使能
副本这一因果在结论里必须明说。

### sweep6 结果（2 卡部署矩阵，2026-09-11 16:3x 北京时间）：**三个副本配置全部「明确超越」**

| 配置（两张卡） | @48total | vs TP2 同窗 | vs TP2 历史 | TPOT | TTFT | 每副本引擎态 |
|---|---:|---:|---:|---:|---:|---|
| TP2 bf16 锚点（同窗重测） | 372.07 | — | +6.2%（见下） | 104.2ms | 6.10s | Running 45-48 满载，usage 43.9% |
| TP2 锚点 @96（192p, c96） | 469.62 | — | — | 159.2ms | 10.87s | — |
| R2b：2×（fp8+KV fp8 无spec） | **548.17** | +47.3% | +56.5% | 66.5ms | 5.42s | Running 24/24，usage 50.8% |
| R2c：2×（同上+MTP k=1） | **618.76** | **+66.3%** | **+76.7%** | **55.6ms** | **4.90s** | Running 24/24，usage 94.4% |
| R2a：2×（同上+MTP k=2） | 578.49 | +55.4% | +65.2% | 44.5ms | 7.39s | Running 14-17+Waiting 7-8（墙） |
| R2b @96total（2×[96p,c48]） | 622.74 | +32.6% vs 469.62 | — | 101.0ms | 8.18s | 引擎态未采（见证据缺口） |

> **⚠️ 本节的数字是 sweep6 自己的窗口，不要再当头条引用（2026-09-11 晚）。**
> sweep7 用同一窗口把这条链拆成四因子后，**R2c@48t 这一格从 618.76 改成 652.98**，
> 同窗总增益从 **+66.3% / +60.1% 定为 +74.09%**（见〈修正案 4 结果〉）。
> 上表其余各格未被推翻，但「+66%」这个数已作废——**现在对外只有一个数：+74.09%**。

- **判据裁决：@48total ≥ 420——R2b 548 / R2c 619 / R2a 578 全部大幅通过**，
  且不等锚点选谁（同窗 372 或历史 350.2）结论都成立。用户成功判据
  （单请求更快 + 总吞吐更大 + 精度掉不多）**三配置全中，R2c 最优**：
  TPOT 55.6ms（TP2 的 53%@48 / 35%@96）、TTFT 4.90s（TP2 的 80%@48 /
  45%@96）、吞吐 +66%（同窗口径）、精度沿用 fp8 引擎 C-Eval 0.775
  （vs bf16 0.795，McNemar n.s.）；
- **TP2 锚点窗口标记**：372.07 vs 历史 350.2±3.3 = +6.2%，超预注册的 5%
  污染线 → 按预案**以同窗相对值为主**。注：本次窗口邻座 qserver（GPU0
  39GB）在全部 gpu_snapshots 里 util 0%——是驻留显存不是活跃算力，本次
  窗口可能比 09-06 历史窗（当时有活跃共享负载，TP2 跨窗波动 ±48% 有案）
  更干净；无论锚点取哪边，副本对都赢得不可逆；
- **预测对账**：R2b @48t 548（预测 540-620 ✓ 低段）、R2c @48t 619
  （预测 560-640 ✓ 正中）、R2a @48t 578（预测 560-600 ✓）、**R2b @96t
  622.7（预测 680-720 ✗ 低的诚实 miss）**——miss 机制：邻座 284MB 足迹
  把每副本 KV 池从 136,533（历史 K2）压到 129,706（15.83x），c48 档更贴
  容量天花板，叠加两副本+两 bench client 共享宿主 CPU；
- **机制逐位闭合（第三次）**：R2c usage 94.4% ↔ 24 路×2 块 = 48 块 / 池
  ~51 块；R2a 钉死 17 路×3 块 + Waiting 7-8 ↔ 与 sweep3 单卡墙同构——
  **k 是容量旋钮**：每个 k 加一块/请求的快照税，最优 k 取决于每副本并发
  离墙多远（c24 时 k=1 赢 k=2，c≤16 时 k=2 赢——C2b 单卡 @16 301 > k=1
  的 M1 269.7）。接受率 k=1 82.6-83.0%（两副本）与 sweep1 M1 的 81.0%
  同档；k=2 pos0 78.3-79.9% / pos1 58.7-61.6% 与 C2 的 78.6%/59.9% 同档；
- **bf16 副本对照物理不存在**：52GB bf16 装不进 48GB 单卡——量化是副本
  架构的前置使能，这层因果在结论里明说（公平性节预记）。

**证据缺口（诚实记录）**：R2b_c96t 的引擎态未采（脚本每相只调一次
engine_stats，`engine_stats_R2b_c48t.txt` 只覆盖 c48t 轮）；贪心探针
sweep6 未跑（R2b=K2、R2a=C2b 已有谱；k=1+fp8KV 组合未探，无结论依赖它）。

## sweep7 预注册（2026-09-11 北京时间，开跑前落字）

目标不是「再找一个优化」，是**把基线修对、把增益拆开、把天花板钉死**。
sweep1-6 把手段试遍了，但留了三个结构性缺陷，sweep7 先补这个。

### 0. 两条前置纪律（sweep1-6 违反过，本次修正）

1. **基线行必须同窗。** 速览 §0 表基线行现在 `@48：372`（sweep6 窗，09-11
   16:3x）+ `@16：284.9`（09-06 ABBA 窗——**那一窗的 @48 是 350.2±3.3**）。
   同一格两个数字来自两个窗口，正是本项目自己的 ABBA 纪律禁掉的做法。
   → sweep7 在基准窗补测 **TP2 @16**，整行改用同窗值。
2. **单卡线基线要有自己的名字。** §0 表「优化一 同卡 @48：307.6 → 352.9」的
   307.6 **不是**基线 372，是 **L1 =「fp8 权重 + KV bf16」单卡**。那张表实际是
   两条线拼的（双卡线 基线→优化三 / 单卡线 L1→L2→优化二），「每步只动一个
   变量」对自己不成立。→ 文档与口述必须点名 L1。

### 1. 优化手段总账（全量；每个手段只动三样之一：权重字节 / KV 字节 / 算子速度）

| 手段 | 权重字节 | KV 字节 | 算子速度 | 容量轴实测 | 裁决 |
|---|---|---|---|---|---|
| fp8 权重 W8A8 | 2B→1B | — | 原生 MMA，快 | 池 →136,533 | ✅ 已测（L1→L2）：吞吐 +14.7%、TPOT +12.5% |
| fp8 KV | — | 2B→1B | 略慢 | 99,267→136,533（**+37.5%，非 +100%**） | ✅ 已测（L2）；大头 GDN 状态压不动（⟶ 见 §2 口子 1，这条假设未验证） |
| int8 权重 | 1B = **与 fp8 相同** | — | L20 上 fp8 原生、int8 非 | **零增益** | ⛔ 不做（预注册阴性：容量轴等价 fp8） |
| int4 权重 GPTQ W4A16 | 1B→0.5B | — | **Marlin decode TPOT +57%**（46.3→72.6ms） | 81,920→124,700（**+53%**）、Running 16-17→**27** | ❌ 已测阴性：@48 164.0、C-Eval 0.7550 |
| int4 权重 AWQ | 同 int4 | — | **未知——唯一开放口子** | 同 int4 | ⏸ 先用 1 条 grep 判生死（§2 口子 6） |
| int4 KV | — | 无可用 kernel | — | — | ⛔ 不做（vLLM 0.28 `--kv-cache-dtype` 面只有 auto/fp8_*；需一条 `--help` 确认后写入档） |
| MTP k=1 | — | −（+1 回滚快照块/请求） | +（接受率 82.6-83.0%） | 单卡池 136,533→97,757 | ✅ 副本布局最优 |
| MTP k=2 | — | −（+2 块） | +（pos0 78.3-79.9% / pos1 58.7-61.6%） | 池 →81,920 | ✅ **k 是容量旋钮**：单卡 @16 赢（301），双副本 24 路输（578.5 vs 618.8） |
| MTP k=3 | — | −（+3 块） | 接受率必然更低 | — | ⛔ 不做（pos2 接受率低于 58.7% 且块税再 +1） |
| ngram / 外部 draft | — | — | — | — | ⛔ 已弃（random 数据集无重复子串，接受率趋零） |
| fp8 KV × MTP | — | — | — | — | ✅ **正交互用**：fp8 KV 单独 @16 零收益，叠加 MTP 再 +5.5%（机制：verify 每步读 k+1 位置 KV） |
| 前缀缓存 | — | — | — | — | ✅ 已测（@16 −74% 首字 / @48 零收益） |
| 调度预算 batched-tokens | — | — | − | — | ❌ 已否（TPOT −33% 换 TTFT 3×、吞吐 −11%） |
| max-model-len | — | — | — | **0**（不进运行时驻留） | ✅ 已裁决阴性（C4） |
| 副本 / TP / PP 布局 | — | — | — | — | ✅ 副本 > TP2 bf16（sweep6）；**TP2 fp8 未测**（§4 实验 1）；**PD 分离未测**（§2 口子 4） |

### 2. 六个「没被想到、但确实存在」的口子（本次新增，按价值排序）

**口子 1 ★ GDN 状态自身的 dtype。** 文档里「占大头的那部分状态**压不动**」
从未被验证，是假设。用 sweep2 两组数反推：

```
bf16 KV：9.89 GiB /  99,267 tok = 106,983 B/token
fp8  KV：9.58 GiB / 136,533 tok =  75,348 B/token
解得：GDN 状态 = 43,713 B/token（占池 58%），真 KV(bf16) = 63,270 B/token
```

KV 量化只动了那 42%。**若 vLLM 以 fp32 存 GDN 状态**（混合架构常见），压到
bf16 即状态 ×2 → 池 75,348 → 53,491 B/token，**容量 ×1.41**，不动权重、不动
算子、不碰精度敏感的 1/4 层。**先花 1 分钟读源码确认 dtype**，再决定是否排实验。
这一条若成立，容量轴最高优先级立即改判。

**口子 2 CUDA graph 捕获桶 vs 实际 batch 分布。** `cudagraph_capture_sizes` =
`[1,2,4,8,...,256]`；bench 要求 24 路但引擎态 Running 实际在 **18/20/24 间跳**。
批量落在两桶之间即退 eager/piecewise——纯性能损失，全程未看过。手段：显式指定
更密的桶（如 20-28 每 2 一格）。可解释 R2c TPOT 的非整数倍关系。

**口子 3 `--gpu-memory-utilization` 0.92→0.95。** 从未扫过。+0.01 ≈ +0.45 GiB
≈ 2 块；+0.03 = +1.35 GiB ≈ 7 块 → k=1 每请求 2 块 → **每副本 +3 路，双副本
48→54 路**，量级 +12%。最省事，风险是贴边界被邻座抢显存。

**口子 4 PD 分离（1 卡 prefill + 1 卡 decode）。** 两卡预算下**唯一未测的第三种
布局**，且正对我们最差的指标（TTFT 6.1s/4.9s）。混合架构额外有利：GDN 状态是
每序列固定大小、不随 token 长，跨卡传输量远小于纯 KV 模型。成本高（两引擎 +
KV 连接器），上窗口前先查 vLLM 0.28 对混合架构 PD 的支持程度。

**口子 5 `--enforce-eager`：把 CUDA graph 池还给 KV。** 0.55-1.13 GiB ≈ 3-6 块
≈ 每副本 +1.5-3 路。代价是每步重发射 kernel（mini-vllm 对比已证 host 开销在
小 batch 下占比可观）。**不是为了用它，是为了量出「graph 池值几路」。**

**口子 6 AWQ：1 条 grep 判生死，不要用窗口赌。** vLLM 在 sm80+ 会把 AWQ 自动
升级成 `awq_marlin`——若是，则我们测到的 +57% 不是「GPTQ 的问题」而是
**「Marlin 这一族 kernel 在 sm89 低 batch decode 上的 dequant 开销」**，这是更硬
的结论，AWQ 直接判死。若存在原生 awq kernel 路径才排实验，判据：**TPOT ≤ 50ms
（dequant 开销 < fp8 的 1.1 倍）且 @48 > 420**，两条都中才算翻案。

**附带（不是手段，是混杂）**：卡对差异。B0/B1 在 GPU2+3、R2 在 GPU1+2，且 GPU0
有邻座 43.8GB 常驻。**同配置在 (0,1)/(1,2)/(2,3) 上的差异从没测过**——这是唯一
能让 619 vs 372 失效的东西，跑一次当「卡对基线」。

### 3. 指标清单（五层；无分母的数字不许进表）

**A 端到端（`vllm bench serve`）**——对外只报这层：输出吞吐（**必同报成功请求数
+ Benchmark duration** 以便验算）、**TTFT mean/median/P99**、**TPOT
mean/P50/P99**（开 MTP 时必须与接受率同报，否则一次前向多 token 会稀释 TPOT，
单看 55.6ms 是假数）、Peak concurrent、Failed（>0 即整组作废）。

**B 引擎内部（`/metrics` + `loggers.py`）**：Running/Waiting、**GPU KV cache
usage %**、prefix cache hit rate（random 数据集恒 0，必须声明）、投机接受率
（**分位置**，分母显式）。**采样时点改为每 bench 点 3 次（前/中/后）**——sweep6
整相只采一次，R2b_c96t 的引擎态就是这么丢的。

**C 显存台账（启动日志 + 负载中 nvidia-smi）**：权重 / 峰值激活 / CUDA graph 池 /
KV+Mamba 池（`Available` GiB + `N tokens` + `X.XXx`），**四项加总必须闭合到
0.92 × 44.5**。sweep6 的 grep 漏了 `Available KV cache memory` /
`Estimated CUDA graph memory` / `model weights take` / `non-default args`，本次全采。

**D 数值**：C-Eval 200 题 + McNemar；贪心逐 token 一致率 n/8 探针。

**E 窗口卫生**：in-window 锚点首尾偏差 **>3% 整窗作废**；每相邻座 GPU 快照；
负载中 GPU 利用率快照。

### 4. 实验设计

窗口顺序 **`B0锚 → B1 → R2c → R2b扫描 → B0锚`**，全程同窗，B0 首尾偏差定窗口有效性。

| # | 臂 | GPU | 配置 | 动的旋钮 | 对照 | 成本 |
|---|---|---|---|---|---|---|
| 0 | **B0** | 2,3 | TP2 bf16（现基线） | — | — | 已有 |
| 0 | **B0-16** | 2,3 | 同上 @16 | 无（补测） | 修 §0-1 跨窗 | 1 bench |
| 0 | **B0-ledger** | 2,3 | 启动即采全台账 | 无（补采） | 修 §0-1 缺口 | 0 |
| 1 | **B1** | 2,3 | TP2 **fp8 权重 + fp8 KV** | **量化** | B0 → **拆因** | 1 serve/1 bench |
| 2 | **R2c** | 1,2 | 2× fp8 副本 + k=1 | 布局 | B1 @48 + 复现 618.8 | 已有/1 bench(@72t) |
| 2 | **R2b** | 1,2 | 2× fp8 副本 k=0 | spec 深度 | R2c | @120t/@144t |
| 3a | grep | — | AWQ marlin 升级条件 | — | 判口子 6 | 1 分钟 |
| 3b | R2c-16 | 1,2 | k=2 @16total | spec 深度（离墙远） | k=1 @16total | 1 bench |
| 3c | AWQ | 1,2 | 仅当 3a 判活 | 量化算子 | B1 | 1 serve/1 bench |
| 3d | 精度 | — | C-Eval 仅当 B1 为新配置 | — | fp8 0.775 | ~30min |

**拆因是本次核心**（§0-2 的缺陷 3）：`372 (TP2 bf16) --量化--> X (TP2 fp8)
--副本替代 TP--> 619 (R2c)`。它同时回答一个必被问、现在答不上的问题：
**「fp8 既然能装单卡，双卡为什么不量化完接着 TP2？」** 现在只能说「副本比
**bf16 的** TP2 好」，不能说「副本比 TP2 好」——B1 就是那个缺的数。

### 5. 明确不做（逐条理由，免得被问时现编）

int8（与 fp8 同 1 字节/参数，容量轴零增益）、int4 KV（该版本无 kernel）、
MTP k=3（接受率与块税双输）、TP4（PCIe 无 NVLink，TP2 已证通信受限）、
4 卡（与基线无关，另窗口独立问题）、TP2 @144（信息量低；TP2 的极限是延迟问题
不是容量问题——@48 时 usage 仅 43.9%）。

### 6. 公平清单

同总 prompt 数；同 bench 工具/workload（random 1024/256、temp 0）/指标；
每臂启动即采台账；每点 3 次引擎态；解析器对真实输出自测过（坑 12 的 nan）；
每相邻座快照；**卡对差异声明**（B0/B1 在 GPU2+3 vs R2 在 GPU1+2，若 B1 在两
卡对差异 >5% 则结论必须声明此混杂）；prefix cache 恒 0 声明。

### 7. 预注册预测与判据

- **B0-16**（基准窗 TP2 @16）≈ 270-310。sweep6 窗的 @48 比历史高 6.2%，
  @16 可能同向偏高。**锚点判据**：B0 首尾偏差 >3% → 整窗作废。
- **B1**（TP2 fp8）@48 ≈ **430-520**（权重读减半改善 TPOT，372→~480 量级）。
  **判据双向都有故事**：≥420 则「量化在双卡上单独成立」（且 619 的 +66% 可拆为
  量化 × 布局两段）；若 ≤372 则「量化在 TP2 布局上零收益甚至负收益」——
  那 372→619 全部来自布局，量化只是**使能器**，这同样是干净的结论。
- **R2b @120t/@144t**（60/72 路每副本，墙在 ~47）≈ 640-700 但 **TTFT >8s**
  （过墙排队）。**判据**：@144t 若 >650 且 TTFT <8s → 天花板不止 620，实验 2 翻案。
- **R2c @72t**（36 路每副本，超 24 路墙）≈ 600-650 且 **Waiting 暴涨**
  ——验证 94.4% 是真墙而非采样点巧合。
- **R2c-16（k=2 @16total）**：若 k=2 赢 k=1 → **动态 k 策略成立**（低负载 k=2、
  高负载退 k=1），这是可直接进生产的结论；若输 → 「k 是容量旋钮」进一步收敛为
  「墙远用 k=2、墙近用 k=1」的单调解。

### 8. 脚本改动清单（设计定了才动脚本）

相对 `mtp_bench6.sh`：①`startup_lines` 补采四项台账（§3-C）；②`summarize_dual`
正则改 `[^\n:]*:`（坑 12 的 nan，已在 bench7 草稿修好并自测）；③`engine_stats`
每点 3 次；④新增 B1 臂与 B0-16 补测；⑤4 卡相位改 opt-in（`RUN_4CARD=1`）——
基线是两张卡，4 卡不参与基线对比。

### B0 结果（2026-09-11 12:27:12–12:32:20 容器 UTC，本次实跑）：**锚点复现，@16 预测落空**

服务端 non-default args 与 sweep6 的 TP2 臂**逐位相同**（`max_model_len 8192` /
`tensor_parallel_size 2` / `max_num_seqs 128` / 无 `quantization`，vLLM 0.28.0 stock）；
邻座条件也相同（GPU0 43.8GB 外部作业 + GPU1-3 各 284MB 足迹），所以这一行的
跨窗比较成立。证据 `sweep7/`。

| 指标 | B0 @16 | B0 @48 | sweep6 TP2 @48 | B0@48 vs sweep6 |
|---|---|---|---|---|
| Output token throughput (tok/s) | 239.61 | **375.09** | 372.07 | **+0.81%** |
| Benchmark duration (s) | 102.57 | 65.52 | 66.05 | |
| Successful / Failed | 96 / 0 | 96 / 0 | 96 / 0 | |
| Total input / generated tokens | 103339 / 24576 | 103325 / 24576 | 103333 / 24576 | |
| Request throughput (req/s) | 0.94 | 1.47 | 1.45 | |
| Peak output tok/s | 368.00 | 816.00 | 816.00 | 逐位相同 |
| Peak concurrent requests | 23.00 | 52.00 | 54.00 | |
| Mean / Median / P99 TTFT (ms) | 2232.06 / 2172.78 / 6016.44 | 5802.17 / 2280.42 / 17680.08 | 6098.02 / 2270.61 / 18261.93 | |
| Mean / Median / P99 TPOT (ms) | 58.15 / 58.17 / 63.85 | 104.28 / 116.35 / 122.29 | 104.16 / 116.24 / 122.70 | 细节级吻合 |
| Mean / Median / P99 ITL (ms) | 57.93 / 45.29 / 624.37 | 103.87 / 62.22 / 644.54 | 103.75 / 62.27 / 641.57 | 细节级吻合 |

**锚点复现**：375.09 vs 372.07 = **+0.81%**（判据 3%）。而且本窗跑了两次独立 boot
（#2 那次 @16 用了 32 条，@48 未受其影响）——两次 @48 = 374.96 / 375.09，
**两次 boot 相差 0.03%**；Median TPOT 116.35 vs 116.24、Mean ITL 103.87 vs 103.75、
Peak output 816.00 逐位相同。这是本线第一次拿到同配置的**两 boot 重复性**。

**污染已消除**：8 个 engine_stats 采样（@16 ×3、@48 ×3，另 @16 的 s1 在首个 8s
窗口内引擎还没打印）里 `Prefix cache hit rate` **全为 0.0%**；口径字段现在随
`bench_*.log` 的客户端 Namespace 一起入库，可反查：
`seed=701/702`（每点独立）、`num_prompts=96`、`max_concurrency=16/48`、
`random_input_len=1024`、`random_output_len=256`、`temperature=0.0`、
`request_rate=inf`、`num_warmups=0`、`backend=openai-chat`。

**台账闭合**（sweep6 缺的那四项，本次采到）：`Available KV cache memory 13.12 GiB`
（sweep6 13.11）、`GPU KV cache size 264,071 tokens / 32.24x`（与 sweep6 逐位相同）、
`Estimated CUDA graph memory 0.61 GiB` → `actual 0.6 GiB（差 3.0%）`、
`dtype=torch.bfloat16 / quantization=None / kv_cache_dtype=auto`。

**@16 预注册预测落空**（§7 预测 270-310，实测 **239.61**），落空的方向也和预注册
的理由相反（预测说"可能同向偏高"，实际比 284.9 低 15.9%）。机制本次才看清：
**@16 是客户端并发受限点，不是引擎饱和点**——全程 `Running: 16 / Waiting: 0 /
KV usage: 14.6%`，`--max-concurrency 16` 把在飞请求钉死在 16。96 条的 duration
102.57s 恰是 32 条（34.56s）的 **2.97 倍**，而速率 239.61 vs 237.07 只差 **1.1%**：
**在客户端受限点上，`--num-prompts` 只改时长、不改速率**。

对 §0 的影响（**结论：修不了，只能撤下或标注**）：09-06 那行不是"条数不对"能解释的。
而且方向自相矛盾——同一批 09-06 ABBA 的 @48 = 350.2，本窗 @48 = 375.09（**+7.1%**）；
@16 则是 284.9 → 239.61（**−15.9%**）。一高一低不同向，"机器快慢"解释不了，仓库里
也查不到 09-06 那行的口径字段（文档缺口，见坑 17c）。**本窗自洽的基线是
@16 = 239.61 / @48 = 375.09**；头条比例用同窗重算：**652.98 / 375.09 = +74.09%**
（此处原写「618.8 / 375.09 = +65.0%」，终点已由修正案 4 更正为 652.98）。

**本次没测到 / 没测全的**（下一相位前先看这段）：

1. **负载中的 GPU 利用率没采到**——`gpu_snap` 只在相位端点触发，相位两头都空闲，
   所以 `utilization.gpu` 恒 0%（功耗 218-224W、61°C 倒是把负载痕迹带上了，但滞后）。
   修法：3 次引擎态采样的同一个循环里插一次 `gpu_snap`。
2. **曲线中段无点**：@16 与 @48 之间（如 @32）没有读数，两卡基线只能给两端。
3. 每点 3 次引擎态只覆盖 102s / 65s 里的 24s，且 @16 的 s1 落在引擎打印之前。
4. **B0' 还没跑**，窗口首尾判据（§7 的 >3% 作废）尚未兑现。

### B1 结果（2026-09-11 12:34:08–12:39:32，拆因臂）：**量化单独成立 +30.1%，但拆因同时挖出两个口径问题**

同 GPUs、同窗；相对 B0 只多了 `--quantization fp8 --kv-cache-dtype fp8`。

| 指标 | B1 @16 | B1 @48 | B0 @16 | B0 @48 | B1@48 vs B0@48 |
|---|---|---|---|---|---|
| Output token throughput (tok/s) | 330.70 | **488.01** | 239.61 | 375.09 | **+30.1%** |
| Benchmark duration (s) | 74.31 | 50.36 | 102.57 | 65.52 | |
| Successful / Failed | 96 / 0 | 96 / 0 | 96 / 0 | 96 / 0 | |
| Mean / Median / P99 TTFT (ms) | 1219.97 / 919.64 / 4981.73 | 4265.53 / 957.81 / 14666.97 | 2232.06 / 2172.78 / 6016.44 | 5802.17 / 2280.42 / 17680.08 | |
| Mean / Median / P99 TPOT (ms) | 43.57 / 44.39 / 46.56 | 80.58 / 91.59 / 93.73 | 58.15 / 58.17 / 63.85 | 104.28 / 116.35 / 122.29 | −22% |
| Mean / Median / P99 ITL (ms) | 43.40 / 30.31 / 306.94 | 80.26 / 44.21 / 319.62 | 57.93 / 45.29 / 624.37 | 103.87 / 62.22 / 644.54 | |
| Peak concurrent / Peak out tok/s | 21.00 / 545.00 | 53.00 / 1104.00 | 23.00 / 368.00 | 52.00 / 816.00 | |

@16 = 330.70 vs 239.61（**+38.0%**）；@48 = 488.01 vs 375.09（**+30.1%**）。
判据 §7「≥420 则量化在双卡上单独成立」→ **兑现**（488 ≥ 420）。**30 个引擎态采样
的 prefix hit 全为 0.0%**，per-point seed 在 fp8 臂上同样有效。
注意 **B1 @48 的 KV 占用只有 13.6%**（B0 是 43.9%）：488 是带宽/算子读数，
**不是容量读数**——TP2+fp8 在这一载荷下离墙还很远（池 83.5x 并发余量）。

#### 显存台账（用户点名要的；四项闭合，能验算）

| 项 | B0（bf16） | B1（fp8 权重 + fp8 KV） | 变化 |
|---|---|---|---|
| `Model loading took` | 25.69 GiB | 14.31 GiB | −44.3% |
| consumed memory（权重 + 非 torch） | 25.85 GiB | 14.63 GiB | −11.22 |
| peak activation | 1.99 GiB | 2.34 GiB | +0.35 |
| CUDAGraph 池（actual / estimated） | 0.60 / 0.61 GiB（差 **3.0%**） | 1.16 / 0.97 GiB（差 **16.4%**） | ×1.93 |
| **KV + Mamba 池** | **13.12 GiB / 264,071 tok / 32.24x** | **23.98 GiB / 684,032 tok / 83.50x** | **+82.8% GiB / +159% token** |
| 四项加总 | 41.56 GiB | 42.11 GiB | 预算 = 0.92 × 44.52 = **40.96** |

**闭合算式（两条都逐位对上）**：

```
KV = 0.92 × 44.52 − consumed − peak_activation
B0: 40.96 − 25.85 − 1.99 = 13.12  → 日志报 13.12  ✓
B1: 40.96 − 14.63 − 2.34 = 23.99  → 日志报 23.98  ✓（舍入）
```

→ **CUDAGraph 池记在 0.92 预算之外**：四项加总比预算多出的量（+0.60 / +1.15）
几乎正好等于 graph 池（0.60 / 1.16）。这正是 vLLM 自己那行提示的含义——
「0.9200 ≡ 0.9062（不含 graph profiling）」，并给出把 utilization 提到
**0.9338（B0）/ 0.9418（B1）** 才能保住同等 KV 的换算。**顺带一条操作结论：
graph 池估计器在 fp8 下误差 16.4%（bf16 只 3.0%），按估计值排 KV 会偏大。**

**fp8 KV 只砍了池子里那 1/4**：字节/token 从 106,742 → 75,302（×0.705）。
反解：不变部分（mamba 状态，占多数）43,713 B/token 保持 bf16，注意力 KV
63,270 → 31,635（对半）→ 合计 75,348，与实测 75,302 差 **0.06%**。**这就是
KV 容量只涨 83%（GiB）而不是 100% 的原因**，同时独立复核了此前推出的
43,713 B/token 不变量分解。**换句话说：想让 KV 池再翻倍，动 fp8 KV 已经到顶，
剩下的 58% 被 GDN/mamba 状态的 bf16 下限锁死了**（与源码核查结论一致）。

#### 拆因挖出的两个口径问题（本次最重要的产出，比 +30% 本身重要）

**问题 1：`max_num_batched_tokens` 的默认值随 usage context 分档，四条臂里
两条口径不同。** vLLM 0.28.0 `engine/arg_utils.py:2578` 起，非 H100/A100 硬件：

```python
default_max_num_batched_tokens = {UsageContext.LLM_CLASS: 8192,
                                  UsageContext.OPENAI_API_SERVER: 2048}
```

`vllm serve` 走 OPENAI_API_SERVER → **默认 2048**；sweep6 的副本臂显式传了
`--max-num-batched-tokens 8192`（各臂 non-default args 里逐条可见）。也就是
**B0/B1 = 2048 而 R2b/R2c = 8192，预填充步长上限差 4 倍**——sweep6 那个
**+66% 里混着这个旋钮**，而它此前从未被列进公平清单。（附带一条通用坑：
同一台机器上 `LLM(...)` 默认 8192、`vllm serve` 默认 2048，**两种调用方式的
数字默认不可比**。）修法：补一个桥接臂（下面 §9）。

**问题 2：R2 侧只有 @48t 干净。** sweep6 的 R2b/R2c @96t/@120t/@144t 都是同一
boot 里后跑的点，被 seed 复用污染（坑 16）。**这次 per-point seed 修好后，
重跑 R2b/R2c 可以让每个点都干净**——所以 sweep7 的 R2 相位不只是"复现"，
而是把 sweep6 那几个被污染的格子重测出真值。

**当前拆因骨架**（量化段已测，另两段待 B1b/R2 补齐）：

| 段 | 对照 | 比值 |
|---|---|---|
| **量化** | B0 @48 → B1 @48（同 2048） | 375.09 → 488.01 = **+30.1%** |
| 布局 + 调度步长 + 投机（合体） | B1 @48 → R2c @48t | 488.01 → 618.76 = **+26.8%** |
| 合计 | | 1.301 × 1.268 = **1.650** ≈ 618.76/375.09 = 1.650 ✓ |

**注意这张表推翻了 sweep6 的叙事重心**：+66% 里最大的一段是量化（+30%），
不是布局；布局的作用要等 B1b 把调度步长摘掉之后才能定论。
**这直接决定面试那句话说成「副本布局赢 66%」还是「fp8 赢 30%，布局再赢 X%」。**

### 9. 预注册修正案（B1b，2026-09-11 12:5x 落字，**开跑前**）

新臂 **B1b** = TP2 fp8 + fp8 KV **+ 显式 `--max-num-batched-tokens 8192`**，
点 **@48（桥接）与 @96（探 TP2 余量）**。理由：B1 与 R2 臂在布局之外还差着
调度步长，不补这个桥接点，布局那一段无法单独归因；@96 则回答「B1 只有
13.6% KV 占用，488 是不是被载荷限住了」。

**双尾判据（不硬编方向，因为我没有可靠先验）**：
- B1b@48 落在 488 ± 3% 内 → 2048 vs 8192 对该臂中性 → sweep6 的 +66% 不含此项
  混杂，拆因只需「量化 / 布局(+投机)」两段；
- 显著偏离 → 该旋钮进公平清单，sweep6 的 +66% 必须按三因子重算，且本节表格
  「布局+调度步长+投机」那一段要再拆一次。
- B1b@96 若 ≥ 600 → 说明 TP2+fp8 在 96 并发下能追平 R2c 的 618.8，
  **「副本布局»TP2」这个论断在等并发下不成立**，必须改口径表述（等饱和度对照）；
  若 ≤ 550 → 布局优势独立存在，sweep6 的叙事保住。

### B1b 结果（12:52:57–12:54:56，桥接臂）：**调度步长 +4.5%，四因子拆因闭合，但预注册判据有个洞**

| 指标 | B1b @48 | B1 @48 | B1b @96 |
|---|---|---|---|
| Output token throughput (tok/s) | **509.83** | 488.01 | **587.45** |
| Benchmark duration (s) | 48.20 | 50.36 | 41.83 |
| Successful / Failed | 96 / 0 | 96 / 0 | 96 / 0 |
| Mean / Median / P99 TTFT (ms) | 6204.17 / **5502.37** / 13253.98 | 4265.53 / **957.81** / 14666.97 | 13566.40 / 13169.91 / 25355.87 |
| Mean / Median / P99 TPOT (ms) | 69.98 / **71.44** / 91.46 | 80.58 / **91.59** / 93.73 | 109.82 / 111.44 / 153.59 |
| Peak concurrent / Peak out tok/s | 70.00 / 1104.00 | 53.00 / 1104.00 | 96.00 / 1536.00 |

台账同样闭合：40.96 − 14.84（consumed）− 2.34（peak act）= **23.78 GiB** ✓
（报 23.78）；池 677,888 tok / 82.75x（比 B1 少 0.7%，8192 步长多吃了一点
activation 与缓冲）。23 个引擎态采样 prefix 全 0.0%。

**判据裁决**（§9 双尾）：
- **@48 = 509.83，比 B1 的 488.01 高 +4.5% → 超出 ±3% 带**。按预注册「显著偏离
  → 该旋钮进公平清单，sweep6 的 +66% 必须按三因子重算」执行（下面已重算）。
  **旋钮本身的机制**：8192 步长下 Median TTFT 从 958ms 掉到 5502ms（×5.7），
  而 Median TPOT 从 91.59 改善到 71.44（−22%）——**`max_num_batched_tokens`
  是 TTFT↔TPOT 的交换旋钮，净吞吐 +4.5% 不是免费的**。这个方向在不同臂上还不
  一样：sweep3 的 C2b（单卡 fp8+k=2）改同一旋钮只有 +0.6%，所以它是**arm 相关**
  的，正好说明"跨臂默认值不一致"这件事不能靠"大概没事"糊过去。
- **@96 = 587.45 → 落在判据的洞里**：我写的是 ≥600 翻案 / ≤550 布局成立，
  实测 587.45 **两边都不占**。**这是我预注册没写严，不是数据的问题**——补一条
  更该问的对照：等载荷比较应是 B1b@96 vs R2 的 @96t，而 R2b@96t = 622.74 是
  被污染的（sweep6 同 boot 后跑的点），干净值未知——**所以这个格子现在测不了，
  只能靠重跑 R2 拿真值**。⚠️ 不要拿 B1b@96 去和 R2c@48t 的 618.76 并列，
  那是 96 路 vs 48 路，不是等载荷。

#### 四因子拆因（全部实测，乘积闭合到 0.03%）

> **⚠️ 两个更正（2026-09-11 晚）。**
> **① 本表是「跨窗」版**（布局/投机两段用的是 sweep6 窗口的 548.17 / 618.76）。
> 同窗定稿版见下文〈延展窗结果〉与〈修正案 4 结果〉，终点已从 618.76 改为 **652.98 / +74.09%**。
> **②「乘积闭合到 0.03%」不是证据，是恒等式**——四段是按顺序相邻节点的比值，
> 乘积必然等于 end/start（telescoping）。这里 0.03% 只是显示精度。
> 唯一有信息量的是总量本身与每个比值各自的窗口有效性。

| 段 | 对照（同一 @48=96 条口径） | 增益 |
|---|---|---|
| **量化** bf16 → fp8 权重+KV | B0 375.09 → B1 488.01 | **+30.1%** |
| **调度步长** 2048 → 8192 | B1 488.01 → B1b 509.83 | **+4.5%** |
| **布局** TP2 → 2 副本（fp8, k=0） | B1b 509.83 → R2b@48t 548.17 | **+7.5%** |
| **投机** k=0 → k=1（同布局） | R2b@48t 548.17 → R2c@48t 618.76 | **+12.9%** |
| 合计 | 375.09 → 618.76 | **+65.0%** |

1.301 × 1.0447 × 1.0752 × 1.1289 = 1.6499 → 375.09 × 1.6499 = **618.9**，
与实测 618.76 差 **0.02%**。**这就是 sweep6「+66%」的全部来源，四段各自带分母。**
（注意：布局段用的 R2b@48t 来自 sweep6 窗口，等 sweep7 重跑复现后才算同窗定稿；
跨窗漂移的已知量级是 B0 锚点的 +0.81%。）

#### 预注册修正案 2（B1c，2026-09-11 13:1x 落字，**开跑前**）

**问题**：B0/B1/B1b 在 **GPU2+3**，R2 全在 **GPU1+2**——公平清单里那句
「若两卡对差异 >5% 则结论必须声明此混杂」至今**没有任何测量**，而布局段只有
+7.5%，一个卡对效应就足以改写它。
**新臂 B1c** = B1b 的配置，换到 **GPU1+2**，只跑 @48 一个点。
**判据**：|B1c@48 − B1b@48| ≤ 3% → 布局段可继续用 B1b 作分母；> 3% →
布局段的分母必须换成 B1c，四因子表要重算。**预测：|差| < 3%**（同型号卡、
两对都空闲、都无 NVLink；若真差这么多，那本身就是个该进结论的发现）。

### B1c 结果（13:12:59–13:14:06，卡对控制臂）：**判据通过，卡对效应 2.21%**

| 指标 | B1c (GPU1+2) | B1b (GPU2+3) | 差 |
|---|---|---|---|
| Output tok/s @48 | **521.12** | 509.83 | **+2.21%** |
| duration (s) | 47.16 | 48.20 | −2.2% |
| Median TTFT (ms) | 5341.45 | 5502.37 | −2.9% |
| Median TPOT (ms) | 70.55 | 71.44 | −1.2% |
| 台账 Available KV | 23.76 GiB | 23.78 GiB | −0.02 GiB |

**裁决：|2.21%| ≤ 3% → 布局段继续用 B1b 作分母，四因子表不改。** 预测成立。
代价要记：卡对效应给布局段引入 **≤2.2% 的不确定度**，结论里必须标注（不到 3%，
按预注册不重算，但也不能当成 0）。副产品：两对卡的 KV 池只差 0.02 GiB，
说明这个规模上换卡对几乎不影响容量。

### R2c / R2b 结果（13:18:16–13:28:44）：**天花板不在 @96t，在 @120t**

R2c (k=1) 与 R2b (k=0) 都在 GPU1+2，fp8 权重 + fp8 KV + 8192 步长，per-point seed。
每副本 lanes = max-concurrency，两副本同一时刻各跑一条 bench。

| 臂 | 每副本 lanes | Output tok/s (TOTAL) | duration (s) | Median TTFT (ms) | Median TPOT (ms) | KV 峰值 / Waiting |
|---|---|---|---|---|---|---|
| R2c_c48t | 24 | 600.40 | 40.96 | 3539 / 3865 | 56.20 / 57.28 | 93.9% / 0 |
| R2c_c72t | 36 | 664.45 | 55.45 | 6378 / 6466 | 60.67 / 61.12 | 97.8% / 11 |
| R2b_c96t | 48 | 622.58 | 78.63 | 6730 / 6912 | 104.97 / 105.73 | 46.6%（prefill 受限采样）|
| R2b_c120t | 60 | **682.96** | 89.53 | 9062 / 9286 | 112.13 / 113.10 | 99.5% / 1 |
| R2b_c144t | 72 | 653.79 | 112.27 | 13129 / 13212 | 112.44 / 113.20 | 99.5% / 13 |

**结论 1：@96t 不是天花板。** sweep7 预注册写的是「@120t/@144t 夹逼 622.7 是拐点
还是悬崖」，实测**两者都不是**：@120t 更高（+9.7%），@144t 才回落（−4.3%）。
峰值在 **60 lanes/副本 = 682.96 tok/s**，这是本项目至今最高的实测吞吐，
而且它比「+66%」那个被反复引用的 618.76 还高 10.4%。

**结论 2：k=1 的并发上限只有 k=0 的一半（~25 vs ~47），但原因还没查清。**
R2c_c72t 在 36 lanes 下只跑得动 25 条（Waiting 11，KV 97.8%）；R2b 能跑到 47 条
（KV 99.5%）。两个臂的 KV 池几乎一样大（9.28 vs 9.11 GiB），所以这不是池子大小
能解释的。**这条只能标成「已观测、未归因」**——见下面「没测到/没测全」。
因此「k=1 在高负载下会反超」这个说法**现在还不能下**：我手上只有
k=0@60lanes (682.96) > k=1@36lanes (664.45)，lane 数不同，**交叉点本身没测**。

#### 跨窗校验：三格三个方向，第三格必须解释

| 格 | sweep6 | sweep7 | Δ |
|---|---|---|---|
| B0@48（锚点） | 372.07 | 375.09 | +0.81% |
| R2b@96t | 622.74 | 622.58 | **−0.03%** |
| R2c@48t | 618.76 | 600.40 | **−2.97%** |

第二格值得单独说：sweep6 的 R2b 顺序是 **c48t 在前、c96t 在后**（`mtp_bench6.sh:145-151`），
两格都是 seed 0，所以 c96t 确实复用了 c48t 那 48 条 prompt（坑 16 的机制成立），
而且它的引擎态**从来没被采样过**（脚本只对 c48t 调了 engine_stats）。
但 sweep7 用冷启动 + 自己的 seed 重测，**复现到 −0.03%**。所以「污染过」是事实，
「污染改变了数值」不是——这一格现在由两次独立测量互证，比任何关于污染的理论都硬。

**第三格当时的解释：「MTP 接受率是『prompt 集』的函数」。**

| R2c_c48t | drafts | accepted | 接受率 |
|---|---|---|---|
| sweep6 p1 / p2 | 6716 / 6701 | 5547 / 5565 | 82.59% / 83.05% |
| sweep7 p1 | 6778 | 5486 | **80.94%** |

同样 12288 个输出 token，sweep7 需要 **+0.92%** 的解码步数（6778 vs 6716）；
duration 39.50 → 40.96 = +3.70%。步数解释其中约 1/4，剩下 ~2.8%
是未归因的跑间方差（两个窗口的 host 负载不同）。**这不是事后编的解释，
是 drafts/accepted 两个计数器直接读出来的**（`vllm:spec_decode_num_drafts_total` /
`num_accepted_tokens_total`）。

> **⚠️ 本段的两个结论后来都被推翻，标题留在这里作为纠错轨迹：**
> **① 「剩下 ~2.8% 是 host 负载」→ 已证伪**（R2c0b 在 loadavg 15.10 下仍得 651.60）。
> **② 「接受率是函数」→ 已撤回**（修正案 3：接受率排序与吞吐排序相反）。
> **③ 「k=1 天生多一个方差项」→ 被 R2cAB 的 +2.46% 量化为「有，但很小」**，
> 而 600.40 那 2.97% 里池只占 2.46pp 的一部分。
> 方法论只有一条还站着：**k=1 的跨窗比较要同 seed**——但 坑 21 说明这一轮连
> 「同 seed」都没真正做到（跨 boot 撞号），是修正案 4 的 SB/R2b48z/R2cAB 才补上的同池对照。

### B0p 结果（14:36:35–14:41:43，收尾锚点）：**窗口有效，漂移 0.09%**

| 指标 | B0 | B0p | Δ |
|---|---|---|---|
| Output tok/s @48 | 375.09 | 374.74 | **−0.09%** |
| duration (s) | 65.52 | 65.58 | +0.09% |
| Median TTFT (ms) | 2280.42 | 2289.42 | +0.4% |

`WINDOW CHECK B0 vs B0p: drift=0.09% -> VALID`。整个 sweep7 窗口
（12:27 → 14:41，跨 2 小时 14 分）有效，窗内所有臂可直接互比。

> 顺带修掉一个会静默失效的门：B0p 相位的判据原本写的是 `want B0p && want B0`，
> 而 `want` 查的是 **PHASES 列表**不是文件。所以 `PHASES=B0p` 单独跑会**什么都不做
> 且不报错**——和坑 18 同一种失败形态（判据只可能静默失败）。已改成
> `[ -s $OUT/bench_B0_c48.log ]`：窗检需要的是那个产物，产物在，括号就有意义。

#### 预注册修正案 3（R2b48 + R2c0 + B0''，**脚本落定并开跑 14:43:35，本文 14:44 落字，
两者都在出数之前**）

**触发**：还有两个数字悬在跨窗借来的分母上。
1. **布局 (+7.5%) 与 投机 (+12.9%) 共用同一个借来的中项**：sweep6 的 R2b@48t = 548.17。
2. 跨窗第三格 −2.97% 只归因了约 1/4（接受率），剩下 ~2.8% 没测。

**先看清楚哪些已经是同窗的**：布局×投机 的**下端** B1b (509.83) 和**上端**
R2c@48t (600.40) 都在 sweep7 窗口内，所以

> **乘积固定：R2c@48t / B1b = 600.40 / 509.83 = +17.77%**
> （sweep6 口径的同一乘积是 618.76/509.83 = +21.4%，差的正是那 −2.97%）

所以未知的**不是「总共多少」，只是「怎么切」**。这决定了下面判据的写法。

**延展窗**：B0p (374.74，已测) 作开锚 → 新臂 → B0''（收锚）。

**新臂 1：`R2b48`** = R2b (k=0, GPU1+2, fp8, 8192 步长) @48t 单点，自己的 seed。
> **预测（落字在前）：548 ± 3% → [532, 564]**。依据：sweep7 的 R2b_c96t 已把
> sweep6 同格复现到 −0.03%，且 k=0 臂不含接受率项，不吃 prompt 集的亏。
> **判读（三侧都写死）**：
> - 落 [532, 564] → sweep6 的 548.17 可用；布局 ≈ +7.5%、投机 ≈ +9.5%。
> - 落 **> 564** → sweep6 那格偏低 → **布局变大、投机变小**（两者互补）。
> - 落 **< 532** → sweep6 那格偏高 → 布局变小、投机变大。
> 三种结果都不推翻结论，只改切法。**这就是「乘积固定」这个预注册的价值：
> 我把一个可能翻案的风险，提前转化成了一个只影响归因切分的问题。**

**新臂 2：`R2c0`** = R2c (k=1, GPU1+2) @48t，**显式 `--seed 0`**，单点。
> 依据：sweep6 的 R2c_c48t 是 `seed=0` + `random_range_ratio=0.0`（逐条恰好 1024 token，
> 已在它的 bench Namespace 里核实），所以 seed 0 能**逐字节复现同一个 prompt 集**。
> 一次测量回答两个独立问题：
> - **同窗 A/B（主问题）**：R2c0(seed 0) vs R2c_c48t(seed 708/709)。同窗口、同配置、
>   同 boot 参数，**只差 prompt 集** → 差多少就是 prompt 集/接受率的纯效应。
> - **跨窗 A/B（副问题）**：R2c0(sweep7) vs 618.76(sweep6)。prompt 集相同，
>   **只差窗口** → 差多少就是纯窗口漂移。
> **预测**：主问题 **+3.0% ± 1.5%**（600.40 → 约 [612, 625]），方向与已测的接受率差
> （82.82% → 80.94%）一致；副问题 |差| < 1.5%。
> **证伪条件写明**：若主问题测出 ≈ 0，则接受率解释被推翻，得回头查 sweep6 那格
> 是否另有来源。写死这个方向，是为了不给自己留事后解释的余地。

**不做**：不测 k=1 并发上限（~25 vs ~47）的机制——那要拆 `cache_config_info`
的逐序列台账，是独立一轮；不追 c16/c48 之间那个 14-token 的输入差（0.014%）。

### 延展窗结果（14:43:33–14:52:50）：**中项补齐、四因子全部同窗；但我的接受率解释被证伪**

**R2b48 = 549.55**（p1 275.52 / p2 274.03），预测 [532, 564] → **落在带内**，
与 sweep6 的 548.17 差 **+0.25%**。于是四因子的中项不再需要借：

> **⚠️ 下表已作废（2026-09-11 晚，被修正案 4 的三个对照推翻）。保留原文是为了留下纠错轨迹。**
> 它用 R2c_c48t = 600.40 当终点，而 600.40 已被证明是**低 7~8% 的离群点**（不是池、不是负载，
> 见「修正案 4 结果」）。正确终点是 **R2c0 = 652.98**，同窗总增益 **+74.09%**。
> 连带作废的还有本节末的「多半是负载造成的」和「切分 (布局 +7.8% / 投机 +9.3%)」。

| 段 | 对照（~~全部同窗~~） | 增益 |
|---|---|---|
| 量化 bf16 → fp8 权重+KV | B0 375.09 → B1 488.01 | ~~+30.1%~~（此项未变）|
| 调度步长 2048 → 8192 | B1 488.01 → B1b 509.83 | ~~+4.5%~~ → 同池 **+5.66%** |
| 布局 TP2 → 2 副本（fp8, k=0） | B1b 509.83 → R2b48 549.55 | ~~+7.79%~~ → **+6.57%** |
| 投机 k=0 → k=1（同布局同 lanes） | R2b48 549.55 → R2c_c48t 600.40 | ~~+9.25%~~ → **+18.82%** |
| 合计 | 375.09 → ~~600.40~~ **652.98** | ~~+60.07%~~ → **+74.09%** |

1.301 × 1.0447 × 1.0779 × 1.0925 = 1.60055 → 375.09 × 1.60055 = **600.35**，
实测 600.40，差 **0.008%**。（**注意：本句当时写的终点是错的**——600.40 后来被判定为离群点，
正确终点 652.98、同窗总增益 **+74.09%**，见〈修正案 4 结果〉。此处仅存纠错轨迹。）

> **⚠️ 第三处同族错误（2026-09-11 晚自查发现）：上面这个「乘积闭合 0.008%」不是证据，是恒等式。**
> 四段因子是按顺序相邻两个测量节点的比值，乘积**必然**等于 end/start——telescoping，
> 无论中间值是多少都会闭合。真正有信息量的只有：① 总量 R2c0/B0 是直接测到的；
> ② 每个比值各来自一对同窗相邻臂，需要各自检查窗口有效性。
> 所以「闭合到 0.001%」这句话本身零信息，与 [[#正确性发现]] 里
> 「steps/s 排序与吞吐一致」属同一族错误（把恒等式当独立证据）。两处都已修掉。
> **教训：写下任何「A 与 B 一致」之前，先问一句「这两个量是不是由同一个量除出来的」。**

**B0pp = 374.06**；`WINDOW CHECK B0p vs B0pp: drift=0.18% -> VALID`。
延展窗有效，B0p (374.74) 可以直接当开锚用。

#### 修正案 3 的主预测落空，而且机制方向反了 —— 接受率解释撤回

| 指标 | sweep6 R2c_c48t | sweep7 R2c_c48t | sweep7 **R2c0** |
|---|---|---|---|
| prompt 集 | seed 0 | seed 708/709 | **seed 0**（同 sweep6）|
| Output tok/s | 618.76 | 600.40 | **652.98** |
| drafts | 6716 | 6778 | 6743 |
| accepted | 5547 / 5565 | 5486 | 5521 |
| 接受率 | 82.59 / 83.05% | 80.94% | 81.88% |
| duration (s) | 39.50 | 40.96 | 37.46 |
| **steps/s** | 170.0 | 165.5 | **180.0** |

- **同窗 A/B（只差 prompt 集）**：600.40 → 652.98 = **+8.76%**；预测 +3.0% ± 1.5% → **落空**。
- **跨窗 A/B（同 prompt 集）**：618.76 → 652.98 = **+5.53%**；预测 |差| < 1.5% → **落空**。

> **这两个「落空」的归因后来也被推翻了（见「修正案 4 结果」）**：两个对照都以为
> 差距出在 prompt 集。后期直接测了池效应——同 boot 背靠背换池（R2cAB）只有 **+2.46%**，
> k=0 换池（R2b48 → R2b48z）只有 **−0.06%**。也就是说池是真的只有 ~2%，
> 8.76% 里剩下的 6% 属于一个尚未解释的低估。
> 预注册的分支判据仍然有效（它确实落空了），但**落空的理由写错了**。

**更要紧的是机制反了**：接受率排序 82.82% > 81.88% > 80.94%，吞吐排序
652.98 > 618.76 > 600.40 —— **完全相反**；steps/s 排序 180.0 > 170.0 > 165.5
与吞吐顺序一致。所以三者的差异在**解码步速**，不在接受率。
**接受率解释被证伪，撤回**——预注册里写死的证伪条件起了作用，不必争论。

> **⚠️ 但「steps/s 排序与吞吐完全一致」这句当时被我当成第二个证据，其实它是恒等式。**
> `steps/s = drafts / wall`，`tok/s = 24576 / wall`——两者都只是 `1/wall` 乘以一个常数，
> 排序必然一致，零信息量。撑起这一段的**只有一个**证据：接受率排序与吞吐排序相反
> （而 `tok/step = 1 + 接受率`，所以若接受率是驱动量，吞吐应当跟着接受率走——它没跟）。
> 结论不变，但证据数从「两条」降为「一条」，已修正。
> **教训：写下任何「A 与 B 一致」之前，先问一句「这两个量是不是由同一个量除出来的」。**

**当时的头号嫌疑：host loadavg（共享机的邻居）。** 我为这个目的加的 loadavg 记录：

| 测量 | 时刻 | loadavg(1m) | steps/s |
|---|---|---|---|
| R2c_c48t | 13:19 | **9.22** | 165.5 |
| R2c0 | 14:49 | 3.31 | 180.0 |
| R2b48 | 14:47 | 2.52 | — |

**唯一一次高负载测量，正好是唯一一次低吞吐。** 但这只是 n=1 的相关，不是证明。
同时要承认反证：B0/B0p/B0pp 三个锚点跨 loadavg 3–6 只漂 0.09%/0.18%，
所以负载敏感度即便存在，也**可能是「副本 + k=1」这条路径特有的**，不是全局的。

> **⚠️ 这个嫌疑已被 R2c0b 直接证伪（2026-09-11 15:0x）。** R2c0b 窗口起始的
> loadavg 采样是 **15.10 / 13.47 / 12.87**（比肇事那次记录的 9.22 更高），
> 结果 **651.60**，与 loadavg 3.31 下的 R2c0 (652.98) 只差 0.21%。
> **loadavg 不是解释变量。** 当时的推理错在「n=1 相关 + 一个未排除的共同因子」，
> 而共同因子是有的：那一次低吞吐本身就是离群点，与负载同现纯属巧合。
> 保留本段是为了留下这个错法的轨迹——**共享机上，单点 loadavg 与单次吞吐的巧合，
> 不足以支撑归因，必须做同 boot 背靠背对照。**

#### 预注册修正案 4（R2c0b + R2cH + B0'''，**脚本落定并开跑 15:00:27，本文 15:0x 落字，两者都在出数前**）

**新臂 1：`R2c0b`** = R2c @48t，`FORCE_SEED=0`。与 R2c0 **同窗、同配置、同 boot 参数、
同 prompt 集**——输入完全一致，所以任何差就是**纯跑间方差**，这是 k=1 路径从没测过的东西。
> **判别两个活着的假设，判读写死**：
> - |R2c0b − 652.98| ≤ 2% → 跑间方差不大 → R2c_c48t 的 600.40 更可能是**负载**造成的
>   （S1），投机因子取 ~**+18.8%**，总增益 ~**+74%**。
> - > 2% → k=1 路径本身不可复现到 5% 以内（S2）→ **投机因子只能报区间，不能报一个数**，
>   四因子表要改成带不确定度的写法。

**新臂 2：`R2cH`** = R2c (k=1) @120t(60 lanes) + @144t(72 lanes)，一次 boot 跑两点。
> 与 R2b_c120t (682.96) / R2b_c144t (653.79) 的 **lanes 数完全相同，只差 k**。
> 这才是交叉点的直接测量（现在手上只有 k=0@60lanes > k=1@36lanes，比不了）。
> **预测：R2cH_c120t < R2b_c120t (682.96)**，依据是 k=1 并发上限 ~25 会在 60 lanes 下
> 压出 ~35 条 waiting。**若反了，「k=1 压并发」这个解释就要推翻。**

**顺带补**：`load_snap` 现在在每次引擎采样的同时记 loadavg（此前只有 bench 前后各一次），
让每个点都带自己的负载证据入库——因为这一轮的结论已经依赖它了。

**窗口**：B0pp (374.06) 开锚 → 新臂 → B0'''（收锚）。

### 修正案 4 结果（15:00:27–15:12:33 容器 UTC）：**S1 数值分支兑现（+74.09%），但 S1 的负载机制被证伪；R2cH 预注册命中**

| 臂 | 实测 | 对照 | 差 | 判据 |
|---|---|---|---|---|
| R2c0b（k=1 @48t, seed 0） | **651.60** | R2c0 652.98 | **−0.21%** | ≤2% → **S1 兑现** |
| R2cH @120t（60 lanes, k=1） | **666.06** | R2b_c120t 682.96 | **−2.47%** | 预测「<」→ **命中** |
| R2cH @144t（72 lanes, k=1） | **670.35** | R2b_c144t 653.79 | **+2.54%** | 未预注册，反号 |
| B0''' @48t（收锚） | **374.13** | B0pp 374.06 | +0.02% | 窗口 VALID |

**1. 判据兑现：走的是 S1 的数值分支，不是 S2。**
R2c0b = 651.60，与 R2c0 差 **0.21%**，远在 2% 判据内。按预注册：
**投机因子取 +18.82%，同窗总增益 +74.09%**，四因子表已按此改写（见上）。
同时这也是 k=1 @48t 路径**第一次**有一个跑间方差测量——两次独立 boot 给出
652.98 / 651.60，**≤0.5%**。所以 S2（「k=1 路径本身不可复现到 5% 以内」）不成立，
600.40 是一个**有具体原因的离群点**，不是路径本身的抖动。

**2. 但 S1 的机制（负载）死了——数值分支对了，故事错了。**
见上文对 loadavg 段的证伪框：R2c0b 在 loadavg **15.10** 下跑出 651.60，
比 loadavg 3.31 下的 652.98 只低 0.21%。**预注册只写死了数值判据、没写死机制判据，
所以严格说它「赢」了；但赢的理由不是它给的那个。** 这条要留在纠错轨迹里：
下次预注册要把**机制**也写成可证伪的（本轮修正案 3 做到了，修正案 4 没做到）。

**3. R2cH：预注册命中，但「k=1 全线吃亏」的解释要改口。**
预注册写的「R2cH_c120t < R2b_c120t (682.96)，依据是 k=1 并发上限 ~25 在 60 lanes 下压出
~35 条 waiting；若反了，『k=1 压并发』这个解释就要推翻」——实测 **666.06 < 682.96，命中**，
「k=1 压并发」暂时活着。
**但 @144t 反号了**：R2cH 670.35 > R2b 653.79（**+2.54%**）。
所以正确的说法是：**k=1 的代价随 lanes 增加而收窄并翻号，交叉点在 60 与 72 lanes 之间**
（60 lanes：k=0 赢 2.47%；72 lanes：k=1 赢 2.54%）——而不是「k=1 一律吃亏」。

代价看 TTFT 更清楚：R2cH @120t 中位 TTFT **23.4 s**、@144t **36.2 s**，
而 @48t 只要 2.4 s。**@120t 以上两个配置都已过载**，666/670/683 这些数字是在严重排队下
测出来的，绝对值**不应作为生产能力引用**（要报生产能力只能报 @48t 那一档）。

**4. 池效应被直接测掉——600.40 的两个候选解释至此全部出局。**
补的两个**同 boot 背靠背**对照（这是唯一能把池效应与一切别的效应分开的形态）：

| 对照 | 变的是什么 | 结果 |
|---|---|---|
| R2cAB70 (seed 702) → R2cAB00 (seed 0) | 只换池（k=1 @48t） | 645.39 → 661.29 = **+2.46%** |
| R2b48 (seed 701/702) → R2b48z (seed 0) | 只换池（k=0 @48t） | 549.55 → 549.20 = **−0.06%** |

两条结论：① **池效应是真的，但 k=1 上只有 ~2.5%、k=0 上 ~0%**——所以 600.40 的 8.76%
缺口**不是池**（池只能解释 2.5pp）；② 池效应**不是**与 k 无关的常量偏移，
至少在 k=1 上被放大（n=1，R2cABr 正在重复这一点）。
顺带给 坑 21 的可比性损失定了量：**~1.2pp**（下一段）。

> ⚠️ **2026-09-11 夜跑修正（R2cABr，见「夜跑结果」节）**：这张表的 **+2.46% 被推翻为
> 位置效应**。本轮 R2cABr 用同样的 `0/70/0` 形态重测（全 seed 0 对照），
> 只换池那一步只有 **+0.26%**。真正在做功的是「同一 boot 内第一个测量点系统性偏慢
> ~1.5%」——见下节「发现 B」。所以：
> - 「池效应 +2.46%」这个数字**不要再用**；
> - 但**「600.40 不是池」这个结论反而更稳了**（池效应比原来测到的还小）；
> - `R2b48 → R2b48z` 那行的 −0.06% 不受影响（两点都在同一 boot 的第 3、4 位，都已过斜坡）。

**5. SB：唯一一次同池测「调度步长」，并给坑 21 定价。**
SB2048 = **485.97** → SB8192 = **513.50** = **+5.66%**，两臂**同为 seed 702**
（TP2 + fp8 权重+KV，与 B1/B1b 同配置）。而记录里的 +4.47% 是
B1 (seed 702) vs B1b (seed 701) 的**跨池**比。
把同池比 +5.66% 代进链、保持 B1 节点不动：

| 段 | 记录值（跨池） | 同池修正 |
|---|---|---|
| 调度步长 2048→8192 | +4.47%（B1 vs B1b） | **+5.66%**（SB2048 vs SB8192）|
| 布局 TP2→2 副本 | +7.79% | **+6.57%** |
| 投机 k=0→k=1 | +18.82% | +18.82%（直接测） |
| **总量** | **+74.09%** | **+74.09%（不变）** |

**所以 坑 21 影响的是「调度步长 ↔ 布局」之间的切分（各 ±1.2pp），不是总量。**
总量由 R2c0/B0 直接决定，与池的分配无关。
**代价披露**：SB 这一对同时暴露了 TTFT 的代价——SB2048 中位 TTFT **957 ms**，
SB8192 **5493 ms**（**5.7×**）。+5.66% 的吞吐不是免费的。

**6. 窗口：四次锚点跨 2h41m，总跨 0.28%。**
B0 375.09 → B0p 374.74（0.09%）→ B0pp 374.06（0.18%）→ B0''' 374.13（**0.02%**），
期间 loadavg 在 3–15 之间摆动。`WINDOW CHECK B0pp vs B0ppp: drift=0.02% -> VALID`。
**锚点稳到 0.02%，说明本页所有 ±2% 以上的差都不是「机器变慢」能解释的**——
这一条同时是上面证伪「负载」的依据。

**7. 仍未解决（就按未解决记）**
- **600.40 的 6% 缺口无解释。** 已排除：池（~~实测 +2.46%~~ ⚠️ 夜跑改判为位置效应，
  真实池效应只有 ~1.3%，**排除更稳**）、负载（loadavg 15.10 下正常）、
  跑间方差（0.21%）、调度器差异（同 boot 同参数）。唯一剩下的入口是
  「那次 boot 的引擎态异常」——但 R2c_c48t 的 tok/step **1.817** 与 R2cAB 的 **1.822** 一致
  （decode 正常），异常的只有 TTFT **3539/3865 ms vs 2391 ms**，
  指向 **prefill/排队侧**，不是 decode。
- **夜跑队列（16:08 起，目标一整晚）**：`R2bZ`（k=0 阶梯 108/120/132/144t
  **同池同 boot**，夹逼 682.96）→ `R2cZ`（k=1 @48t 三次重复 + c120t/c144t，
  全 seed 0 → k=1 噪声底 + 同池交叉点）→ `FV`（四因子链五节点全 seed 0）→ `R2cABr`
  （一 boot 内 0/702/0 三点，分离池/顺序/噪声）。顺序按价值排，不按脚本顺序：
  FV 依赖 R2cZ，所以 R2cZ 必须在前；R2cABr 问题最次要，放最后。
  **队列被共享机反复打断，全部如实记录**：16:14 首跑撞进邻居的 15 秒空窗，
  副本 1 OOM 死掉（坑 25 现场，见下）；16:20 换 GPU2+3 重启，
  **新闸门在 16:21 就挡下了一次同一形态的抢先**（`GPUS_FREE_ABORTED`）。
  闸门、`wait_up` 快速失败、以及 `RPA`/`RPB` 卡对覆盖都是这一晚加的。
  **结局：四相跑完（17:58:56 `ALL_DONE`），另有一次 R2cZ 作废重跑（18:16–18:23）。**
  全程只被共享机推迟、没有一次抢到邻居的卡（`gpu_wait.txt` 记了每一次 hold 与 abort）。
  但**没被邻居打断，却被自己打断**：一个孤儿服务器霸占 8341 吞掉了 R2cZ 整相 —— 见坑 26。

### 夜跑 R2bZ 结果 + R2cZ 的预注册（先落字后见数）

队列 16:54:11 起跑（闸门在 GPU3 上空转 10×30 s 后才放行），17:01:37 R2bZ 相位结束，
8/8 artifact 齐。**一次 boot、一个池（seed 0）、一对卡**，所以这一列阶梯自身可比：

| 点 | 每副本并发 | 吞吐（双副本和） | Mean TTFT |
|---|---|---|---|
| @108t | 54 | 656.45 | 10.9 s |
| @120t | 60 | 680.83 | 13.9 s |
| **@132t** | **66** | **703.26** | 16.8 s |
| @144t | 72 | 652.41 | 19.6 s |

- **历史值同池复现到 0.3% 内**（这是第一次）：@120t 680.83 vs 记录 682.96 = **−0.31%**；
  @144t 652.41 vs 记录 653.79 = **−0.21%**。跨 boot、跨 seed、跨卡对的两组数在同一个池上
  对上了，说明历史阶梯的**高度**是可信的。
- **但峰的横坐标是错的**：k=0 的峰在 **@132t = 703.26**，比记录峰值 682.96 高 **+2.97%**。
  过去「峰值 682.96 @120t / @144t −4.3%」是**采样假象**——阶梯里没有 @132t 这一格。
  机制上不奇怪：@144t（72 并发/副本）越过 KV 池的 51 块闸门，Running 被钉住、Waiting 堆积，
  所以 132→144 是掉下来的；而 108→132 段是尾批填充变好（并发越高，batch 排空期越短）。
- ⚠️ 这些点的 TTFT 是 10.9–19.6 s，**全部深度过载**。这一列测的是**吞吐曲线的形状**，
  **不是生产可用容量**。对外报容量仍只能用 TTFT 可接受的点（@48t 及以下）。

**R2cZ 的预注册**（写于 R2cZ 尚未落盘时，17:0x；17:06 修正一次，见第 0 条）：
0. **修正：本条预注册初稿挂空了一个判据。** 初稿写的是「k=1 在 @132t 输」，我是照 R2cZ
   摘要块里 `for c in (108,120,132,144)` 的循环写的——但那是**摘要**读的点，不是**相位**跑的
   点：R2cZ 相位实际只跑 `c48t×3 / c120t / c144t`（`<script>` 第 793-794 行），
   **@108t 和 @132t 根本没有数据**（摘要块会在这两格打 `n/a`）。
   这正是「解析器/判据要对着实物核一遍」的同一课：**判据指向的点必须是相位真的会采的点**，
   否则预注册退化成一句无法证伪的话。修正时 R2cZ 仍零结果落盘（只有刚起的 server 日志），
   所以下面这几条仍是真预注册。
1. 历史交叉点在 60 与 72 并发/副本之间（k=1 @120t 输 666.06 < 682.96，@144t 赢 670.35 > 653.79）。
   现在 **k=0 在 66 并发/副本被实测成 703.26，高于它两侧的 60 和 72**——k=0 这条曲线的极大值
   就在 66 附近，而它在本次设计的可采点上（60、72）**都处在极大值下方**。
2. **预测（只落在真的会采的点上）：**
   - `R2cZ_c120t < R2bZ_c120t = 680.83`（k=1 在 60 并发/副本输）；
   - `R2cZ_c144t > R2bZ_c144t = 652.41`（k=1 在 72 并发/副本赢，复现历史 670.35 > 653.79）。
   即交叉点落在 **(60, 72] 并发/副本**。
3. **证伪条件**：`R2cZ_c120t > 680.83` → 「60 并发处 k=0 赢」作废；`R2cZ_c144t < 652.41`
   → 「72 并发处 k=1 赢」作废，则「k=1 越靠近墙越占优」一起作废，另找机制。
4. **证据缺口（现在就记下，不许事后补）**：k=0 的峰在 @132t 是本轮新发现，而 R2cZ
   **不采 @132t**。所以即使第 2 条全中，**交叉点的精确位置仍然locate不到**——
   只能说它在 (60,72] 这个未采样带里。下一轮若要定位，必须给 R2cZ 补 @132t；
   本轮不允许把 (60,72] 说成「交叉点 = 66」。
5. **解释护栏（先写死）**：k=0 与 k=1 的曲线形状不同（峰不在同一横坐标）。任何
   「X 并发时 k=A 优于 k=B」的写法都必须带上两条曲线的峰位，否则读者会当成单调结论。
6. 噪声底（`R2cZ_c48t_{a,b,c}` 背靠背三次，同 boot 同池）：**预测 spread ≤ 1%**。
   依据是 2h41m 长窗口的总漂移只有 0.28%，背靠背三次应更紧。若 >1%，则
   「0.21% 跑间方差」这个此前用过的噪声数字要作废重估。

### 坑 26：孤儿服务器霸占端口，整个相位静默测了错配置（R2cZ 作废）

**结论先行：R2cZ 整相作废，上面那三条判据（2a/2b 全中、噪声底 0.81%）全部撤回——
它们是拿 k=0 的数当 k=1 比出来的。R2bZ 保住了，但带一条注记。**

**怎么发现的。** R2cZ 落盘后两个副本极度不对称，而且是**三次重复都稳**的：

| @48t | p1 | p2 |
|---|---|---|
| a | 274.49 | 325.46 |
| b | 274.01 | 330.81 |
| c | 273.63 | 331.23 |

p1 三次只在 0.31% 内浮动，p2 在 1.75% 内——**这不是噪声，是一个稳定状态**。而同一对卡上
k=0 的 R2bZ 两副本是对称的（327/329、339/341）。顺着三条线索交叉定位：

1. p1 的 bench log **整个 `Speculative Decoding` 块缺失**（k=0 服务器没有 drafts，
   客户端拿到零增量就不打印该块）；
2. `specmetrics_R2cZ_c48t_a.txt` 里**只有 8342 一个块**；
3. `server_R2cZ_1.log` 只有 **40 行**、`grep -c drafter` = 0、连 EngineCore 行都没有。

第 3 条直接给出机制：**8341 上那个进程从来就不是 R2cZ 的进程。** 实际占着端口的是
pid 170213，`ps` 显示它 `--port 8341` 且**没有 `--speculative-config`**、`ppid=1`（已成孤儿）、
自 **16:36** 起一直活着。R2cZ 真正该用的 171659 在 boot 时抛的是：

```
(APIServer pid=171659)     listen_address, sock = setup_server(args, reuse_port=False)
(APIServer pid=171659) OSError: [Errno 98] Address already in use
```

于是链条是：**新服务器 bind 失败秒退（留 40 行日志）→ `wait_up` 打 /health → 孤儿答 200 →
相位"就绪" → 全部 p1 流量进孤儿**。而孤儿恰好在服务流量这件事，在 R2bZ 的日志里留了铁证：
16:54:34 的 `Engine 000: ... Running: 36 reqs` 那一行的 pid 前缀是 **170213**，不是 171659。

**它是怎么来的。** 这个孤儿是我自己造成的：更早那次夜跑被我的自匹配 SIGTERM 杀掉时，
它已经起的服务器被托孤给 init（`ppid=1`），逃过了收尾。**一个被杀掉的父进程会留下活着的服务器**，
而下一个相位完全看不出区别。

**为什么 R2bZ 还能用。** 逐字段比对孤儿与 R2bZ 那一臂的 `non-default args`——
`max_model_len 8192 / quantization fp8 / kv_cache_dtype fp8 / max_num_batched_tokens 8192 /
max_num_seqs 128 / 无 speculative_config`——**完全一致**。所以 R2bZ 的两半都是同配置的 k=0 服务器，
阶梯有效（@132t 峰值的结论保留）。**注记**：p1 来自一个早 18 分钟启动的进程，
卡号也未必与 p2 相同，所以 R2bZ 是"配置等价的跨进程配对"，不是"同 boot 双副本"。

**被撤回的与留下的：**

- **判据 2a/2b 撤回**：`R2cZ_c120t`(672.85) 与 `R2cZ_c144t`(661.34) 都含一个 k=0 的假副本，
  不能用来判交叉点。**k=0/k=1 的同池交叉点这一晚没有得到答案。**
- **判据 6 被证伪（这条是真的）**：p2 单独三次 = 325.46 / 330.81 / 331.23，
  mean 329.17、spread **1.75% > 1%**。所以 k=1 在 @48t 的跑间散布约 **1.8%**，
  此前引用的「0.21% 跑间方差」对 k=1 过于乐观，**作废**。
  （那个 0.81% 的表面"噪声底"是假的：一半是个稳定 k=0 服务器，把方差压小了。）
- **R2bZ 阶梯保留**，带上面那条注记。

**修法（已进脚本，两个方向同时堵）：**

1. **`port_precheck`（起之前）**：boot 前先打 `/health`，**凡是已经有人应答就拒绝启动**，
   并把现场 `ps` 写进 `port_conflict.txt`。这是主闸——端口被占时**根本不 spawn**，
   拒绝继承一个来路不明的服务器。
2. **`wait_up` 改问"我的 pid 还活着吗"（起之后）**：原来的 `pgrep -f -- "--port $1 "`
   问的是"这个端口有没有进程"，孤儿带着同样的 `--port` 完美通过——这正是坑 25b 的修法
   只修了一半的地方。现在 `boot_replica`/`boot_tp2` 把 spawn 出来的 pid 记进 `BOOT_PID[port]`，
   `wait_up` 直接 `kill -0` 那个 pid，**bind 失败的秒退会立刻被判死**。

两条都过了功能测试（本地起监听 → 拒绝并落 `port_conflict.txt`；空闲端口 → 放行）。

### 夜跑结果（16:54–18:23 UTC）：四相跑完 + 一相作废重跑

| 相位 | 结果 | 状态 |
|---|---|---|
| R2bZ（k=0 阶梯 108/120/132/144t，同池同 boot） | 656.45 / 680.83 / **703.26** / 652.41 | ✅ 有效（注记见下） |
| R2cZ 首跑（k=1 三次重复 + 120t/144t） | p1 是孤儿 k=0 服务器 | ❌ **作废（坑 26）** |
| FV（四因子链五节点，全 seed 0） | 见发现 A | ✅ 有效 |
| R2cABr（一 boot 内 0a/70/0b） | 见发现 C | ✅ 有效 |
| R2cZ 重跑（修好端口守卫后，18:16–18:23） | 见发现 D | ✅ 有效 |
| Z2（两条 warm 阶梯，位置对齐，18:38–19:07） | 见发现 F：**交叉点定位完成** | ✅ 有效 |
| Z3（交叉带细扫 + @132t 就地复测，19:21–20:09） | 见「Z3 结果」：**P1 证伪**（凹口在 63 不在 66）、P2/P3/P4 确认 | ✅ 有效 |
| Z4（@138t 断崖的预注册复测，20:11–21:01） | 见「Z4 结果」：**Q1/Q2/Q4 证伪、Q3 确认** → 断崖整段撤回；另发现 68–70 lanes/副本 上的副本劈叉 | ✅ 有效 |
| Z5（k=1 @126t 凹口的独立复测，21:04–21:26） | 见「Z5 结果」：**R1/R2/R3/R4 全确认** → 凹口独立复现，且**不是** Z4 那个劈叉、不是单副本效应；劈叉阈值（>2%）本相零命中 | ✅ 有效 |
| Z6（位置效应的定向复测，21:34–21:52） | 见「Z6 结果」：**Q1/Q2/Q3/Q4 全确认** → `发现 B` 弱形式被测实（@48t 首点 **−1.37%**，噪声底 0.03%）；⚠️ **但交错方向把「boot 首点」与「@48t 首点」绑死，形状归属未判**，交由 Z7 反向交错 | ⚠️ 1.37% 已被 Z7 作废 |
| Z7（反向交错，判别 boot 特有 vs 形状特有，22:07–22:25） | 见「Z7 结果」：**S1/S2 双双 FALSIFIED、S3/S4 确认** → **NEITHER fired**：`发现 B` 弱形式第三次落空、**整条作废记为「无位置效应」**，Z6 的 1.37% 被打成可疑孤立读数 | ✅ 有效（决定性） |

#### 发现 A：四因子链在**一个池、一个窗口**内复现到 0.25pp

| 因子 | 今晚同池同窗 | 记录值（修正案 4） | 偏差 |
|---|---|---|---|
| 起点 FV0（bf16 TP2 @48t） | 374.24 | 375.09 | −0.23% |
| 量化 bf16→fp8 权重+KV | **+29.87%** | +30.10% | −0.23pp |
| 调度步长 2048→8192 | **+5.37%** | +5.66% | −0.29pp |
| 布局 TP2→2 副本 | **+7.24%** | +6.57% | +0.67pp |
| 投机 k=0→k=1 | **+18.79%** | +18.82% | −0.03pp |
| **合计** | **+74.33%** | +74.09% | **+0.24pp** |

这是**第一次**把整条链放在同一个池、同一个窗口里跑（修正案 4 的五段是跨窗口的）。
四格全部落在 ±0.7pp 内，合计 +74.33% vs +74.09%。**+74.09% 这个对外数字成立。**
（摘要块自己打印了 `completeness: ALL FIVE PRESENT` —— 这行是 nan bug 修好之后加的，
旧版本会在缺文件时静默给出一条半截链。实弹检验通过。）

#### 发现 B：boot 之后的第一个测量点在 **c48t 上**偏慢 ~1.5%（❌ 已整条作废：Z2 证伪普遍形式，Z6+Z7 证伪唯一幸存形式）

> ❌ **最终裁决（2026-09-11 夜，Z6+Z7）：此条记为「无位置效应」。**
> 演变过程：Z2 先证伪了它的**普遍形式**（c120t 上位置 1 对位置 3 无效应），
> 幸存下来的弱形式"只有在 c48t 上"在 **Z6** 里第一次被测实到 1.37%
> （同形状重复吻合到 0.03%），**但 Z6 的交错方向把"boot 首点"与"@48t 首点"绑死**；
> **Z7 用反向交错（@120t 在 boot 第一位）只读到 0.41%**，判据要求的 ≥1% 没达到，
> **按预注册的"两者都不成立"分支，弱形式第三次落空、整条作废**，
> 连 Z6 的 1.37% 也被标为可疑的孤立读数。见「Z6 结果」「Z7 结果」。
> **下面这一段以及交接给 Z2 的那一段保留作过程记录，但结论全部不成立。**
> 唯一的残留物是一条**待预注册的新假设**（boot 首点偏低、量级 0.3–1.4%、不稳定），
> **它不是结论。**

两个**互相独立**的 boot，用同 seed、同位置对照，吻合到 0.1%：

| 位置 | R2cZ 重跑（全 seed 0） | R2cABr | 同 seed 同位置之差 |
|---|---|---|---|
| 1 | 652.42（c48t_a） | 652.87（0a, seed 0） | **0.07%** |
| 2 | 663.12（c48t_b） | 654.54（70, seed 70） | （池不同，见发现 C） |
| 3 | 662.34（c48t_c） | 662.52（0b, seed 0） | **0.03%** |

两个 boot 各自的位置 1→3 上升 **+1.52%** 和 **+1.48%**，而位置 2→3 只再动 −0.12% / +1.22%。
合起来是：**第一个点慢约 1.5%，第二个点起基本到平台。**

**为什么这条重要：** sweep7 里**每一个「boot 后第一个点」的绝对值都被这条斜坡压低了 ~1.5%**，
包括链首的 B0/FV0 和链尾的 R2c0/R2cZ_c48t_a。好在链的**两端都是位置 1**，
所以四因子链内部的比值是公平的（这也解释了为什么整条链能复现到 0.25pp）。
**但要拿链上任何单点去和「稳态产能」比，必须记住这一点。**
另外：此前引用的「跑间方差 0.21%」和本轮早期看到的「噪声底 0.81%/1.75%」，
**本质都是这条斜坡，不是随机噪声** —— 随机噪声的真实量级比那些数字小。

> ⚠️ **Z2 证伪（同晚 19:07，见下「发现 F」）：上面这条被写成了普遍规律，是过头了。**
> Z2 的 `w0→c120t` 是**同池、同形状（conc 60 / 120 题）、位置 1 对位置 3**的受控对照，
> 结果是 k=0 **−0.02%**、k=1 **+0.34%** —— **斜坡在 conc 60 上不存在。**
> 所以正确的表述是：**这个效应只在 c48t 上被观测到（2/2 个 boot），在 c120t 上没有出现
> （1/1 个 boot），机制未识别，不许当普遍规律使用。**
> 「第一个点慢 1.5%」这句话**不能**再原样引用；引用时必须连档位一起说。
> 保留下来的只有一个更弱的说法：**boot 后第一个点的绝对值存在一个档位相关的偏低，
> 量级在 c48t 上约 1.5%、在 c120t 上测不到。**

#### 发现 C：池效应是 **~1.3%**，不是 +2.46%；记录里那个数主要是位置效应

R2cABr 用 `0a / 70 / 0b`（seed 0 / seed 70 / seed 0）在**一个 boot 内**跑：

- 只换池那一步（0a→70）只有 **+0.26%**；
- 同池的 0a→0b（位置 1→3）却有 **+1.48%**。

把 R2cABr 与 R2cZ 重跑并排（同 seed 同位置才可比），位置 2 上 **seed 70 比 seed 0 慢 1.29%**。
**结论：池**不**是空变量（seed 70 稳定慢 ~1.3%），但记录里的「+2.46%」是把位置斜坡
算进池里了。** 对 600.40 的影响：**「不是池」这个排除反而更稳**（池比原测的还小）。

#### 发现 D：k=0/k=1 交叉点在**同一个池**上复现了历史形状

| 点 | 每副本并发 | k=0（R2bZ） | k=1（R2cZ 重跑） | 差 |
|---|---|---|---|---|
| @120t | 60 | 680.83 | 673.29 | **−1.11%**（k=0 赢） |
| @144t | 72 | 652.41 | 675.13 | **+3.48%**（k=1 赢） |

对照记录值（跨池）：@120t k=0 赢 2.47%、@144t k=1 赢 2.54% —— **形状同向、量级同阶**。
k=1 @48t = **652.42** vs 记录 R2c0 **652.98** = **−0.09%**，链尾也锚住了。

**仍然没有答案的**：@132t（66 并发/副本）。k=0 在那里的 703.26 是全局最高点，
但 **R2cZ 的相位设计里没有 @132t**（预注册修正时已把这个缺口先写死），
所以**交叉点的精确位置仍只能说在 (60,72] 这个未采样带里，不许报成一个点**。

#### 发现 E：R2bZ 的注记

R2bZ 的 p1 来自那个孤儿进程（配置与 R2bZ 那一臂逐字段相同，所以数值可用），
p2 来自它自己的 boot。因此这是一次**配置等价的跨进程配对**，不是「同 boot 双副本」。
阶梯形状（@132t 是峰、比 682.96 高 2.97%）保留。

#### 发现 F：Z2 —— 交叉点补上了缺失的那一格，同时**证伪了发现 B 的普遍形式**

Z2 把两条阶梯都跑成**热的**（各自 boot 后先丢一个预热点 `w0`），
然后在**完全相同的位置**上取 108/120/132/144。这是唯一一次 k=0 与 k=1 逐点可比的阶梯。

| 点 | 每副本并发 | k=0（warm） | k=1（warm） | 差 | 谁赢 |
|---|---|---|---|---|---|
| @108t | 54 | 659.11 | 646.23 | −1.95% | k=0 |
| @120t | 60 | 685.80 | 669.31 | −2.40% | k=0 |
| **@132t** | **66** | **707.50** | **654.76** | **−7.45%** | **k=0** |
| @144t | 72 | 656.32 | 670.35 | +2.14% | k=1 |

**交叉点定位完成**：k=0 在 54/60/66 三格全赢、k=1 在 72 赢 →
**交叉点在 (66, 72] 并发/副本之间，两侧都由实测点夹住**（此前只能停在"未采样带"里，
现在 @132t 这一格有了数）。这个预注册缺口到此闭合。

**三件必须一起说的事：**

1. **@132t 是 k=0 的峰（707.50），也是两臂差距最大的一格（−7.45%）。**
   所以"@132t 附近 k=0 大幅占优"是真的，但这**不能**被读成"66 并发是 k=0 的推荐工作点"——
   该点 TTFT 与 @144t 同属深度过载区（见发现 E 的告诫），**绝对值不是生产能力**。
2. **k=1 的阶梯是非单调的**：646.23 → 669.31 → **654.76** → 670.35，在 @132t 处**下凹**，
   而 k=0 恰在同一格达到最高。两个副本一致（327.95/326.81 vs 相邻格子 33x），不是单副本抖动。
   **机制未识别**，如实记为未解释。可用的猜测方向（未验证）：k=1 的每请求快照税使它的
   块墙比 k=0 更靠左，66 并发处先撞墙；但这条解释**与 @144t k=1 反而赢**相矛盾，
   所以只能当线索，不能当结论。

   > ⚠️ **已被 Z3 证伪并修正（同日下午，见下「Z3 结果」）：把下凹的位置说成 @132t 是错的。**
   > Z3 用 6 lanes/副本 的步长细扫，k=1 的**最小值落在 63 lanes/副本（@126t = 648.30）**，
   > @132t（654.47 / 复测 657.04）只是它回升路上的中段。**非单调这件事是真的且复现了**，
   > 错的是**位置**——Z2 的 12 lanes/副本 步长太粗，把这个凹口插值到了相邻的格子上。
   > 上表里 `654.76` 那个"下凹"读数是真实的，它不是最小值。
   >
   > ✅ **已获独立复测（当日 21:04–21:26，见「Z5 结果」）：Z5 用单臂 k=1、另一个 boot
   > 复现了"@126t 同时低于 @120t 与 @132t"（654.78 vs 677.19 / 656.50），且证明
   > 它**不是** Z4 那个两副本劈叉、也**不是**单副本效应。所以这条改写的方向是对的。
   > **但复现得住的内核是"从 @120t 下台阶（−3.3%）"，不是"最小值的精确位置"**——
   > 本带同一个点跨 boot 就差 0.3–0.6%，而 Z5 里 @126t 只比 @132t 低 0.26%。
   > `648.30` 是按判据保留的一次读数，**不是一个可以到处引用的常数**。
3. **发现 B 被证伪（再次，且是它自己的设计证伪的）**：`w0`（位置 1）与 `c120t`（位置 3）
   同池同形状，k=0 −0.02%、k=1 +0.34% —— **conc 60 上根本没有斜坡**。
   斜坡只在 c48t 上出现过（2/2 boot），**不是一条"boot 后第一个点"的普遍规律**。
   README 相应段落已就地改正，`0.21% 跑间方差` 那一句也随之失效（它量的是另一个档位的事）。

   > ❌ **收尾（当夜 Z6+Z7）：这里留下的那个"c48t 上的斜坡"幸存形式，后来也被证伪了。**
   > Z6 曾把它测到 1.37%，但 Z6 的设计分辨不了"boot 首点"与"@48t 首点"；
   > Z7 反向交错只读到 0.41%（判据 ≥1%）→ **`发现 B` 最终记为「无位置效应」**。
   > 详见「Z6 结果」与「Z7 结果」。

**顺带得到的**：把 warm 的 k=0 阶梯和冷的 R2bZ 阶梯逐格比，
warm 分别高 **+0.41% / +0.73% / +0.60% / +0.60%** —— **整个 boot 预热确实值约 +0.6%**，
只是这个量级远小于发现 B 当初声称的 1.5%，而且它体现在整条阶梯的平移上、不是第一个点的凹陷。

#### 预注册：Z3 —— 交叉区细扫 + @132t 就地复测（**本节写在见到任何 Z3 数字之前**）

**由头**：发现 F 留下两条无法自证的结论。一条是「k=1 在 @132t 下凹」，另一条是
「k=0 的峰在 @132t」。两条都建立在**单次读数**上，而 @132t 这一格在整轮里从来没有过第二次测量——
它是 Z2 新加的一格，没有历史值可以对照，所以它自己是峰还是尖刺，**当晚无法判定**。

**设计**（与 Z2 同构，保证可比）：两条臂各一次 boot，各自先丢一个预热点 `w0`
（conc 60 / 120 题 / seed 0），随后在**完全相同的位置**上取

```
w0(丢弃) -> @120t -> @126t -> @132t -> @132tr -> @138t -> @144t
            60/rep   63/rep   66/rep   66/rep    69/rep   72/rep
```

`@132tr` 是 @132t 的**就地复测**（紧邻其后，位置 4 与 5），同一臂、同一池、同一 boot，
唯一的差别是位置差一格。这是判定"下凹/尖刺 vs 真实结构"的对照。

**预注册判据（先写死，含证伪条件）：**

| # | 预测 | 证伪条件（触发即改结论，不辩论） |
|---|---|---|
| P1 | k=1 的 @132t 下凹**复现**：`@132tr` 落在 654.76 的 ±1.5% 内（≤ 664.6），且低于同臂 @126t 与 @138t | `@132tr` ≥ 665（回到 669 的趋势线）→ **发现 F 第 2 条撤回**，改记为「@132t 处 k=1 有一次未复现的低读数」 |
| P2 | k=0 的 @132t 峰**复现**：`@132tr` ≥ 700（在 707.50 的 −1% 内） | `@132tr` ≤ 690 → 「峰在 @132t」**降级**为「(126,138) 是一段平台、峰位未定」 |
| P3 | 交叉区是**单调过渡**：两臂差从 @120t 的 ~−2.4% 沿 126/132/138 收敛到 @144t 的 ~+2.1% | 126 或 138 上出现比 132 更大的差 → 该区间是**锯齿**而非单调过渡；这本身是发现，且说明结论不许跨格插值 |
| P4 | 同臂 @132t 与 @132tr 之差 ≤ 1%（boot 内位置漂移按 ~0.2%/格估） | 差值 > 1.5% → **该区间本身不稳定**；则所有单点结论（含 R2bZ 记录峰值 703.26）都要加误差棒，"峰在 @132t" 不许再单独引用 |

**明确不在本次范围内**（不得从 Z3 的数据里推）：
- 不重测整条阶梯，只扫 (60,72] 这一带；
- 不用 Z3 反推"推荐工作点"——该区间整体处于深度过载区（TTFT 远离生产可接受范围），
  峰位只是结构信息，不是产能；
- 不碰 600.40 的缺口归因。

**环境与可比性声明**：GPU1 + GPU3（RPA=1 / RPB=3），与 Z2 相同；邻居的 `qserver`
仍驻留 GPU0（43.8 GB 常驻、util 0%，与 sweep6/Z2 同一状态）——因此 Z3 与 Z2 **同环境可比**；
若邻座在 Z3 期间转为活跃，按窗口作废规则处理（锚点漂移 >3% 则整窗作废）。

#### Z3 结果（19:21–20:09 UTC，跑完即写；判据由 summarizer 机械打印，非事后解释）

预注册的四条判据，逐字取 `summary.txt` 的 CONFIRMED/FALSIFIED：

| 判据 | 结论 |
|---|---|
| P1 k=1 的 @132t 下凹复现 | **FALSIFIED** |
| P2 k=0 的 @132t 峰复现 | **CONFIRMED** |
| P3 交叉区只有一个符号变化 | **CONFIRMED** |
| P4 两次 @132t 读数差 ≤1% | **CONFIRMED**（k=0 +0.03% / k=1 +0.39%） |

**两条阶梯（warm，位置对齐，seed 0）**：

| 点 | lanes/副本 | k=0 | k=1 | 差 |
|---|---|---|---|---|
| @120t | 60 | 687.80 | 673.34 | −2.10% |
| @126t | 63 | 696.84 | **648.30** | **−6.97%** |
| @132t | 66 | **708.22** | 654.47 | −7.59% |
| @132tr | 66（下一位置复测） | **708.40** | 657.04 | — |
| @138t | 69 | **646.25** | 664.54 | +2.83% |
| @144t | 72 | 657.45 | 672.01 | +2.21% |

**P1 被证伪，但证伪的方式本身就是收获——错的是位置，不是现象。**

判据的第二条（`@132r` 必须低于同臂 @126t 与 @138t）不成立：k=1 的
`@126t = 648.30` 比 @132t 的 654.47/657.04 **更低**。也就是说
**k=1 的非单调性是真的、而且复现了**，只是它的凹口最小值在
**63 lanes/副本（@126t）**，不在 66。Z2 的步长是 12 lanes/副本，
120 与 144 之间只采到 @132t 一格，把这个凹口插到了相邻格子上。
**发现 F 第 2 条的"在 @132t 处下凹"已就地撤回并改写**（见上）。
`@132t` 那个 654.76 是真实读数，它只是不是最小值。

**P2 被确认，而且 k=0 的峰现在有三次独立读数**：Z2 的 707.50、
Z3 的 708.22 与 708.40 —— **三次跨 boot 读数极差 0.13%**。
66 lanes/副本 上 k=0 见顶，这条现在比 Z2 时硬得多。

**P3 被确认 → 交叉点收紧到 (66, 69] lanes/副本**（Z2 只能给到 (66, 72]）。

> ❌ **本行的 (66, 69] 已作废，回到 (66, 72]（2026-09-12 核对时发现，Z4 之后未同步）。**
> 这个收紧**完全依赖 @138t（69 路）那一格 k=1 赢**，而它的分母正是 Z4 判据 Q1 证伪后
> **整段撤回**的 k=0 读数 646.25。用 Z4 自己的 @138t 替进去，符号直接反转：
> k=0 **682.78** > k=1 665.94 → **69 路是 k=0 赢**，不是 k=1。
> 但**也不能据此把区间改收成 (70, 72]** —— Z4 已证明 68–70 这一带上 k=0 的两副本会
> 稳定劈叉、总吞吐在 646↔683 之间跳（见本节末「Z4 顺带看清的一件事」），
> 所以这一带的读数**两个方向都不许用来下结论**。
> **能站住的只有 Z2 四个实测点夹出的 (66, 72]**；`@138t` 这一格对交叉点问题**无贡献**。
> 教训与 [坑 23](同族)：撤回一个读数时，要**把所有依赖它的下游结论一起扫一遍**——
> 这里漏扫了，直到次日核对速览时才被发现。
全程只有一个符号变化，两臂各自单调地跨过零点，所以这个区间内
"哪个臂更好"是良定义的；但**仍然只能说区间，不能说点**。

**P4 被确认 → 这个带本身是稳的**（两个臂的两次 @132t 分别只差 +0.03% 与 +0.39%）。
这条很重要：它**否决了**"该带不稳定、所有单点结论都要加误差棒"的担心，
所以 R2bZ 记录里的峰值 703.26 与 Z2 的 707.50 都不需要因不稳定而打折。

---

**❌ 已撤回：k=0 在 @138t 的"断崖"（原为细扫顺带发现的事后观察）**

> 原文写的是：@138t 两副本同时掉 −8.8%（354.08/354.14 → 323.04/323.21，
> 互相吻合到 0.05%），同窗引擎采样 `Running 45 / Waiting 15–18 / KV 98.4%`
> 符合"撞 KV 块墙"签名，@144t 部分回升。**该整段现已撤回。**
>
> **撤回依据**：Z4 的预注册复测（判据写在数据之前，见下「Z4 结果」）对同一 rung
> 给出 **682.78 / 就地复测 683.44（差 0.10%）**，**不是 ≤660**，Q1 判据**证伪**。
> Z4 的 @136t（678.74）与 @140t（687.38）也没有把任何东西"夹住"，Q2 同样证伪。
> **结论：Z3 那次 646.25 是一次没有复现的孤立读数，"断崖""凹口""部分回升"
> 三个说法一并作废，KV 块墙解释也随之删除（它要解释的现象已经不存在）。**
>
> **那个孤立读数也查过是不是宿主负载造成的——不是，而且这条查得很硬**：
> 该点运行的窗口是 **19:40:47–19:42:47**，那一带 loadavg 只有 2.2–2.6；
> 当晚最接近的负载尖峰（12.62）出现在 **19:35:42**，落在**另一个点**（k=0 @126t，
> 19:35:33–19:37:16）里，而那个点读数**正常**（696.84）。
> 更强的反证在 Z4：**@132t 的窗口（20:24:35–20:26:21）压着当晚最高的尖峰 22.38
> （约 19:35 那次的 2 倍），@132t 却读出 708.58——全晚最高值。**
> 所以负载尖峰压不动这些数字，**这次孤立读数至今无解释**，按无解释记。
> （这与 600.40 的处置同构：先是 loadavg 9.22 被当成原因，随后 15.10 的窗口跑得很快。
> 本相又添第三个反例，loadavg 归因在这台共享机上可以视为**没有预测力**。）



**顺带观察（同样未预注册）**：k=0 臂两个副本的读数差 ≤0.13%（七格里最大 0.13%），
而 k=1 臂的副本间差最大到 1.37%（范围 0.05–1.37%）——k=1 的副本间离散大一个量级。
但**符号不稳**（同一 rung 上有时 p1>p2、有时 p2>p1，跨相位也翻），
所以**不是"某张卡更快"的固定偏移**，只记为"k=1 下副本间离散更大"，机制未查。

#### 预注册：Z4 —— 给 Z3 扫出来的 k=0 @138t 断崖做复测（**本节写在见到任何 Z4 数字之前**）

**由头**：Z3 的 k=0 断崖（@138t 掉 −8.8%、两副本同时掉）是**细扫顺带发现的**，
不是预注册检验过的假设，因此它在 README 里的身份是"生成假设"。
按本项目规矩，**一个事后发现要进任何结论，必须先有一次自己的预注册复测**。
本节就是那次复测的判据，写在数据之前。

**设计**（与 Z2/Z3 同构，保持可比）：两条臂各一次 boot，各自先丢预热点
`w0`（conc 60 / 120 题 / seed 0），随后在**完全相同的位置**上取

```
w0(丢弃) -> @132t -> @136t -> @138t -> @138tr -> @140t -> @144t
            66/rep   68/rep   69/rep   69/rep    70/rep   72/rep
```

`@136t` 与 `@140t` 把断崖从两侧夹住（Z3 只有 66 与 72），
`@138tr` 是断崖那一格的就地复测。

**主判据（只用 k=0 臂 —— 断崖是这个臂的事）：**

| # | 预测 | 证伪条件（触发即改结论，不辩论） |
|---|---|---|
| Q1 | 断崖复现：k=0 `@138t` ≤ 660（646.25 的 ~+2% 内），**且**低于同臂 @136t 与 @140t | `@138t` ≥ 690 → **断崖是一次性事件**，README 里那段事后观察**整段撤回** |
| Q2 | 是**窄凹口**不是宽塌陷：k=0 `@136t` 与 `@140t` **都** ≥ 685 | 任一个 ≤ 670 → 塌陷横跨 (66,72)，"断崖/凹口"这个说法**作废**，改称"该区间整体退化" |
| Q3 | 就地复测稳定：k=0 `@138tr` 与 `@138t` 差 ≤ 2% | 差 > 3% → 该格读数不可引用，任何单点数字**作废** |
| Q4 | 恢复到 @144t：k=0 `@144t` 比 `@138t` 高 ≥ 3% | 差 ≤ 1% → "部分回升"**作废**，改为"跌下去就没回来" |

**次判据（k=1 臂）：只做描述性记录，不设结论。** 理由：在一个臂上已经检验了 4 条假设，
再对第二个臂做 4 条同样的检验会把多重比较当成发现。k=1 臂的数据只用来回答
"这个断崖是 k=0 独有还是两臂都有"，且只在**差异大到无需检验**时才在 README 里提一句。

**KV 墙签名（预测性，非判据）**：Z3 观察到断崖那一格的引擎采样是
`Running 45 / Waiting 15-18 / KV 98.4%`。Z4 逐格记录 `@136t/@138t/@138tr/@140t` 的
Running/Waiting/KV%，用于看**KV 使用率是否恰好在断崖起始的那一格跨过 ~95%**。
若跨过点与断崖起点不重合，则"块墙"解释**当场作废**（不留作线索）。

**明确不在本次范围内**：
- 不重测部署相关区间（24–60 lanes/副本），**不得**用 Z4 修改任何对外数字；
- 不因本相结果改动 +74.09% 那条链；
- 该区间仍是深度过载区，峰位/崖位都**不是**推荐工作点。

**环境**：GPU1 + GPU3（RPA=1 / RPB=3），邻座 `qserver` 驻留 GPU0。窗口作废规则同前。

#### Z4 结果（20:11–21:01 UTC，跑完即写；判据由 summarizer 机械打印）

预注册的四条主判据（只用 k=0 臂），逐字取 `summary.txt`：

| 判据 | 结论 |
|---|---|
| Q1 @138t 断崖复现（≤660 且低于 @136t/@140t） | **FALSIFIED** |
| Q2 是窄凹口不是宽塌陷（@136t/@140t 都 ≥685） | **FALSIFIED** |
| Q3 就地复测稳定（≤2%） | **CONFIRMED**（682.78 → 683.44 = 0.10%） |
| Q4 恢复到 @144t（比 @138t 高 ≥3%） | **FALSIFIED**（−3.72%） |

| 点 | lanes/副本 | k=0 | k=1（**无判据，仅描述**） |
|---|---|---|---|
| @132t | 66 | **708.58** | 657.13 |
| @136t | 68 | 678.74 | 660.20 |
| @138t | 69 | 682.78 | 665.94 |
| @138tr | 69（就地复测） | **683.44** | 666.96 |
| @140t | 70 | 687.38 | 668.32 |
| @144t | 72 | 657.39 | **673.13** |

**按预注册的证伪条件逐条执行（不辩论、不重新解释）：**

1. **Q1 证伪 → 上面那段 k=0 @138t 的事后观察整段撤回**（已就地改写为撤回声明）。
   Z4 在同一 rung 读出 682.78 / 683.44，与 Z3 的 646.25 差 5.6%，且在预注册的
   "一次性事件"阈值之内（≥690 才叫干净证伪，682.78 落在 660–690 之间）——
   **但判据是合取式，`≤660` 与 `低于 @136t` 两条都不成立，所以照判据就是证伪。**
   判据是提前写死的，不因为"差一点"而放宽。
2. **Q2 证伪 → "断崖""凹口"两个词作废。** @136t 只有 678.74，没有 ≥685。
   该区间按实测改述为：**k=0 越过 66 lanes/副本 后从峰值（708.58）掉到一个
   679–687 的平台（−3.2% ~ −4.2%），再在 72 上掉第二级（657.39，−7.2%）。**
3. **Q3 确认 → 这个 rung 本身是稳的**（就地复测差 0.10%）。这条同时说明
   Z3 那次 646.25 是**孤立事件**，不是"该格很吵"——所以撤回是针对那一次的，
   不是针对整个 rung 的。
4. **Q4 证伪 → "部分回升"删除**，改为：**跌下去没有回来，@144t 比 @138t 再低 3.72%。**

**仍然站得住、而且被加强的**：**k=0 的峰在 66 lanes/副本，现在有四次跨 boot 读数 ——
707.50（Z2）/ 708.22、708.40（Z3）/ 708.58（Z4），极差 0.15%。**

**k=1 臂（描述性，不设判据）**：Z4 里 k=1 在 66→72 上是**平滑单调上升**
（657.13 → 673.13），与 Z3 里 k=1 在 63 那个凹口之后回升到 @132t 的形状一致。
Z4 **没有采 @126t**，所以它既不确认也不否定 Z3 的凹口——**不要说 Z4"证实了 k=1 没有凹口"**。

**附带查掉的一条归因（方法论，值得单记）**：Z3 那个孤立读数最顺手的解释是宿主负载，
查下来**不成立**（证据见上面的撤回声明）：尖峰不在它的窗口里，而 Z4 的 @132t
顶着全晚最高尖峰 22.38 跑出全晚最好读数。此外 loadavg 轨迹本身显示
**201 个采样里 30 个 ≥6** —— 尖峰在这台共享机上是常态，拿单次尖峰去解释单次异常
在这个环境里本来就不该被当作证据。

---

**⚠️ Z4 顺带看清的一件事（事后观察，非预注册）：这条带上的"总吞吐"有一部分是调度产物**

Z4 的 k=0 臂在 **68–70 lanes/副本** 上，两个副本**稳定地劈叉**：

| 点 | p1（GPU1） | p2（GPU3） | 差 |
|---|---|---|---|
| @132t（66） | 354.08 | 354.50 | 0.12% |
| @136t（68） | 357.27 | **321.47** | 10.5% |
| @138t（69） | 359.47 | **323.31** | 10.6% |
| @138tr（69） | 359.94 | **323.50** | 10.7% |
| @140t（70） | 361.94 | **325.44** | 10.6% |
| @144t（72） | 328.43 | 328.96 | 0.16% |

劈叉从 68 起、连续四格加上就地复测都在，不是抖动。**关键是把它和 Z3 对齐看**：

- **p2 在两次 boot 里逐位一致**：@138t 的 p2 分别是 **323.21（Z3）与 323.31（Z4）**，差 0.03%。
- **p1 是双峰的**：@138t 的 p1 在 Z3 是 **323.04**、在 Z4 是 **359.47**。
- 所以 **Z3 的 646.25 与 Z4 的 682.78 之差，整个来自 p1 在两个状态之间跳**，
  而 p2 一个字都没变。

**这条比"撤回断崖"更进一步，而且方向是收紧的**：68–70 lanes/副本 这一带的
**总吞吐不是产能的可靠度量**——同一个 rung 可以是 646 也可以是 683，
取决于 p1 落在哪个状态。所以：

1. Q1 的**撤回维持**：断崖不成立；
2. **但也不能反向说"这带其实是平的"** ——那会是在用另一个同样受调度影响的读数
   替换一个受调度影响的读数。**正确的处置是把这条带从"可引用结论"里整个拿掉**，
   两个方向都不下结论；
3. Z3 的 646.25 现在有了比"孤立读数"更好的解释：**它多半是劈叉的一种实现**
   （两副本都落在低位），而不是一次外部干扰。这仍然是解释**那个读数**，
   不是恢复"断崖"这个说法。

**对已有账本的影响：零，且已逐点核实。** 把 sweep7 里**每一个**双副本 bench log
的 p1/p2 都算了一遍（|p1−p2| / 均值），**超过 2% 的只有 Z4 的这四格**（外加
`R2cH_c120t` 的 2.3%，属边缘，非部署头寸）。**所有对外引用的部署点
（c48t / c72t / c96t / c120t 等）两副本都在 1% 内吻合**，所以 +74.09% 那条链、
619 那个数、以及 Z2/Z3 的阶梯**都不受影响**。

**这条同时是一条可复用的自检**：双副本点一旦 |p1−p2| > 2% 就该被当成可疑
（要么是坑 26 那类污染，要么是这条带的高并发劈叉）。**当初那个孤儿服务器污染，
用这一个扫描就能一眼抓到**——坑 26 当时是靠四路交叉证据拼出来的。

**该阈值在后续相位上的实际战绩**：Z5（见下）在 63 与 66 lanes/副本 六个 rung 上
**零命中**（最大 0.69%），所以**劈叉的带没有被拉宽**——它仍然只在 **68–70** 上出现过，
是这条带最靠上的一小段，不是"过载就会劈叉"。

#### 预注册：Z5 —— k=1 @126t 凹口的独立复测（**本节写在见到任何 Z5 数字之前**）

#### 预注册：Z5 —— k=1 @126t 凹口的独立复测（**本节写在见到任何 Z5 数字之前**）

**由头**：Z3 有两条结论，一条是"k=0 在 @138t 断崖"，一条是"k=1 的凹口在 63 lanes/副本"。
**第一条在 Z4 复测里被证伪并整段撤回了。第二条至今只有 Z3 那一次 boot 的读数**，
而它已经进了 README 的主叙述（发现 F 第 2 条的改写稿）。**同一个相位里一半的结论刚被
复测推翻，另一半还在没有独立复测的情况下被引用——这个不对称必须先消掉。**

而且 Z4 给出了一个**具体的新嫌疑**：68–70 lanes/副本 上 k=0 的两副本会稳定劈叉，
而**劈叉会压低"总吞吐"这个读数**。Z3 的 @126t 恰好也有劈叉迹象
（p1 321.92 / p2 326.38，差 1.4%）。**所以"凹口"完全可能只是劈叉的一个较弱的实现。**

**设计**（单臂 k=1，一次 boot，丢一个预热点，位置对齐）：

```
w0(丢弃) -> @120t -> @126t -> @126tr -> @132t -> @126tb
            60/rep   63/rep   63/rep    66/rep   63/rep
```

三个 @126t 读数（位置 3、4、6，夹着一次 @132t）用来区分"持续的凹口"与
"一次性的低读数"；@120t 与 @132t 是两侧对照。

**预注册判据（先写死，含证伪条件）：**

| # | 预测 | 证伪条件（触发即改结论，不辩论） |
|---|---|---|
| R1 | 凹口复现：`@126t` 同时低于 `@120t` 与 `@132t` | 不低于任一侧 → **凹口撤回**，发现 F 第 2 条的改写稿一并作废（改回"Z2 的 @132t 低读数无复现"） |
| R2 | 就地复测稳：`@126tr` 与 `@126t` 差 ≤1.5% | 差 >2.5% → 该 rung 读数不可用，**任何单点数字作废** |
| R3 | **不是劈叉**：`@126t` 与 `@126tr` 的 \|p1−p2\| **都** ≤2%（Z4 新诊断阈值） | 任一 >2% → **凹口与 Z4 的劈叉同源、凹口说法作废**，直到换一个不受该劈叉影响的仪器再测 |
| R4 | 是**两副本一起**低：@126t 的 p1 与 p2 **各自**都低于自己在 @120t 的值 | 只有单侧低 → 那是单副本效应（即劈叉），不是系统级凹口，**凹口作废** |

`@126tb`（位置 6 的第三次读数）只作描述性记录，不设判据。

**明确不在本次范围内**：不重新讨论 k=0（那条线已由 Z4 关闭）；不改动 +74.09% 链；
不因本相结果修改任何对外数字；不把该带说成推荐工作点（仍是深度过载区）。

**环境**：GPU1 + GPU3（RPA=1 / RPB=3），邻座 `qserver` 驻留 GPU0。

#### Z5 结果（21:04–21:26 UTC，跑完即写；判据由 summarizer 机械打印）

预注册的四条主判据（单臂 k=1），逐字取 `summary.txt`：

| 判据 | 结论 |
|---|---|
| R1 凹口复现（`@126t` 同时低于 `@120t` 与 `@132t`） | **CONFIRMED**（654.78 < 677.19 且 < 656.50；Z3 那次是 648.30） |
| R2 就地复测稳（≤1.5%） | **CONFIRMED**（654.78 → 653.52 = 0.19%） |
| R3 **不是劈叉**（两个 @126t 读数的 \|p1−p2\| 都 ≤2%） | **CONFIRMED**（0.54% / 0.32%） |
| R4 是两副本一起低 | **CONFIRMED**（p1 338.59→328.28；p2 338.60→326.50） |

| 点 | lanes/副本 | total | p1 | p2 | \|p1−p2\| |
|---|---|---|---|---|---|
| `w0`（丢弃） | 60 | 671.07 | 335.52 | 335.55 | 0.01% |
| `@120t` | 60 | **677.19** | 338.59 | 338.60 | 0.00% |
| `@126t` | 63 | **654.78** | 328.28 | 326.50 | 0.54% |
| `@126tr` | 63 | 653.52 | 327.29 | 326.23 | 0.32% |
| `@132t` | 66 | **656.50** | 329.39 | 327.11 | 0.69% |
| `@126tb` | 63 | 652.84 | 327.08 | 325.76 | 0.40% |

**四条全中，所以按预注册执行的是"保留"那一支**：凹口不是 Z4 那个劈叉现象、不是单副本效应、
就地复测稳、且两个副本一起低。**Z3 的两条结论到此都有了独立复测——一条（k=0 @138t 断崖）
被推翻，一条（k=1 @126t 凹口）被复现。** 预注册里点名的那个不对称消掉了：
`发现 F 第 2 条` 的改写稿不再建立在同一个读数上。

**复现的量级，以及必须如实标注的一条薄边**：三个 @126t 读数 654.78 / 653.52 / 652.84，
极差 0.30%；与 Z3 的 648.30 跨 boot 差 1.0%。**凹口朝 @120t 的那一侧很扎实
（−3.32%，Z3 −3.72%）；朝 @132t 的那一侧只有 −0.26%（Z3 −0.94%）。**
判据是合取式，"低于两侧"两条都成立、两次 boot 同号，**照判据就是 CONFIRMED，不放宽也不收紧**；
但要一并记下：**该带上同一个点跨 boot 本身就差 0.3–0.6%**（@120t 677.19 vs 673.34 = +0.57%；
@132t 656.50 vs 654.47 = +0.31%），**所以"@126t 是本带最小值"这一条靠的是与噪声同阶的间距。**

**因此这条带能复现得住的内核是**：**k=1 从 @120t 到 63 lanes/副本 掉一档（−3.3%），
之后 63–66 是 652.8–656.5 的平台（Z5 四点极差 0.56%），再往后才回升。**
"凹口在 63 lanes/副本"**保留**（判据兑现），**但它可复现的内核是"下台阶"，
不是"最小值的位置"**——Z3 那次 648.30 离平台 1.0%，Z5 这三次只离 0.3%。
**R1 兑现 ≠ 把 648.30 当成一个可引用的常数。**

**判别性的那条（Z5 就是为它设计的）**：R3 与 R4 双双确认 →
**凹口不是 Z4 那个两副本劈叉的弱实现**。劈叉在 Z5 六个 rung 上一次都没出现
（最大 0.69%，全部远低于 2% 阈值），而凹口照旧出现——**新诊断阈值在 Z5 全程零命中**，
台账不受本相影响（与 Z4 那次全账本扫描的结论一致）。

**顺带看掉的两条（描述性，不设判据）：**

1. **引擎准入计数看不见这个凹口。** 各 rung 采样的尾部都是
   `Running ~25 / Waiting 0–7 / GPU KV cache 95–99%`——**@120t、@126t、@132t 的准入态一样，
   吞吐却差 3.3%**。这与 Z3 那个（已撤回的）k=0 断崖的签名完全不同（那个是 `Running 45 /
   Waiting 15–18`），说明这一档差在**每步速度/调度**而不在准入；**也不是 KV 墙**
   （Z4 已把墙解释删掉，这里再次不支持它）。
   ⚠️ 这几条是含排空段的 `tail` 快照，**只作方向性参考、不当逐格签名**（Z4 的教训）。
2. **首点（`w0`）偏低 0.90%**（671.07 vs 677.19，同形状、相邻位置）。这是第三个 boot 在
   60 lanes/副本上看到首点偏低；而 `发现 B` 的幸存弱形式说的是"只有在 c48t 上出现"。
   **但位置安排与 Z2 的对照不同**（这里是位置 1 对 2，Z2 是 1 对 3），**且只有一次读数，
   不足以翻案**——只记为"值得为它单独预注册一次"的口子，**不进任何结论**。
   > 后记（Z6/Z7）：Z6 把 `发现 B` 的弱形式测实了（@48t 首点 −1.37%），但把 @120t 的
   > **第二个**点测成 −0.09%——**这条 w0 读数至今仍是 @120t 上的孤例**（Z2 的 @120t 首点是
   > +0.34%、Z5 是 −0.90%）。见「Z6 结果」的盲点段与「预注册：Z7」。

**窗口负载**：Z5 窗口 18 个 loadavg 采样、1-min 最高 **9.81**（≥6 的尖峰仍在）。
凹口在 Z3 窗口（最高 12.62）与 Z5 窗口（9.81）两种负载下都出现——又一次与
"拿单次尖峰解释单次异常"相反，与 Z4 已记的那条一致。

#### 预注册：Z6 —— 位置效应（boot 后第一个测量点）的定向复测（**本节写在见到任何 Z6 数字之前**）

**由头**：这个项目里被同一个坑咬过两次的，只有"位置"，不是"配置"。
① `发现 B` 最初声称"boot 后第一个测量点系统性慢 ~1.5%"，**被 Z2 证伪了普遍形式**
（c120t 上位置 1 对位置 3 只有 k=0 −0.02% / k=1 +0.34%），幸存的弱形式限定为
**"只有 c48t 上的第一个点"**（2/2 boot 出现）。
② 而 Z5 刚刚在 **60 lanes/副本** 上看到被丢弃的 `w0` 比同形状的 @120t 低 **0.90%**
——**方向与弱形式一致，但档位超出了它的限定**。这两条合起来说明：**幸存弱形式
既没有被确认、也没有被推翻，它只是从来没有被一次专门的实验测过。**
③ 这件事值钱的地方在于它**直接决定阶梯怎么读**：本 README 里所有阶梯的可比性
都建立在"位置对齐"上（Z2/Z3/Z5 各丢一个预热点）。**如果位置效应比我们以为的更大或更普遍，
那么"位置对齐"这个前提本身的适用范围就要写清楚。**

**设计**（单臂 k=1，**一次 boot**，两个形状交错、seed 固定为 0）：

```
n1=@48t  n2=@120t  n3=@48t  n4=@120t  n5=@48t  n6=@120t
```

**交错是设计的关键**：如果只把 @48t 连跑三次，就分不清"boot 后的第一个点"与
"某个形状的第一个点"；交错之后，`n1` 是**全 boot 的第一个点**，
而 `n2`/`n4`/`n6` 里只有 `n2` 是**@120t 的第一个点**。**seed 固定为 0**，
所以六次跑的是**同一个 prompt 池**，差异只能来自位置（这一点与 坑 21 的教训一致：
位置效应实验里绝不能同时动 seed）。

**预注册判据（先写死，含证伪条件）：**

| # | 预测 | 证伪条件（触发即改结论，不辩论） |
|---|---|---|
| Q1 | **@48t 上存在首点效应**：`n1` 比同形状的 `n3`/`n5` 低 **≥1%** | 差 <1% → **弱形式"只有 c48t 上的第一个点"就此作废**，`发现 B` 整条（含幸存弱形式）改为"无位置效应" |
| Q2 | **@120t 上没有首点效应**：`n2` 比 `n4`/`n6` 低 **<0.5%** | `n2` 也低 ≥1% → **不是 c48t 特有，是"boot 后第一个点"的普遍效应**，`发现 B` 的普遍形式**恢复**（Z2 的证伪被本相推翻） |
| Q3 | **不是全局漂移**：`n1..n6` 不呈单调上升 | 单调上升（无反转）→ 这是**运行期漂移**而非位置效应，本相**对位置效应不下结论** |
| Q4 | **噪声底小于效应**：同形状的非首点之间一致（`n3` vs `n5` ≤0.5%，`n4` vs `n6` ≤0.5%） | 任一对差 >1% → **该带噪声底 ≥ 效应量级，本相不可判**，位置效应问题维持"未决" |

**Q3 与 Q4 是防呆**：没有它们，一个纯粹的运行期漂移会被误读成位置效应
（这正是 坑 23 的同族错误——把同一个量换个说法再算一遍证据）。

**明确不在本次范围内**：不改动任何对外数字；不重新打开 k=0/k=1 交叉点或任何阶梯结论；
不因为本相结果调整 `+74.09%` 链；**不测 c48t/c120t 之外的档位**（想扩档必须先另立预注册）。

**环境**：GPU1 + GPU3（RPA=1 / RPB=3），邻座 `qserver` 驻留 GPU0（本相**不**依赖它的状态）。

#### Z6 结果（21:34–21:52 UTC，跑完即写；判据由 summarizer 机械打印）

预注册的四条判据，逐字取 `summary.txt`：

| 判据 | 结论 |
|---|---|
| Q1 @48t 首点低 ≥1% | **CONFIRMED**（`n1` 653.57 vs `n3`/`n5` 均值 662.68 = **+1.37%**） |
| Q2 @120t 首点不低 <0.5% | **CONFIRMED**（`n2` 670.31 vs `n4`/`n6` 均值 669.68 = **−0.09%**） |
| Q3 不是全局漂移（不单调上升） | **CONFIRMED**（653.6 / 670.3 / 662.6 / 670.7 / 662.8 / 668.7） |
| Q4 噪声底低于效应（`n3`-vs-`n5`、`n4`-vs-`n6` 都 ≤0.5%） | **CONFIRMED**（**0.03%** / 0.30%） |

| 点 | 形状 | 位置 | total | p1 | p2 | \|p1−p2\| |
|---|---|---|---|---|---|---|
| `n1` | @48t | 1 | **653.57** | 325.67 | 327.90 | 0.68% |
| `n2` | @120t | 2 | 670.31 | 335.36 | 334.95 | 0.12% |
| `n3` | @48t | 3 | 662.59 | 331.47 | 331.12 | 0.11% |
| `n4` | @120t | 4 | 670.69 | 334.75 | 335.94 | 0.35% |
| `n5` | @48t | 5 | 662.77 | 331.45 | 331.32 | 0.04% |
| `n6` | @120t | 6 | 668.66 | 335.24 | 333.42 | 0.54% |

**四条全中 → `发现 B` 的幸存弱形式第一次被"专门测"过，而且站住了（1.37%）。**
这一相的窗口还异常干净：同形状的非首点之间 **@48t 差 0.03%**、@120t 差 0.30%，
比跨 boot 的 0.3–0.6% 小一个量级 —— **1.37% 的效应是噪声底的几十倍，
不存在"分不出来"的问题。**

**Q3 的形态值得单独看**：`653.6 → 670.3 → 662.6 → 670.7 → 662.8 → 668.7`
是**按形状交替**的，不是按位置单调。**这直接排除"服务器越跑越快/越跑越慢"的漂移解释**——
低点只有一个（`n1`），其余五个在各自形状内互相吻合。

**⚠️ 必须披露的设计盲点 —— Z6 只回答了一半，另一半被它自己的设计挡住了**：
**`n1` 同时是"boot 后的第一个点"和"@48t 的第一个点"，因为交错是从 @48t 开始的，
这两件事在设计里被绑死了。** 所以 Z6 证明的是
「**boot 后的第一个测量点（这次落在 @48t）低 1.37%**」，
**而不是**「这个效应是 @48t 特有的」——后者必须把 @120t 放在第一位才能分辨。
**把跨 boot 证据摆在一起，@120t 首点那一侧本来就是自相矛盾的**：
Z2 的 `w0`（@120t，位置 1）**没有**效应（k=1 +0.34%），Z5 的 `w0`（@120t，位置 1）**低 0.90%**，
Z6 的 @120t 点（位置 2、**非**首点）低 0.09%。
**所以"boot 特有 vs 形状特有"这一问，Z6 只是把它逼到了唯一一个还没做过的方位上，
并没有回答它。** 在此之前，`发现 B` 的弱形式只能按**"boot 后第一个测量点"**记，
**不能**按"c48t 特有"记——见下「预注册：Z7」。

**对已有账本的影响：零。** 所有阶梯本来就丢一个预热点；这一相**本想**给那个做法
补上一个用专门实验测出来的量级（~1.4%）。
p1/p2 全部 ≤0.68%，**2% 诊断阈值再次零命中**（Z5 之后第二相连续零命中）。

> ⚠️ **Z7（反向交错，22:07–22:25，15 分钟后）没能复现这 1.37%，本节的"1.37%"已被作废。**
> Z7 把 @120t 放在 boot 第一位，`m1` 只读到 **0.41%**（判据要求 ≥1%），@48t 放到位置 2 后
> **完全没有效应（+0.04%）**。按预注册的"两者都不成立"分支，**`发现 B` 的弱形式整条作废，
> `发现 B` 记为「无位置效应」，而本节的 1.37% 被标为可疑的孤立读数**。
> **本节余下的内容（Q3 排除漂移、Q4 噪声底 0.03%、交错设计的方法论）仍然成立并保留**，
> 但**结论那一句不成立**——见「Z7 结果」。

#### 预注册：Z7 —— 反向交错，分辨"boot 特有"与"形状特有"（**本节写在见到任何 Z7 数字之前**）

**由头**：Z6 把 `发现 B` 的幸存弱形式测实了（1.37%），但**它的交错方向让
"boot 后的第一个点"与"@48t 的第一个点"成为同一个点**，两件事无法分辨。
这一问不是修辞：**如果是 boot 特有，那么"每个相位丢一个预热点"就够；
如果是形状特有，那么在任何改变形状的阶梯里，每一格都是"该形状的第一个点"
——那个做法就要重新审视。** Z2 与 Z5 在 @120t 首点上还互相矛盾（±0.34% / −0.90%），
所以必须用**唯一没试过的方位**去做一次判别。

**设计**（与 Z6 **完全对称，只交换两个形状的位置**；单臂 k=1、一次 boot、seed 固定 0）：

```
m1=@120t  m2=@48t  m3=@120t  m4=@48t  m5=@120t  m6=@48t
```

Z6 是 `48,120,48,120,48,120`，Z7 是 `120,48,120,48,120,48`。
**两相合起来是一个 2×2：形状 × 是否首点，四个格子都至少有一个读数。**

**预注册判据（二选一判别式，先写死）：**

| # | 预测 | 证伪条件（触发即改结论，不辩论） |
|---|---|---|
| S1 | **boot 特有**：`m1`（@120t，boot 第一个点）比 `m3`/`m5` 低 **≥1%** | `m1` <1% → **不是 boot 特有**（转 S2 判定） |
| S2 | **形状特有**：`m2`（@48t，位置 2）比 `m4`/`m6` 低 **≥1%**，**且** `m1` 低 <0.5% | `m2` <1% → **形状特有也不成立** |
| S3 | 不是漂移：`m1..m6` 不单调上升 | 单调上升 → 纯漂移，本相**不判** |
| S4 | 噪声底低于效应：`m3`-vs-`m5`、`m4`-vs-`m6` 都 ≤0.5% | 任一 >1% → 本相**不可判**，维持未决 |

**S1 与 S2 由构造互斥**（S2 要求 `m1` 不低）。**两者都不成立**（`m1` 与 `m2` 都 <1%）
→ 效应在这个窗口里根本没复现 → **`发现 B` 的弱形式第三次落空，整条作废**，
`发现 B` 记为"无位置效应"，并**回头质疑 Z6 那 1.37% 是不是一次孤立读数**。

**明确不在本次范围内**：不改动任何对外数字；不测 c48t/c120t 之外的档位；
不重开任何阶梯结论；**本相只判"首点效应归谁"这一个问题**。

**环境**：GPU1 + GPU3（RPA=1 / RPB=3），与 Z6 同。Z6 刚在同一个宿主上跑完，
所以两相之间的窗口差异可以忽略。

#### Z7 结果（22:07–22:25 UTC，跑完即写；判据由 summarizer 机械打印）

预注册的四条判据，逐字取 `summary.txt`：

| 判据 | 结论 |
|---|---|
| S1 **boot 特有**（`m1` 比 `m3`/`m5` 低 ≥1%） | **FALSIFIED**（`m1` 668.36 vs 671.11 = **+0.41%**） |
| S2 **形状特有**（`m2` 低 ≥1% **且** `m1` 低 <0.5%） | **FALSIFIED**（`m2` 662.03 vs 661.75 = **−0.04%**，即 0.04% **偏高**） |
| S3 不是全局漂移（不单调上升） | **CONFIRMED**（668.4 / 662.0 / 671.2 / 661.0 / 671.0 / 662.5） |
| S4 噪声底低于效应（都 ≤0.5%） | **CONFIRMED**（**0.03%** / 0.22%） |

| 点 | 形状 | 位置 | total | p1 | p2 | \|p1−p2\| |
|---|---|---|---|---|---|---|
| `m1` | @120t | 1 | 668.36 | 334.77 | 333.59 | 0.35% |
| `m2` | @48t | 2 | 662.03 | 330.77 | 331.26 | 0.15% |
| `m3` | @120t | 3 | 671.22 | 336.46 | 334.76 | 0.51% |
| `m4` | @48t | 4 | 661.03 | 330.21 | 330.82 | 0.18% |
| `m5` | @120t | 5 | 670.99 | 336.12 | 334.87 | 0.37% |
| `m6` | @48t | 6 | 662.48 | 331.04 | 331.44 | 0.12% |

> **DISCRIMINATION（逐字）**：`-> NEITHER fired: the effect did not reproduce in this window.
> 发现 B's weak form is VOID (third strike), and Z6's 1.37% is itself suspect as an isolated reading.`

**按预注册的证伪条件执行（不辩论、不重新解释）：S1 与 S2 都没兑现 → 走
"两者都不成立"那一支。** 也就是说：

1. **`发现 B` 的弱形式（"只有 c48t 上的第一个点慢"）第三次落空，整条作废**，
   `发现 B` 记为**「无位置效应」**——这是本题的最终裁决。
2. **Z6 的 1.37%（一小时前刚写进上一节）被标为可疑的孤立读数**——它自己的内部重复
   吻合到 0.03%，**但那不构成复现**；复现要的是**另一次 boot**，而 Z7 那次只读到 0.41%。
   **这是今晚第二次推翻自己一小时前的写法（第一次是 Z4 推翻 Z3 的断崖）。**
3. **判据不放宽**：`m1` 只读到 0.41%，判据写的是 ≥1%，**照判据就是证伪**。
   "方向看着对"不是把 1% 改成 0.5% 的理由——那正是预注册要防的事。

**⚠️ 但不许因此漏记一个描述性事实（预注册外，不构成结论、不许进结论、也不许拿它救 Z6）**：
把两相的**同形状非首点**拼起来做参照，"boot 第一个点"这个位置**在两个方位上都是名义偏低的**，
只是量级对不上：

| 格子 | 读数 | 同形状非首点均值（两相合并） | 差 |
|---|---|---|---|
| @48t 的 boot 首点（Z6 `n1`） | 653.57 | 662.18（Z6 `n3`/`n5` + Z7 `m2`/`m4`/`m6`，5 个） | **−1.30%** |
| @120t 的 boot 首点（Z7 `m1`） | 668.36 | 670.37（Z6 `n2`/`n4`/`n6` + Z7 `m3`/`m5`，5 个） | **−0.30%** |

**符号一致、量级差 4.3 倍。** 这只能记为**待预注册的新假设**：
"boot 第一个点偏低，量级 0.3–1.4% 且不稳定"——**它现在不是结论，也不是"部分成立"，
就是一条没通过的假设**。

**顺带得到两条硬的（这两条是数据本身的性质，不依赖判据）**：
① **两相的稳态读数是逐位级可复现的**——@48t 的五个非首点落在 661.03–662.77（极差 0.26%），
@120t 的五个落在 668.66–671.22（极差 0.38%），**两相之间只差 0.14% / 0.05%**。
② **因此本相不存在"分不出来"的问题**：S4 的 0.03% / 0.22% 说明这台机器在两相之间极稳，
**0.41% 是个真读数，只是没到 1%**——所以证伪是"效应不够大"，不是"噪声太大"。

**对账本的影响：零，而且预处理做法维持不变。** 丢一个预热点只花一格，
**现在它不再由"测得的位置效应"支撑，而是纯保险**；README 里所有阶梯的读数一个字都不用改
（它们全是丢掉预热之后的稳态点）。p1/p2 全部 ≤0.51%，**2% 诊断阈值连续第三相零命中**。

#### 证据完整性说明（必须披露）

R2cZ 重跑**覆盖**了受污染那次的 `server_R2cZ_1.log`（40 行）与全部 `bench_R2cZ_*` 日志。
受污染那次的数字只存在于本节与上面坑 26 的表格里，**原始文件已不可得**。
幸存的原始铁证是 `server_R2bZ_1.log`（含 171659 的 `Errno 98` traceback，
以及 170213 在 16:54:34 服务流量的 pid 前缀）。**下次遇到同类污染，先备份再重跑。**

**2026-09-12 追加披露（地址脱敏，事后改的）**：本目录 12 个 `server_*.log` 里的
`mq_connect_ip=10.82.0.xx (local)` 已替换为 `mq_connect_ip=<host-ip>`。
改的是 vLLM 启动时打印的**宿主私网地址**，与任何实验读数无关，
被改行的其余内容逐字未动。**这类改动必须披露**：
它意味着本目录的日志文件**不是原始字节**，凡是引用这些日志的结论
都应以"路径与地址已脱敏、测量内容未动"为前提来读。

---

## sweep8 预注册（k=2 高并发臂）

> **本节写在跑之前。** 下面的数字栏全部是空格，跑完才回填。
> 落字时间见本节所在 commit 的提交时间（先于任何 sweep8 读数）。

### 为什么跑

sweep7 把 **k=0 对 k=1** 的交叉点夹到了 **每副本 (66, 72] 路**，
但 **k=2 在整个高并发段是空白**。全仓库 k=2 只有两个点：

| 已有的 k=2 读数 | 位置 | 结果 |
|---|---|---|
| `R2a`（sweep6） | 两副本，每副本 24 路 | k=2 **578.49** vs k=1 **652.98**（输 11.4%） |
| 单卡 @16 | 单卡，16 路 | k=2 **301.4** vs k=1 **269.7**（赢 11.8%） |

中间从"每副本 24 路"往上一路到 72 路，**k=2 一次都没测过**。

**这与速览 §0.1 的口播稿有直接冲突。** 口播稿目前写的是
"我预注册了『投机深度 2 会在高并发反超深度 1』，跑到 132 条并发那一格被自己的数据推翻"。
逐字核对本仓库后确认：**这句话没有对应实验**——`@132t` 那一格是 **k=0 与 k=1** 的
交叉带扫描（Z2/Z3/Z4/Z5），从头到尾没有 k=2 进过场；
全仓库唯一的深度相关预注册（sweep6 §R2c-16）写的是**反方向**
（"低负载 k=2、高负载退 k=1"），而且它也**没有跑出结果**。
所以那句话现在的状态不是"被推翻"，是**从未被测**。

本相位的唯一目的：把这段空白补上，让那句话要么被实测钉死，要么被实测翻案。

### 臂（两臂只差一个数）

| 臂 | 配置 |
|---|---|
| `R2cZ8` | 2× 副本，fp8 权重 + fp8 KV，`--max-num-batched-tokens 8192`，`num_speculative_tokens=1` |
| `R2c2Z8` | 同上，`num_speculative_tokens=2` |

**卡对：`RPA=2 RPB=3`（GPU2+3）。**
本相位内**所有对照都在同一对卡、同一次 boot 上完成，卡对效应自动抵消**。
与 sweep7 的 Z2 k=1 阶梯（跑在 GPU1+2）做跨卡对核对时，
按 B1c 的定价带 **≤2.2% 保留**。**本相位的结论不依赖跨卡对比较。**

### 点（两臂完全相同）

`FORCE_SEED=0` 单池，先丢一个预热点，再逐点对齐——与 Z2 同法。

| 点 | 每副本并发 | 合计 | 想问什么 |
|---|---|---|---|
| `w0` | 60 | 120 | 预热丢弃点（与 `@120t` 同形状同池） |
| `@24t` | 12 | 24 | 墙内。k=2 的墙 ~17，12 有 5 路余量 |
| `@32t` | 16 | 32 | 墙内贴边。16 距 k=2 的墙只剩 1 路 |
| `@48t` | 24 | 48 | 骑在 k=1 的墙上；k=2 已在墙外 |
| `@108t` | 54 | 108 | 高并发段，与 Z2 同格 |
| `@120t` | 60 | 120 | 同上 |
| `@132t` | 66 | 132 | 同上（k=0 峰位所在格） |
| `@144t` | 72 | 144 | 同上 |

### 预注册预测（跑之前写死，三条）

1. **墙内两格 k=2 赢。** `@24t` 与 `@32t` 上 k=2 > k=1，
   方向复现单卡 @16 的旧读数（+11.8%）。幅度不预测。
2. **`@48t` 及以上全部 k=2 输，且差距收敛成平台，不随并发继续恶化。**
   依据：两臂各自被自己的准入墙钉死——k=1 钉在 **每副本 24 路**，
   k=2 钉在 **每副本 ~17 路**（sweep6 R2a 实测 `Running 14-17 + Waiting 7-8`）。
   过了墙以后加的请求全进 `Waiting`，**不再改变稳态产能**，所以差距应当是个常数，
   而不是单调扩大的裂口。
3. **k=1 的 `@108t/@120t/@132t/@144t` 四格近乎持平（极差 <3%）。**
   依据：sweep7 Z2 实测这四格的引擎态**全部**是
   `Running: 24 reqs, Waiting: 36 reqs`——**同一条墙上的同一个操作点，只是排队深度不同**。
   本相位在另一对卡上复现这个形状，就是对"四格是一条墙的四个读数"这个判断的独立检验。
   （已有的 Z2 读数：646.23 / 669.31 / 654.76 / 670.35，极差 3.3%——**刚过 3%**，
   所以本条有真实被推翻的可能。）

### 判据（合取；两条都成立才算"证伪『深度 2 在高并发反超深度 1』"）

- **(a)** `@24t` **且** `@32t`：`R2c2Z8` > `R2cZ8`（同点、同池、同窗、同一次 boot）；
- **(b)** `@132t`：`R2c2Z8` < `R2cZ8`，**且差距 ≥ 10%**。

### 证伪条件（写死，不许事后放宽）

**`@48t` 及以上任一格出现 `k=2 ≥ k=1` → 本预测被推翻。**
届时必须重写的是"k 是容量旋钮"这条律，
**而不是调判据**：k=2 的池比 k=1 小（81,920 vs 96,665，每请求多交 1 块快照税），
如果更小的池在高并发下反而更快，那说明瓶颈不在准入路数上，机制层要重找。

**对称地，若 (a) 也不成立**（墙内 k=2 也没赢），那推翻的是
"深度在远离容量墙时收益更大"这条更早的律——它现在只由单卡 @16 **一个点**支撑。

### 记录要求（照 §D 的指标清单，一点不省）

每点 3 次引擎态采样（`Running` / `Waiting` / KV usage）、启动即采参数台账、
`spec_metrics` 采接受率与 drafts/accepted（**两臂都采**，k=1 也采，用于同窗对照）、
每点 seed 记入 `seeds_used.txt`、卡对与 loadavg 快照。
`@132t` 额外采一次 `spec_metrics`。

### 成本

GPU2+3，**2 次 boot**，2 × 8 = **16 个 bench 点**。
所有 helper 复用 `mtp_bench7.sh`。`mtp_bench8.sh` 的构造是机械的、可逐条复核：
`head -n -2 mtp_bench7.sh`（去掉原 tail 的 `gpu_snap`/`ALL_DONE`），
再 `sed` 三处（`OUT` → `mtp8`、`PHASES` 默认值 → `"Z8"`、启动 banner 文案），
最后把 **Z8 相位块**追加进去。**改的只有这三处 sed 加一个新增相位块，
sweep7 的任何相位代码一个字都没动**（`bash -n` 通过后核对过行号）。
启动命令：`RPA=2 RPB=3 bash mtp_bench8.sh`。

### sweep8 结果（回填，2026-09-12 04:48:46–05:33:28 UTC）

`ALL_DONE`，16/16 个 bench 点齐全。跑在 **GPU2+3**，两臂各一次 boot，
`R2cZ8` 04:59:26 上线 / `R2c2Z8` 05:21:24 上线，两次 boot 都过了 `FREE_HOLD=10` 的门。

#### 两条阶梯（吞吐 = 两副本 p1+p2 之和，tok/s）

| 每副本并发 | k=1 吞吐 | k=1 引擎态 | k=2 吞吐 | k=2 引擎态 |
|---|---|---|---|---|
| 12（`@24t`） | 473.15 | Running 12 Waiting 0，KV 49.1% | **493.56** | Running 12 Waiting 0，KV 69.0% |
| 16（`@32t`） | 548.41 | Running 16 Waiting 0，KV 65.5% | **573.67** | Running 16 Waiting 0，KV 92.0% |
| 24（`@48t`） | **656.47** | Running 22 Waiting 2，KV 90.1% | 590.56 | Running **17** Waiting 7，KV 97.7% |
| 54（`@108t`） | **648.99** | Running 24 Waiting 30，KV 98.2% | 604.20 | Running **17** Waiting 37，KV 97.7% |
| 60（`@120t`） | **668.13** | Running 24 Waiting 36，KV 98.2% | 605.61 | Running **17** Waiting 42，KV 97.7% |
| 66（`@132t`） | **649.98** | Running 24 Waiting 42，KV 98.2% | 612.82 | Running **17** Waiting 49，KV 97.7% |
| 72（`@144t`） | **670.04** | Running 22 Waiting 50，KV 90.1% | 608.46 | Running **17** Waiting 55，KV 97.7% |
| （丢弃点 `w0`） | 668.54 | Running 24 Waiting 36，KV 98.2% | 593.12 | Running 17 Waiting 43，KV 97.7% |

**逐点差（k=2 相对 k=1）**：`@24t` **+4.31%** / `@32t` **+4.61%** /
`@48t` **−10.04%** / `@108t` −6.90% / `@120t` −9.36% / `@132t` **−5.72%** / `@144t` −9.19%。

#### 判据兑付（按预注册的字面，不放宽）

| 条款 | 预注册要求 | 实测 | 判定 |
|---|---|---|---|
| (a) | 墙内两格 k=2 都赢 | +4.31% / +4.61% | **HOLDS** |
| (b) | `@132t` k=2 输 **且差距 ≥10%** | −5.72% | **FAILS**（方向对，幅度不够） |
| 合取 | (a) AND (b) | — | **NOT MET** |
| 证伪器 | `@48t` 及以上任一格 k=2 ≥ k=1 | 无 | **未触发** |
| (3) | k=1 四高格极差 <3% | **3.24%** | **FAILS**（窄负） |

**必须照实说的三件事：**

1. **合取判据没兑现。** 方向全对（k=2 在 ≥24 路/副本的每一格都输），
   但我给 `@132t` 写死的 −10% 幅度门槛只到了 −5.72%，
   所以按预注册的字面，**我不能宣布"那句口播稿被证伪"**。
   **这就是这次该讲的"翻车"**——不是数据翻了，是**我自己的判据没兑现，
   而且我不改它**。方向性结论照记，判据兑付照记失败，两行并存。
2. **预注册第 3 条窄负。** 3.24% vs <3% 的门槛。旁边 Z2 自己那四个高格是
   3.74%，**两个窗口都落在 3.2–3.7%**——3% 这个数是看数之前拍的，
   拍紧了。但"四格是同一条墙的四个读数"这条**不靠极差支撑**，靠引擎态：
   `Running` 全程钉在 **24**，`Waiting` 单调 30→36→42→50。
   极差是那条判据，引擎态是机制——**机制赢了，判据输了，分开记**。
3. **证伪器没触发**，所以"k=2 在高并发会反超"这个方向**是真被实测否掉的**，
   不是"从未被测"了。这一格从空白变成了有读数。

#### 三条结论

**（一）k=2 的墙是 17，k=1 的墙是 24，而且这两条墙都是 KV 池本身。**
k=2 的 `Running` 在 **六个不同的排队深度上全部等于 17**（12→72 路/副本的六格），
`Waiting` 从 7 一路涨到 55 而 `Running` 一动不动——这是"到墙了"最干净的签名。
k=1 对应的是 **24**。两条墙的 KV 占用分别停在 **97.7% / 98.2%**，
所以**瓶颈就是 KV 池被占满**，没有别的解释空间。
池子本身：**k=2 = 81,009 tokens（9.89x）**，**k=1 = 96,665 tokens（11.80x）**——
差的就是每请求 3 块 vs 2 块的快照税。

**（二）交叉真实存在，而且位置和机制都对得上。**
12 路和 16 路/副本时两臂都**没到墙**（`Waiting` 都是 0），k=2 赢 **+4.31% / +4.61%**；
一旦骑上 k=2 的墙（24 路/副本，`Waiting` 7），k=2 立刻转输 **−10.04%**。
**交叉点夹在每副本 [16, 24) 路之间**，而 k=2 的墙就在 17——
即**"k=2 只在它自己墙以下赢"**。这条比 sweep6 的单卡 @16 **一个点**结实得多：
现在是 **4 个点、2 个区间、两个方向都有读数**。

**（三）接受率不解释这次交叉（第二次证伪同一个解释）。**
`@132t` 上 k=1 接受率 **81.28%**（83,386/102,586），
k=2 **70.06%**（108,681/155,130）——k=1 接受率更高**且**吞吐更高；
而在墙内 k=2 接受率同样更低却**吞吐更高**。
**接受率在两段上排序都与吞吐相反**，与 sweep7 的结论一致：差异在解码步速与容量，不在命中率。

#### 这次唯一真正"没被想到"的口子

`@144t` 上 **k=1 的 `Running` 掉到了 22**（其余五格全是 24），KV 也从 98.2% 掉到 90.1%，
但吞吐反而是它自己四格里的最高（670.04）。
**"墙"在高并发末端会自己塌一格**——本轮只有一个读数，**记下来，不下结论**。
（这与 `w0` 无关，`w0` 的 24/36 与 `@120t` 的 24/36 逐位相同。）

#### 对本相位结论的限定

- 每点 **n=1**，单池 `seed 0`，无重复。逐点差异 4%–10%，远高于窗口内的 0.2%–0.5% 噪声，
  所以**方向可信**；但**具体百分比不可当精确值引用**。
- 卡对跨窗口核对：本窗 k=1 `@132t` = **649.98**，sweep7 Z2（GPU1+2）同格 = **654.76**，
  **−0.73%**，落在 B1c 的 ≤2.2% 卡对带内。**这一条是记录，不是断言。**
- **证据完整性**：`sweep8/` 93 个文件，脱敏按上面的清单逐条过——
  路径 `<out-dir-8>`/`<nvme-root>`/`<model-dir>`，**地址扫描 0 命中**
  （这次是扫了地址的；sweep7 那次没扫，见开头的脱敏清单）。
  每点 3 次引擎态采样齐全，16/16 点无缺。

---

## sweep9 预注册（两个源码口子判死 + 三个扫窗口项 + 一个新旋钮）

> **本节写在跑之前。** 下面「预测」栏是先落字的，跑完只回填不回改。
> 两个源码口子的判决写在前面，因为它们是**不用 GPU 就做完的实验**。

### 一、口子 1「GDN 状态是 fp32」→ **判死，而且死两次**

这条口子是我自己写在 §2 的，原文是：文档里「占池 58% 的那部分状态**压不动**」
**从未被验证、是假设**，若 vLLM 以 fp32 存 GDN 状态，压到 bf16 即 **容量 ×1.41**。
逐行读过源码后：

1. `model_executor/layers/mamba/mamba_utils.py:53` —
   `linear_attention_state_dtype()` 只做一件事：
   `state_dtype = get_kv_cache_torch_dtype(mamba_cache_dtype, model_dtype)`。
2. `config/cache.py:129` — `mamba_cache_dtype: MambaDType = "auto"`，
   而 `utils/torch_utils.py:409` 的 `"auto"` 分支落到 **`model_dtype`**，
   本模型是 bf16。**所以 GDN 状态默认就是 bf16，从来不是 fp32**——
   假设的**前提直接不成立**。
3. 再往下压一档也没有路：`config/cache.py:38`
   `MambaDType = Literal["auto", "float32", "float16", "bfloat16"]`——
   **fp8 不在允许值里**。bf16 ↔ fp16 同字节数，零容量收益。

**判决：容量轴这条假设封死。** 这也**反向加固**了 [容量墙机制](#容量墙机制源码级2026-09-11-补) 的结论——
池里那 58% 既降不了（已是 bf16）也压不动（无 fp8），容量只能从**每请求块数**（k）那边要。
顺带解释了一个老观测：`--kv-cache-dtype fp8` 为什么只 +37.5% 而不是 +100%——
**它压根不碰 GDN 状态**，两个 dtype 是各自独立的旋钮。

### 二、口子 6「AWQ 也许能绕开 Marlin」→ **判死**

1. `model_executor/layers/quantization/auto_awq.py:309` —
   `use_marlin = (not VLLM_BATCH_INVARIANT and is_cuda() and check_marlin_supported(quant_type, group_size, zero_point))`
2. `.../utils/marlin_utils.py:65` — `if device_capability < 75: return []`，
   L20 是 **SM89 → 89 ≥ 75**；
3. group_size **128** 在 `MARLIN_SUPPORTED_GROUP_SIZES = [-1, 32, 64, 128]` 里。

三条全中。**判决：AWQ 会被自动升级成 `awq_marlin`，和 GPTQ 走同一族 kernel。**

**这条判死反而让结论更硬**：W4 那个 **+57% 的 TPOT 不是「GPTQ 的问题」，
是「Marlin 这一族 kernel 在 sm89 低 batch decode 上的 dequant 开销」**。
AWQ **不需要再排实验**——原判据（TPOT ≤50ms 且 @48 > 420）作废，
因为它测不出新东西。

### 三、顺带挖出来的一个新旋钮（本相位要探的）

`config/cache.py:125` `mamba_block_size: int | None = None`，
CLI 入口 `engine/arg_utils.py:1238` `--mamba-block-size`——**我们从来没动过它**。
它定的是 **mamba page 的 token 粒度**。而 §5.2 说块粒度被 mamba page 抬高、
§5.3 说每请求的税是**按块**交的，那么**改块粒度就是改「每请求 2 块快照税」的度量单位**。

两条约束都已确认可满足：必须是 8 的倍数（对齐 causal_conv1d），
且 `config/vllm.py:2551` 要求 prefix caching 打开——**默认就是 True**（`cache.py:96`）。

### 四、相位表

`R2c 终态配置` = 2× 副本、fp8 权重 + fp8 KV + `--max-num-batched-tokens 8192` +
`num_speculative_tokens=1`；`FORCE_SEED=0` 单池，每臂丢一个预热点。

| 相 | 臂 | 卡对 | 点 | 回答什么 |
|---|---|---|---|---|
| `PAIR` | R2c 终态配置 | **(1,2)** 与 **(2,3)** 各一次 | @48t、@120t | **卡对效应值几个百分点** |
| `EG` | R2c + `--enforce-eager` | (2,3) | @48t、@120t | CUDA graph 池值几路 |
| `GMU` | R2c + `--gpu-memory-utilization 0.95` | (2,3) | @48t、@120t | 每副本多几路 |
| `MB` | R2c + `--mamba-block-size 32` | (2,3) | @48t | 块粒度是不是新杠杆 |

### 五、预注册预测（跑之前写死）

1. **`PAIR`：卡对差异 < 1%。**
   依据：sweep7 Z2 的 k=1 臂在 (1,2) 读 @120t = **669.31**，
   sweep8 Z8 的**同一配置**在 (2,3) 读 **668.13**——**跨窗口已经只差 0.18%**。
   **证伪条件：同窗实测 |(1,2) − (2,3)| ≥ 2%**（B1c 的定价）。
   若触发，**布局因子（+6.57%，四因子链见「结果」一节）要重算**，
   因为那个因子的一侧跑在 (2,3)、另一侧跑在 (1,2)。
2. **`EG`：吞吐会降，但 `Running` 会升。**
   `--enforce-eager` 把 graph 池（0.55–1.13 GiB）还给 KV → 每副本 **+1–3 路**。
   预测 `Running` 从 **24 升到 25–27**。
   **证伪条件：`Running` 不动**——那说明 graph 池不是从 KV 池里扣的，这条机制要重写。
3. **`GMU`：0.95 让 `Running` 从 24 升到 27±2。**
   依据：+0.03 × 44.52 = **1.34 GiB ≈ 7 块 ≈ +3 路/副本**。
   **证伪条件：`Running` 不动，或吞吐反降 >1%。**
4. **`MB`：不设方向预测，这是一次探测。**
   只问一件事：**它到底改不改 `block_size`**（从启动日志的 `cache_config_info` 读）。
   **若 `block_size` 不变 → 这个旋钮在本模型上是死的，直接记死，不再排。**

### 六、记录要求

每点 3 次引擎态采样（Running/Waiting/KV%）、启动台账、seed 表、
`gpu_snap`、loadavg 快照。**`PAIR` 的两臂必须同窗口**——
这是这一轮里唯一一个"窗口不同就没意义"的相位。

### sweep9 结果（回填，2026-09-12 05:58:26–07:13:16 UTC）

`PHASES="PAIR EG GMU MB"`，5 次启动全部成功，无 `ABORT`，四相全跑完。
`PAIR` 的两臂确实落在**同一个窗口**里背靠背完成：(1,2) 06:09:07→06:13:39，
(2,3) 06:24:20→06:28:52。

**这是本线第一次把「卡对」当成被测对象，而不是当成一个需要回避的混淆。**

| 臂 | 卡对 | 窗口(UTC) | @48t 合计 | @120t 合计 | KV 池(token) | 池 vs 基线 | Running | block_size |
|---|---|---|---|---|---|---|---|---|
| R2c 终态配置 | (1,2) | 06:09–06:13 | 660.34 | 670.45 | 93,934 | — | 24 | 1584 |
| R2c 终态配置 | (2,3) | 06:24–06:28 | 660.97 | 672.66 | 93,934 | — | 24 | 1584 |
| ＋`--enforce-eager` | (2,3) | 06:39–06:44 | **544.09** | **574.99** | **105,403** | **+12.21%** | **27** | 1584 |
| ＋`--gpu-memory-utilization 0.95` | (2,3) | 06:55–06:59 | 661.27 | **683.19** | **108,134** | **+15.12%** | **28** | 1584 |
| ＋`--mamba-block-size 32` | (2,3) | 07:10–07:13 | 659.30 | — | 93,934 | 0.00% | 24 | 1584 |

判据兑付（阈值照预注册原文，一个字没松）：

| # | 预注册预测 | 实测 | 判决 |
|---|---|---|---|
| 1 | `PAIR` 差异 < 1%（证伪线 ≥2%） | **+0.10%**（@48t）/ **+0.33%**（@120t） | **HOLDS**，证伪线没碰到 |
| 2 | `EG` 吞吐降、`Running` 24→25–27（证伪：Running 不动） | −17.68% / −14.52%，`Running` = **27** | **HOLDS**（踩在预测带的上沿） |
| 3 | `GMU` `Running` 24→27±2（证伪：不动，或吞吐降 >1%） | `Running` = **28**，吞吐 **+0.05% / +1.57%** | **HOLDS**，证伪条件一条没触发 |
| 4 | `MB` 不设方向，只问 `block_size` 动不动 | **不动**（1584→1584，池逐字节相同） | 旋钮判死 |

#### 发现一：卡对效应在本配置上≈0，而且是同窗同配置的直接测量

B1c 当年把卡对效应定价 **2.21%**，那是跨窗口间接推的。这一轮两臂**同配置、同窗口、
背靠背**，实测 **+0.10%（@48t）/ +0.33%（@120t）**，KV 池逐字节相同（93,934），
`Running` 相同（24）。两件事：

- **布局因子（+6.57%）**一侧跑在 (2,3)、另一侧跑在 (1,2)，
  这个跨卡对链接**站得住**，不需要重算；
- 「卡对效应」的定价从 2.21% 收紧到 **<0.5%**（限本配置、本机、本窗口）。

#### 发现二：CUDA graph 池第一次被定价，vLLM 自己在启动日志里给了第一方换算

`--enforce-eager` 把池从 93,934 抬到 **105,403 token（+11,469，+12.21%）**，
`Running` 24 → **27**（稳态读数，KV 98.4%），代价是吞吐 **−17.68%（@48t）/
−14.52%（@120t）**。换算：**+3 路 ≈ +11,469 token ≈ 3,823 token/路**。

同一份启动日志里 vLLM 自己写着：

> `--gpu-memory-utilization=0.9200` 等价于不开 CUDA graph memory profiling 的
> `0.9038`。要维持同样有效 KV 池，把它提到 **0.9362**。

两句指向同一笔账：graph 池是**从 KV 预算里扣的**。所以结论不是「改用 eager」，
而是**这笔交易被定价了：3 路换 −15~18% 吞吐，亏**。cudagraph 从「一个没审过的
默认」变成了**一个被定价过的决策**——这正是这一相位要的东西。

#### 发现三：加显存只在墙上有收益——本线核心论点的又一次正面验证

`--gpu-memory-utilization 0.95`（+0.03 × 44.52 GiB = +1.34 GiB）：池 93,934 →
**108,134（+14,200，+15.12%）**，`Running` 24 → **28**，KV 用到 **99.5%**。

但吞吐：**@48t 只 +0.05%（661.27 vs 660.97），@120t +1.57%（683.19 vs 672.66）**。
@48t 时引擎根本没到墙（Waiting 0），多出来的池**没有地方可用**；@120t 到了墙
（Waiting 36），容量才变成吞吐。

**容量本身不是收益；容量只在容量是约束时才是收益。**

一条可直接用的经验换算：**每 +0.01 的 `--gpu-memory-utilization`
≈ +4,733 token ≈ +1.3 路/副本**（0.03 → +14,200 token / +4 路）。

风险记一笔：0.95 时 KV usage 顶到 **99.5%**，余量 0.5%；0.92 时是 98.2%。
生产上这不是「免费的两路」。

#### 发现四：`--mamba-block-size` 判死——配置进得去，效果出不来

| 读数 | 基线 | `--mamba-block-size 32` |
|---|---|---|
| `cache_config_info` 的 `mamba_block_size` | 16 | **32** |
| `user_specified_mamba_block_size` | False | **True** |
| `block_size` | 1584 | **1584** |
| mamba page padding | 0.51% | **0.51%** |
| KV 池 | 93,934 | **93,934** |
| @48t 吞吐 | 660.97 | **659.30（−0.25%）** |

旋钮**确实进了配置对象**，但下游一个数都没动。源码
`platforms/interface.py:_align_hybrid_block_size` 给了原因：这个字段只作为
`base_chunk_size = mamba_block_size or ...` 进 `chunk_size = lcm(...)`，算出的
`attn_block_size` **只在 `cache_config.block_size < attn_block_size` 时才写回**——
也就是**只能抬高、不能压低**。本模型的 `block_size` 已经被 mamba page 抬到 1584，
旋钮改不动它。

（同一函数里还有一行 `if mamba_cache_mode == "align": cache_config.mamba_block_size
= cache_config.block_size`，而本模型 align 是默认。按那行，metrics 里的
`mamba_block_size` 应该被反写成 1584，可实测回显的是 32。**这两处对不上，没有查清，
如实记为未决细节**，不影响「旋钮无效果」这个实测结论。）

**记死，不再排。**

#### 修正：§5.2 的「attention block size = 1,600 tokens」不是常数

§5.2 把块粒度写成了常数。实测它是 **`f(KV dtype, k)`**，四档全在归档日志里：

| KV dtype | k | block_size | mamba page padding | 归档出处 |
|---|---|---|---|---|
| bf16 | 0 | **784** | 0.13% | sweep7 `B0*` / `FV0` |
| fp8 | 0 | **1568** | 0.13% | sweep7 `B1*` / `R2b*` / `SB*` |
| fp8 | 1 | **1584** | 0.51% | sweep7 全部 `R2c*`、sweep8 `R2cZ8*`、sweep9 全部 |
| fp8 | 2 | **1600** | 0.88% | sweep8 `R2c2Z8*`、sweep4/5 台账 |

784 → 1568 正好 ×2：KV 从 bf16 降到 fp8，每 token 页减半，同一张 mamba page
就要两倍的 token 去盖；k 的那一档来自 mamba page 本身随 spec 变大。

§5.2 当年写 1,600 时手上是 C2（k=2）那一臂，**结论用在自己那一臂上是对的**，
但把它写成通用常数是错的。**本线终态配置（R2c，k=1）的块是 1584。**
对账不受影响：81,920 ÷ 1600 ≈ 51 块、93,934 ÷ 1584 ≈ 59 块，两条除法都还在原来的格子上。

#### 顺带读出来、以前没人念过的两行

- **`kv_cache_utils.py:1227`：`Add 3 padding layers, may waste at most 6.25% KV
  cache memory`**——每一份启动日志里都有，之前没人念。按日志自己的 6.25% 算，
  93,934 token 里约 **5,871 token ≈ 1.6 路/副本**是补出来的。这是本线第一次给这份税
  标价。**它能不能关，没查——列为新口子。**
- **`gpu_worker.py:804` 的内存分解**：`29.53 GiB 权重 + 2.59 GiB 峰值激活 +
  0.67 GiB CUDAGraph`，池 8.84 GiB。这条日志还直接给出了**直接旋钮的现成数值**：
  `--kv-cache-memory=8620057600`（8.03 GiB，贴合请求）/ `11832828928`（11.02 GiB，
  吃满）。§「直接旋钮」那条从「从没用过」变成「有现成数值可用」。

**证据完整性**：`sweep9/` **94 个文件（1.4 MB）**，脱敏按文首清单逐条过——
`<out-dir-9>` / `<nvme-root>` / `<model-dir>` 三类替换后复查：
`zhangwenchao` 残留 **0**、`/root/` 或 `/mnt/` 绝对路径残留 **0**、
IPv4 只剩 `127.0.0.1`（2832 处，回环）与 `0.0.0.0`（10 处）。
**这一轮的原始日志里压根没出现过主机地址**，所以没有需要事后回改的地址
（对比 sweep7 那次：那 12 份 server log 是被事后 sed 过的，字节已非原始）。

#### 限定

- 每点 **n=1**，单池 `FORCE_SEED=0`，单窗口；`PAIR` 两臂背靠背但不交错重复。
- `EG`/`GMU`/`MB` 全在 (2,3)，与基线**同窗同对**，可直接比；它们与 sweep8 的 Z8
  基线**不同窗**，跨窗口的链接只做记录、不做断言。
- `Running` 一律取**启动日志里的稳态读数**（`server_*.log` 的 `loggers.py` 行）。
  `engine_stats_*_s3.txt` 的最后一拍会抓到泄洪段（如 `Running 15 / Waiting 45`），
  **不能当稳态用**——这是这一轮新踩到的取证坑，记为 [坑 26](#坑下次直接抄) 的一条。
- `MB` 只测一个点（@48t）：既然 `block_size` 逐字节没动，补 @120t 没有信息量。

## sweep10 预注册（把 sweep9 新开的两条线接上，2026-09-12 07:2x UTC 落字）

sweep9 留下两样东西：一个**刚被定价的容量税**（CUDA graph 池 ≈ 3 路/副本），
和一条**一直在册但从没做过**的实验（§4 实验 1，TP2 fp8 未测）。这一轮就做这两件。

### 一、`CGS`：不给图池交全税，但也不退回 eager

sweep9 的 `EG` 证明了图池是从 KV 预算里扣的（池 93,934 → 105,403），
代价是 eager 的启动开销（−15~18%）。**但那是把整张图关掉的价格，不是图池本身的价格。**
启动日志里默认捕获了 **51 个桶**：`[1,2,4,8,16,24,…,120,128,136,…,496,512]`。
而本线的运行点在**每副本 24–60 路**（`--max-num-seqs 128`），
**136 以上的 35 个桶一次都不会命中，却照样占显存**。

`--cudagraph-capture-sizes`（`engine/arg_utils.py:1576`）直接给这张表。
`CGS` 臂 = R2c 终态配置 + 只保留 ≤128 的 19 个桶：`[1,2,4,8,16,24,32,40,48,56,64,72,80,88,96,104,112,120,128]`
——**把 136–512 的粗尾巴整条砍掉，小批量的细粒度原样保留**。

**预测**：`CUDAGraph memory` 下降 → KV 池上升 → `Running` 24 → **25–27**；
吞吐 **|Δ| < 2%**（保留图模式，不付 eager 的启动开销）。
**证伪**：KV 池不动（说明图池不由捕获表决定，机制要重写），
或吞吐掉 >3%（说明细粒度桶是必需的，砍桶不是免费午餐）。

### 二、`TP2F`：§4 实验 1 —— TP2 上的量化增益

**基线全程只有一个**（§0.3：两张卡、TP2、bf16），而**量化那一格从来没在这个基线上测过**。
链子上的 +30.10% 是在**副本布局**上量的。如果量化的增益依赖布局，
那"量化 +30.10%"这句话就只在副本布局下成立——这是个必须堵上的口子。

`TP2F` 两臂**同窗口、同卡对（2,3，TP2 的历史原配）、同 sched（都用 serve 默认）**：
- 臂 1：TP2 bf16（无 quant flag）；
- 臂 2：TP2 + `--quantization fp8 --kv-cache-dtype fp8`。
各测 @48、@120。**@48 的 bf16 读数应与历史锚点 375.09 对上**——对不上说明窗口/卡对有问题，
先查窗口，不解读 fp8。

**判据（按修正轨迹 11 的教训，方向与幅度分开兑付）**：
- **A（方向）**：fp8 > bf16。这一条必须兑现。
- **B（幅度）**：增益落在 **[+15%, +40%]**。落在带外**不宣布机制**，
  只记录"量化的增益依赖布局"这个观察，单独追。

### 三、记录要求

同前：每点 3 次引擎态采样、启动台账、seed 表、`gpu_snap`、loadavg。
**额外**：每臂必须从启动日志抽 `Actual usage is ... for CUDAGraph memory`
这一行的 GiB 数——`CGS` 的因变量就是它，没有这个数这一轮等于没测。
**`TP2F` 两臂必须同窗口**（和 sweep9 的 `PAIR` 同一条纪律）。

### sweep10 结果（回填，2026-09-12 07:23:35–07:55:57 UTC）

`PHASES="CGS TP2F"`，三次启动全部成功（外加一次 07:22 的夭折，见 [坑 27](#坑下次直接抄)）。

#### CGS：图池的便宜解法成立——64% 的图池是「永远用不到的桶」

| 读数 | sweep9 基线 `P9pair23` | CGS（砍到 19 桶 ≤128） |
|---|---|---|
| 捕获桶数 | 51 | **19** |
| **`CUDAGraph memory`** | **0.67 GiB** | **0.24 GiB（−64%）** |
| 峰值激活 | 2.59 GiB | 2.15 GiB |
| **KV 池** | **93,934** | **103,219（+9,285，+9.88%）** |
| `Running`（稳态） | 24 | **26**（读数 25/26/26） |
| @48t 吞吐 | 660.97 | **650.00（−1.66%）** |
| @120t 吞吐 | 672.66 | **675.70（+0.45%）** |

| 判据 | 结果 |
|---|---|
| A：graph 内存降 **且** `Running` > 24 | **HOLDS**（0.67→0.24、24→26） |
| B：吞吐 \|Δ\| < 2% | **HOLDS**（−1.66% / +0.45%） |
| 证伪：池不动 / 吞吐 ≤ −3% | **未触发** |

**机制**：`EG` 把整张图关掉能拿回 **11,469 token**，CGS 只砍粗尾巴就拿回 **9,285 token**
——**9,285 / 11,469 = 81% 的图池，是那些本线永远命中不了的桶（136–512）占的**。
而且这部分**不用退到 eager**，不付启动开销。

**代价是真实的，记清楚**：@48t **−1.66%**，@120t **+0.45%**。
砍桶之后落在空隙里的 batch 会被补齐到下一个桶，**墙下白付 padding、墙上才赚回容量**。
这和 `GMU` 是同一条规律的两个面：**容量的收益只在容量是约束时出现**。
（跨窗口比较：基线与 CGS 不同窗，boot-log 那三列是硬的，两个吞吐差值是参考值。）

#### TP2F：量化增益与布局无关——+30.05% vs 副本线的 +30.10%

| 臂 | @48 | @120 | TP2 KV 池 |
|---|---|---|---|
| TP2 bf16 | **373.86** | 432.91 | 264,071 |
| TP2 fp8 | **486.22** | 539.05 | **684,032（×2.59）** |
| 增益 | **+30.05%** | **+24.52%** | |

| 判据 | @48 | @120 |
|---|---|---|
| A（方向 fp8 > bf16） | **HOLDS** | **HOLDS** |
| B（幅度 ∈ [+15%, +40%]） | **HOLDS**（+30.05%） | **HOLDS**（+24.52%） |

**这是本线最想要的那个数**：副本布局上量出来的量化因子 **+30.10%**，
换到 **TP2 布局**上、另一轮 sweep、另一个窗口，量出来 **+30.05%**——
**差 0.05pp**。所以「量化 +30.10%」这句话**不依赖布局**，§4 实验 1 这个在册最久的口子关掉。

两条附带：

- **基线锚点复现**：历史 `B0` @48 = **375.09**，本轮同配置读 **373.86**，**−0.33%**。
  跨窗口能对到 0.33%，整条 +74% 链的基线是稳的。
- **增益随并发下降**：@48 **+30.05%** → @120 **+24.52%**。
  机制与上面同源：@48 时 bf16 的池（264,071）装不下并发，fp8 把池扩到 684,032 **把墙拆了**，
  多出来的全是吞吐；@120 时两边都不卡在池上，量化的收益回落到它本身的算力/带宽贡献。
  **一条可以拿去面试的表述**：量化的 +30% 里有相当一部分不是"算得更快"，是"装得更多"。

#### 证据与又一次地址脱敏

`sweep10/` **42 个文件 / 504 KB**。脱敏后 `zhangwenchao` 残留 0、`/root` 与 `/mnt`
绝对路径残留 0，IPv4 只剩 `127.0.0.1` 与 `0.0.0.0`。

**这一轮又抓到一次宿主私网地址**：`server_P10tp2b.log` 与 `server_P10tp2f.log`
里各有一处 `mq_connect_ip=10.82.0.23`（TP2 走 multiproc executor，才会打这行；
sweep9 的副本臂是 TP=1，没打）。**已打码为 `<host-ip>`——这两份日志不是原始字节。**
和 sweep7 那次是同一个来源、同一个修法；区别是这次在**推送之前**就扫出来了
（扫地址这一步现在是归档流程的固定动作，不再只扫路径）。

#### 限定

- `CGS` 的基线与它**不同窗**（sweep9 07:10 vs sweep10 07:34）。判据 A 的两条
  （graph GiB、池 token）都是**启动日志里的硬数**，不依赖窗口；判据 B 的吞吐差值是参考值。
  若要把 B 做成硬判据，需要同窗再跑一次基线臂。
- `TP2F` 两臂**同窗、同卡对（2,3）、同 sched**，可直接比；与历史锚点是跨窗口，只作校验。
- `tp2_phase` 的每个点固定 **96 prompts**（全矩阵的约定），所以 `conc 120` 实际并发上限是 96。

## sweep11 预注册（两个容量源能不能叠加 —— 2×2，2026-09-12 08:0x UTC 落字）

**这一段是在任何数字出现之前写死的。** 判据一旦写在这里，跑完只能填 HOLDS / FIRED，
不许事后放宽，也不许把不成立的那条悄悄删掉——不成立就进 `## 修正轨迹`。

### 零、这一相位回答什么

到 sweep10 为止，这条线攒了两个**各自独立测出来的容量来源**：

| 来源 | 动作 | 池增量（实测） | 代价（实测） |
|---|---|---|---|
| `CGS` | 砍 CUDA graph 捕获表到 ≤128（19 桶） | +9,285 token | @48t **−1.66%**，@120t +0.45% |
| `GMU` | `--gpu-memory-utilization` 0.92 → 0.95 | +14,200 token | @48t +0.05%，@120t +1.57% |

两个都落在同一条机制上：**容量只在容量是瓶颈的那一档换得动吞吐**。
但**没有人测过两个一起给会怎样**。这既是最实际的问题（面试一定会问
「你到底发什么配置上线」），也是一个真的科学问题：**它们是两个来源，还是同一个来源的两次读数？**

### 一、设计：2×2，同卡对同窗口，四臂

四臂**都在 (2,3)**、都在同一个窗口里背靠背起、每臂一点 `w0` 弃掉再 @48t / @120t，
所以四臂之间**任何一格对任何一格都是同窗同卡对**，B1c 那 ≤2.2% 的卡对效应
在这一相位内部**不构成限定**（对历史锚点仍是跨窗，只作校验）。

| 臂 | tag | 配置 | 上表实测池（预测） |
|---|---|---|---|
| A00 | `P11a00` | R2c 终配（`fp8 + kv fp8 + k=1`），基线 | 93,934（同窗重测） |
| A10 | `P11a10` | R2c **+ CGS** | 103,219 |
| A01 | `P11a01` | R2c **+ GMU 0.95** | 108,134 |
| A11 | `P11a11` | R2c **+ CGS + GMU 0.95** | **117,419（= 93,934 + 23,485，可加预测）** |

跑 `A00` 的理由不是凑数：sweep9/sweep10 的基线都来自**别的窗口**，
不同窗比出来的差值只能当参考。这一相位要下的是"可加性"结论，
基线必须同窗，否则判据 B 根本不成立。

### 二、预注册预测与判据

**预测 1（可加性的方向）**：池 `A11` > `max(A10, A01)` = 108,134。
- 判据 A：`pool(A11) > 108,134` → **HOLDS**。
- **证伪条件**：`pool(A11) ≤ 108,134`。若发生：两个容量源不是两个，
  是同一个来源被两条路径重复计价（比如 CGS 省下的图池本来就在 GMU 抬高的上限里
  被重新吃掉了），那么"每 +0.01 gmu ≈ +4,733 token"这条换算和 §5.2 的容量账
  **都要重写**。

**预测 2（可加性的幅度）**：池增量落在两臂增量之和的 ±15% 内。
- 判据 B：`pool(A11) − 93,934 ∈ [19,962, 27,008]`，即 **`pool(A11) ∈ [113,896, 120,942]`**
  → **HOLDS**。
- **证伪条件**：出区间。低于下界 = **次可加**（两个来源互相挤占，比如 GMU 抬上限后
  CGS 省下的图池被 KV 吃掉的比例变了）；高于上界 = **超可加**（若发生，说明还有
  第三个没识别出来的效应，必须去找）。任一种发生都**照实记进修正轨迹**，
  并把交互项写成数字（`A11 − A10 − A01 + A00`）。

**预测 3（吞吐：代价不叠加）**：
- 判据 C1：`A11` @48t 相对 `A00` 的跌幅 **≤ 2.5%**。中心预测是 −1.61%
  （CGS 的 −1.66% 加 GMU 的 +0.05%）。**证伪条件**：跌破 −2.5% ——
  两个"墙下不划算"的代价会相乘，那么"墙下别动容量"这条口语结论要加上限定。
- 判据 C2：`A11` @120t **≥ max(A10, A01)**。**证伪条件**：低于两者。
  若发生，容量不再是唯一决定项，@120t 的排序里有别的因子。

**预测 4（描述性，不设证伪线，但必须记录）**：`Running(A11) ∈ [28, 31]`
（A00 24 / A10 26 / A01 27 是历史读数，跨窗，只作中心值）。

**已知风险，写在跑之前**：GMU 单独臂已经把 KV 占用顶到 **99.5%**。
再叠 CGS 的 +9,285 token，占用会贴到 ~99.9%，**boot 起不来、或第一个点就撞抢占**都是
可能结局。**如果 boot 失败，那本身就是要的答案**（"0.95 + 剪桶上不去"，上线配置退回 0.94 /
或只取其中一个来源），失败日志与报错行要原样归档，不许只记一句"失败"。

### 三、记录要求（在 sweep9/sweep10 之上追加）

四臂都跑 `startup_lines` + `cfg_metrics` + `gpu_snap`，另外**每臂都要抓**：
1. `GPU KV cache size: N tokens`（池的硬数，判据 A/B 就靠它）；
2. `and X GiB for CUDAGraph memory`（CGS 是否真的只花 0.24 GiB）；
3. `cudagraph_capture_sizes` 的桶数（必须是 19，不然 CGS 没生效）；
4. `Running` **只从 `server_*.log` 的稳态读数取**，不取 `engine_stats_*_s3.txt` 最后一个采样
   （坑 26）；
5. `A11` 若崩，`server_P11a11_*.log` 的最后 40 行原样进 `sweep11/`。

### 四、这一相位的产出物（面试口径）

跑完要么得到**一个可以直接回答"发什么配置"的数字**，要么得到**"不能叠加"这个否定结论**。
两种都算这一相位成功；唯一不算成功的是**没测出来**。

## 复现

### sweep7 运行手册（命令、相位、证据落点）

**一条命令 = 一臂。** 协议是「一次一个实验、记录完整、分析后回想遗漏，再决定下一步」，
所以脚本有相位闸门；**不设 `PHASES` 时仍是预注册的完整窗口**（`B0 B1 R2c R2b B0p`），
脚本的默认行为与预注册文档永远一致。
（下文的 `<root>` = 远端放脚本的工作目录，`<nvme-root>` = 缓存与证据落点，
`<venv>` / `<model-dir>` 同前。）

> **⚠️ 换脚本前必须先确认远端没有在跑（本仓脚本可能领先远端副本）。**
> **bash 是边读边执行的**：覆盖一个正在运行的 `mtp_bench7.sh` 会让它从旧偏移量
> 读到新文件的内容，可能从半行中间恢复并执行垃圾。**正确顺序**：
> `pgrep -f mtp_bench7`（从宿主机，容器内 `ps` 看不到宿主机 PID）确认无进程
> → 再上传 → 再 `bash -n` → 再启动。
> 同理，**补跑相位时不要在 run 进行中改脚本**——数据都在原始 bench log 里，
> 缺的只是汇总打印，宁可在跑完后手工提取，也不要冒这个风险。

**卡对可用 `RPA` / `RPB` 覆盖（默认 `1` / `2`，即预注册的 GPU1+2）。**
写这个开关的原因：2026-09-11 夜跑时邻居长期占着 GPU1，而 GPU2+3 是空的，
不写开关就整个窗口作废。**它改变什么、不改变什么要写清楚**：
每个相位**自己内部**的对照都在同一次 boot、同一对卡上完成，卡对在这些对照里
**自动抵消**（R2bZ 的阶梯、R2cZ 的噪声底与交叉点、R2cABr 的三点都不受影响）；
它只影响**跨相位**的连接——与历史 `R2b_c120t/144t` 的交叉点、以及 FV 的
「布局」那一级对 R2b48z。**B1c 已把卡对效应定价为 2.21%**，所以这些连接
带着 **≤2.2% 的保留**，凡引用必须标注。**一个相位内部绝不允许混卡对**：
一次 boot 的两个副本永远同时取 `RPA` 和 `RPB`。

```bash
# 邻居占着 GPU1 时，改用空闲的 GPU2+3（其余与预注册完全一致）：
RPA=2 RPB=3 PHASES="R2bZ R2cZ FV" bash <root>/mtp_bench7.sh
```

~~~bash
# 只跑基线臂（本次这么跑的）：
cd <root> && PHASES=B0 setsid nohup bash <root>/mtp_bench7.sh \
  > <root>/mtp7_run.log 2>&1 < /dev/null &

# 下一臂（分析完再决定跑哪几个）：
cd <root> && PHASES=B1   setsid nohup bash <root>/mtp_bench7.sh > <root>/mtp7_run.log 2>&1 < /dev/null &
PHASES="R2c R2b"         setsid nohup bash <root>/mtp_bench7.sh > <root>/mtp7_run.log 2>&1 < /dev/null &
# 全窗口 + 可选相位：
PHASES="B0 B1 R2c R2b B0p" RUN_EXTRA=1 RUN_4CARD=1 bash <root>/mtp_bench7.sh
~~~

**证据落点**（全部落在 <nvme-root>，不占容器 overlay，见坑 14）：

| 文件 | 内容 |
| --- | --- |
| `<out-dir-7>/run.log` | 时间线（**容器 UTC**，坑 3） |
| `<out-dir-7>/server_<arm>.log` | 每臂完整服务日志（台账来源） |
| `<out-dir-7>/server_startup_lines.txt` | 内存台账：KV 池 / CUDA graph / 权重字节 / non-default args（坑 12 修好的那批） |
| `<out-dir-7>/bench_<arm>[_pN].log` | `vllm bench serve` 原始输出（指标的**唯一**真源） |
| `<out-dir-7>/summary.txt` | 抓好的指标汇总（单卡臂 `summarize_single`，副本臂 `summarize_dual` 出 SUM/MEAN） |
| `<out-dir-7>/engine_stats_<arm>_s{1,2,3}.txt` | 每点 3 次引擎态（Running/Waiting/KV usage），sweep6 只采 1 次丢了 R2b_c96t |
| `<out-dir-7>/gpu_snapshots.txt` | 每相前后 GPU 快照（邻座证据） |

**记录规矩**：`bench_*.log` 是原始证据，任何时候都能重算；`summary.txt` 只是方便看，
**数字对不上以 `bench_*.log` 为准**。抓取正则有坑 12 的前科，新表必先对真实输出自测。

**脚本内含的全部启动/压测参数**（照抄即可，不用手敲）：

~~~bash
# 服务（两卡 TP2 臂；副本臂见 boot_replica）
CUDA_VISIBLE_DEVICES=2,3 HF_HUB_OFFLINE=1 <venv>/vllm serve <model-dir> \
  --port 8343 --served-model-name qwen38-27b --max-model-len 8192 \
  --tensor-parallel-size 2 --max-num-seqs 128 [--quantization fp8 --kv-cache-dtype fp8]

# 服务（单卡副本臂）
CUDA_VISIBLE_DEVICES=<gpu> HF_HUB_OFFLINE=1 <venv>/vllm serve <model-dir> \
  --port 834<1|2> --served-model-name qwen38-27b --max-model-len 8192 \
  --quantization fp8 --max-num-seqs 128 --kv-cache-dtype fp8 \
  --max-num-batched-tokens 8192 \
  --speculative-config '{"method":"mtp","num_speculative_tokens":1}'

# 压测（每路副本各发一份，吞吐相加、延迟取均值）
<venv>/vllm bench serve --backend openai-chat --endpoint /v1/chat/completions \
  --tokenizer <model-dir> --dataset-name random --random-input-len 1024 \
  --random-output-len 256 --temperature 0 --num-prompts <N> \
  --max-concurrency <C> --host 127.0.0.1 --model qwen38-27b --port <port>

<venv>/python mtp_greedy.py <port> <ARM>   # 贪心等价探针（需要时）
~~~

## 坑（下次直接抄）

1. serve 必须 `setsid bash -c "..."` 包一层再后台：否则 stop 阶段按进程组
   kill 会把整个 sweep 脚本一起带走（sweep1 的 M1 臂赔过一次）；
2. V1 引擎退出常带 SIGABRT（exit 134/1）——JSON 已写完即算成功，各臂独立容错；
3. 容器时钟 UTC：run.log 里 02:08 ≠ 北京 02:08，对表先想清楚时区；
4. spec 配置会自动改写 `max_num_scheduled_tokens` 并缩 KV 池——归因时必须
   拆开（见公平清单 side-effect 行）；
5. 共享机 GPU 被他人批任务占用时，用"等卡 watcher"（轮询 mem+util，空闲即
   自动开跑），比人工盯守靠谱；GPU0 常驻一个 ~2.6GB 外部 daemon（pid 自
   2026-09-08 起），它的空闲阈值要放宽到 4,000 MiB，两卡标定才能起步；
6. AWQ 管线在 llmcompressor 0.13 里是 deprecated 工厂函数，transform 半边
   **没有 targets/ignore 过滤**（会碰 GDN 权重做 scale 改写）——混合精度
   要过滤层的量化走 GPTQModifier（同为 W4A16 格式）；
7. **compressed_tensors 的 targets/ignore 匹配语义是「精确名相等 或 `re:`
   前缀正则（re.match 锚定开头）」**（`compressed_tensors/utils/match.py`
   `match_name`）——glob 通配符（`model.visual.*`、`*linear_attn*`）**静默
   永不命中**，不报错不警告。首轮 int4 脚本因此差点把 GDN+mtp+visual 全
   量化掉（唯一护栏是 visual fc2 的 4304 列不被 128 整除触发的 divisibility
   报错）。两个教训：(a) ignore 写法必须是 `re:.*linear_attn.*` 这类；
   (b) **防呆校验必须复刻框架自己的匹配器**（脚本内 `_match_name` 副本），
   用另一套语义（如 fnmatch）校验等于自己骗自己。修正版断言还顺带排除了
   `self_attn.{q,k}_norm`（RMSNorm 非 Linear，名字含 self_attn 会被误数）；
8. 容器内 `ps` 看不到宿主机 PID（PID 命名空间隔离）——查他人 GPU 进程要
   从宿主机侧 `ps`，否则会把"进程不存在"误判成"任务已结束"；
9. llmcompressor save 的 checkpoint **不带视觉组件**（`preprocessor_config.json`
   / `video_preprocessor_config.json` / `vocab.json` / `merges.txt`）——起服务时
   `get_image_processor_dict` 直接 OSError（fp8 轮 §12.4 同款坑）。修法：从源
   模型目录抄四个文件进 checkpoint。bench5 首跑因此 ABORT 一次，重启即过；
10. **ceval_eval.py 只能在装了 `datasets` 的 venv 里跑**（`<lc-venv>`），`<venv>` 没有
   ——bench 脚本里内嵌 ceval 步骤会静默降级成错误。int4 轮的 C-Eval 因此
   未跑成；补跑：起 int4 服务后用 `<lc-venv>/bin/python ceval_eval.py
   --port 8331 --tag W4 --model-name qwen38-27b`（tag 用 w4 与 fp8 轮对齐）。
11. **补跑 ceval 时别把 `HF_HUB_OFFLINE=1` 带进去**：脚本靠 `setdefault`
   自配 `HF_ENDPOINT=hf-mirror` + 在线——`get_dataset_config_names` 需要到
   mirror 拉 C-Eval 科目列表（数据本体才走本地缓存）。强压离线后 config
   枚举退化为 `['default']`，而缓存里只有科目名 config → 5 秒即崩
   （`Couldn't find cache for ceval/ceval-exam for config 'default'`，
   错误信息里还把全部科目列出来误导你以为缓存坏了）。首次补跑因此白起了
   一次服务。
12. **从 bench 文本输出抓指标时，正则的冒号要跟在完整标签后**：行格式是
   `Output token throughput (tok/s):    164.01`——冒号在 `(tok/s)` 之后
   不在 key 之后，`key + r":"` 的正则静默失配返回 nan（sweep6 的
   summarize_dual 因此全输出 nan，总吞吐改为手工从 p1/p2 相加）。教训同
   match_name 坑：**校验解析器要对着真实输出格式测一次**，别对着想象中的
   格式写。
13. **`\"` 只在「双引号 `bash -c "..."` 内部」这一层是对的，提到顶层单引号
   赋值就变成字面反斜杠**。bench6 把 spec JSON 直接内联在 `bash -c "..."` 行
   里，所以 `'{\"method\": ...}'` 正确；bench7 把 k=1 的 config 提成顶层
   `SPEC1='...'`，照抄同一串就错了——变量展开不会二次处理转义，vLLM 收到
   `{\"method\": ...}` 直接 `JSONDecodeError` 拒绝启动。**检测方法**：拿一个
   打印 argv 的 stub 脚本顶替 `<venv>/vllm`，把 `boot_replica` 的引号结构原样
   跑一遍再 `json.loads`。不测的代价极不对称：B0（bf16，不带 spec）会正常跑
   完，只有 R2c/R2b 两个副本臂会在起服务时崩——**整窗白烧，而且看起来像
   「副本架构不行」而不是「脚本写错了」**。
14. **overlay 被写满是这条线的结构性风险，不是一次意外**。容器只有 overlay
   一个可写层，而 vLLM / torch / HF / pip / uv / Triton 默认全写 `$HOME` 和
   `/tmp`；这一线从来没设过 `VLLM_CACHE_ROOT` / `TRITON_CACHE_DIR` / `TMPDIR`
   / `XDG_CACHE_HOME`，于是每个权重下载、每个 venv、每份编译缓存都落在
   overlay 上。2026-09-11 撑到 894G/894G（剩 635MB），**症状是 vLLM 根本起不
   来**。宿主机 NVMe 是 bind mount、默认没有任何指针指向它。修法：脚本头部导
   出三个缓存变量 + `OUT` 一起指向 NVMe，模型目录走软链（路径零改动）。同机
   其他线（Qwen3-4B / Qwen2.5-Omni / relax-*）早就把 `models/ exps/ repos/`
   放在 NVMe 上了，只有本线堆在 overlay。
15. **共享机上只动自己这条线的目录和文件**。清理前必须按「归属」分三档，不是
   按「大小」：**(a) 本线自己造的、可重跑** → 可删（构建产物、编译缓存、临时
   checkpoint）；**(b) 别线的 venv / 模型 / 数据缓存** → 一律不碰，删掉要对方
   重装重下，而且可能正好打断别人挂在后台的 watcher（本机就有几处按空闲轮询
   自动开跑的 watcher 脚本）；**(c) 用途不明** → 只报不动。判断归属看
   创建时间 + 名字里有没有本线的标识，拿不准就归 (c)。
   **搬迁同理**：本线的模型目录可以移到共享盘再留软链（路径零改动、可逆），
   别人的模型目录即使腾出的空间更多也不动。
16. **`--dataset-name random` 在固定 seed 下是确定性的，同一个 boot 里跑多个
   bench 点会让后跑的点吃到先跑点的前缀缓存**。`vllm bench serve` 的 `--seed`
   默认 `0`（`serve.py:1968` 全局 `np.random.seed(args.seed)`，`datasets.py:2406`
   把 `args.seed` 传给 `RandomDataset(random_seed=...)`，类内
   `np.random.default_rng(random_seed)`）——**同样的 seed + 同样的
   `--random-input-len`，先生成的 prompt 逐 token 相同**。所以 conc 48 跑 96 条
   时，前 32 条与同 boot 里先前 conc 16 跑 32 条的那批完全一样：
   32/96 = **33.3%** 的请求整段命中前缀缓存。
   本次实测（B0 首跑）：`@16` 全程 `Prefix cache hit rate: 0.0%`，紧接着的 `@48`
   是 **29.1–32.4%**；Median TPOT 因此从 sweep6 同配置的 116.24ms 掉到 88.19ms，
   吞吐 425.69 vs 锚点 372.07（**+14.4%**）。而服务端 flags、KV 池
   （264,071 tok / 32.24x）、KV usage（43.9%）、客户端 108 个命名空间键**全部
   逐位相同**——只看配置会误判成"机器变快了"。
   **检测方法**：比对 engine log 里的 `Prefix cache hit rate`；每个 bench 点的
   第一个采样必须是 0.0%，否则该点已污染。**修法**：每个 bench 点给独立
   `--seed`，让 prompt 池互不重叠，顺序就彻底无关（换顺序只是把污染挪到别的点）。
   **回看 sweep6**：TP2 的 `@48` 是整窗第一个 bench（干净），`@96`（469.62）被
   `@48` 污染；`R2b @48t` 干净、`@96t`（622.74）被污染。**头条
   `R2c @48t = 618.8` 与 `TP2 @48 = 372.07` 都是各自 boot 的第一个 bench，
   两个都干净，+66% 的对比本身没塌。**
17. **引用「@16 / @48」这类口径名之前，先分清这个点是「客户端受限」还是
   「引擎饱和」——这比钉 `--num-prompts` 更要紧**。
   **(a) 实测（本窗 B0，两 boot）**：@16 是**客户端受限**点，全程
   `Running: 16 / Waiting: 0 / KV usage: 14.6%`，`--max-concurrency 16` 把在飞
   请求钉死在 16。`--num-prompts` 96 条的 duration 102.57s 恰是 32 条（34.56s）
   的 **2.97 倍**，而速率只差 **1.1%**（239.61 vs 237.07）——**在客户端受限点上
   条数只改时长、不改速率**。
   **(b) 由此作废一条旧推断**：我曾把 09-06 的 @16 = 284.9 归因于"32 条被 ramp
   主导"（见修正轨迹 6），并据此预注册 B0-16 ≈ 270-310；实测 239.61，**假设
   证伪**。真因不在条数，但也不在"机器快慢"：同批 09-06 ABBA 的 @48 = 350.2 →
   本窗 375.09（**+7.1%**），@16 却是 284.9 → 239.61（**−15.9%**），一高一低
   不同向。**处理方式不是猜，是标注**：那些数字在仓库里查不到口径字段，
   §0 的跨窗那一行应当撤下或显式标"未复现"。
   **(c) 纪律**：跨窗/跨轮引用前，从 `bench_*.log` 的客户端 Namespace 反查
   `num_prompts` / `max_concurrency` / `request_rate` / `random_input_len` /
   `random_output_len` / `temperature` / `backend` / `seed`，少一样都不可比
   （本窗起这条 Namespace 随 bench log 一起入库，就是为了以后不必再猜）。
   **(d) 用法**：@16 这类客户端受限点只能当**延迟参考**（TTFT/TPOT 分布），
   不能当容量读数；拿它的吞吐去横比其他配置会造出假结论。

18. **抓启动台账的正则失配是静默的——台账缺项必须回原始 server 日志确认，
   不能信 summary。** 0.28.0 里唯一给全四项的行是 `gpu_worker.py:804`：
   「Actual usage is X GiB for consumed memory (weights + non-torch),
   Y GiB for peak activation, and Z GiB for CUDAGraph memory」——**行文与老版本
   不同**（老版是 `model weights take X GiB`）。我的 `startup_lines` 抄的是老
   措辞，于是 B0/B1 两臂**一行台账都没抓进去**，而证据文件看上去是完整的
   （有 Available KV cache / CUDA graph 池 / 池 token 数），不报错、不警告。
   数值一直在 `server_<arm>.log` 里躺着。教训与坑 12/7 同源：**解析器/正则
   要对着真实输出自测**；而且「我这边四项闭合」这种话，必须能在**已入库的
   证据文件**上重算出来才算数。修法：正则补 `consumed memory|peak activation|
   CUDAGraph memory|Model loading took`，`head` 放宽到 40。
19. **`--max-num-batched-tokens` 的默认值按 usage context 分档，跨调用方式/
   跨臂可能差 4 倍，且不写进公平清单就会静默污染对比。** 0.28.0
   `arg_utils.py:2578`（非 H100/A100）：
   `{LLM_CLASS: 8192, OPENAI_API_SERVER: 2048}`。`vllm serve` → **2048**，
   `LLM(...)` 类 → **8192**；显式传值会出现在各臂 non-default args 里，正好
   可以逐臂核对。本线就踩了：B0/B1 无此参数（2048），sweep6 的 R2b/R2c 显式
   8192 —— **+66% 的头条对照里混着 4 倍预填充步长差**。纪律：**每臂的
   `non-default args` 必须逐字段对一遍**，不能只比"动了哪个旋钮"。

20. **「占位符→假路径」的语法检查产物不能当上传产物；md5 自校验在"比的是不是
   正确的那份"之前毫无意义。**（2026-09-11 现场，B1b 首次启动踩到。）
   为跑 `bash -n`，我把 `<nvme-root>` 之类替换成假路径
   （形如 `/tmp/fake-root`——真路径不必写进本地临时文件），检查通过后
   **没重新生成**就把它上传，再拿这份的 md5 与本地同一份比对——**当然一致**，
   于是"校验通过"。后果：那一轮跑在假路径上——`OUT` 被 `mkdir -p` 新建、
   证据写进假根下、`VLLM_CACHE_ROOT`/`TRITON_CACHE_DIR`/
   `TMPDIR` 全部指向那里（vLLM 在冷缓存上重编译，进程 99% CPU **看起来一切
   正常**），而且**在共享盘根上多建了一个不属于本线的目录**。
   **检测方法**：`run.log` 的 mtime 不前进（脚本开头就写一行），
   `grep -n '^OUT=' <远端脚本>` 看到假路径。
   **修法**：替换产物分两份，`*.check.sh`（假路径，只喂 `bash -n`）与
   `*.real.sh`（真路径，**唯一**上传源）；上传后先断言远端文件
   (a) 无 `<占位符>`、(b) 无假路径、(c) `^OUT=`/`^VENV=`/`^MODEL=` 等于真值，
   **三条都过了 md5 才有意义**。同族：坑 7/12/13/18 全是"校验器没对着真目标"。
21. **seed 变量是个脚本级全局 → 每次调用重置 → 跨 boot 撞号，可比性悄悄受损。**
   （2026-09-11 晚复现审计查出。）脚本里写 `SEED=700`、每个相位 `$((SEED+1))`，
   本意是"每个点一个不同的池"。但**每个相位是独立一次 `bash mtp_bench7.sh` 调用**，
   全局重新初始化 → 每次相位都从 701 开始 → 实际撞号 **701×6 / 702×6 / 703×2 / 0×2**。
   **关键：污染没有发生**——349/349 条引擎采样全是 `Prefix cache hit rate: 0.0%`，
   撞号全部是**跨 boot**（每个相位新起服务器，KV 缓存是新的）。所以结论没被污染。
   **受损的是可比性**：调度步长那一段是 `B1_c48`(seed 702) vs `B1b_c48`(seed 701)，
   两个不同的池在比（量化后由 SB 定价为 ~1.2pp，见修正案 4）。
   **补救（无需重跑的取证路径）**：**每个 bench log 自己写着 seed**——
   `Namespace(... seed=N ...)` 就在日志第一行。把它抽出来即成一张
   **非推断的 tag→seed 表**（已入库 `sweep7/seeds.txt`，24 行）。
   **修法**：① 冻结表（已记录 tag 保留实测 seed，保可复现）；② 新 tag 用
   `cksum % 8000 + 1000` 兜底；③ `seed_snap` 同相位撞号告警；④ `seed_audit`
   启动时打印别名组。已验证 24/24 tag 原值还原。
   **通用形态**：**任何"每个点自动取个不同数"的机制，只要脚本会被多次调用，
   就必须把分配表落盘，不能靠运行期计数器**——否则它静默地退化成"每次都从头开始"。
22. **`gpu_snap` 记的字段决定你能回答哪类问题；少一个字段就少一条归因路径。**
   （2026-09-11 晚查出。）原快照只有 `memory.used / utilization.gpu / clocks.sm /
   power.draw / temperature.gpu`。缺失的恰好是**区分「机器变慢」与「我们的配置变了」**
   的那三个：`clocks.mem`、`power.limit`、`clocks_throttle_reasons.active`。
   在共享机上这三条是「邻居把卡占了」和「我们改坏了」的唯一分界线。
   已补齐（老行无新字段、新行有，读取时按有无字段解析）。
   **通用形态**：**"该记什么"要由"以后可能要回答什么问题"反推，不能只记当时顺手能拿到的**。
23. **恒等式被当成第二个证据（本页最隐蔽的一类错误，本轮抓到两处）。**
   （2026-09-11 晚自查。）① 「`steps/s` 排序与吞吐完全一致」——`steps/s = drafts/wall`、
   `tok/s = const/wall`，两者都是 `1/wall` 乘常数，**排序必然一致，零信息**；
   ② 「四因子乘积闭合到 0.001%」——四段是相邻节点的比值，乘积欠项后
   **必然**等于 end/start，无论中间值是多少。
   两处都让我把"同一个测量"误当成"两个独立证据"，从而高估了结论的稳固度。
   **检查法**：写下「A 与 B 一致」之前问一句 **「这两个量是不是由同一个量除出来的？」**
   是 → 它不构成证据。同族：修正轨迹 6 号（用未知解释未知）——都是
   **在只有一个测量时制造出两个证据的幻觉**。
24. **「改了一半的静默失败」比没改更危险；总结块必须拿真实日志干跑。**
   （2026-09-11 晚，收紧夜跑脚本时在自己的新代码里抓到。）
   FV 相位的汇总块要读两类文件：单副本 `bench_<tag>_c48.log`、
   双副本 `bench_<point>_p<i>.log`。先前发现过 bug①「给已含 `_c48t` 的 tag
   又拼了一次 `_c48t`」，**当时以为改好了**——实际只改了外层 `dual()` 的
   tag 拼法，`dual()` 内部仍调 `val()`，而 `val()` 照旧无条件拼 `_c48`，
   于是去找 `bench_R2b48z_c48t_p1_c48.log`：**文件不存在 → 返回 nan →
   区块"成功"结束**，布局/投机两行和总量全是 nan，只在纸面上看着像跑完了。
   **这是本轮唯一一处「差点把 nan 写进结论」的地方，而它是靠干跑抓到的**：
   把两个 heredoc 抠出来，对**真实日志**（加几个改名副本凑齐文件名）跑一遍，
   看每个数是不是有限——第一次干跑立刻 `FileNotFoundError`，
   第二次（放宽成 nan）看到 `布局 ... +nan%`，第三版才加上
   `completeness: INCOMPLETE -> <tag>` 的首行自报。
   **修法三条**：① 单/双副本两个读函数**分开**（`val`/`dual`），别让一个函数
   猜文件名格式；② 缺文件返回 nan 而不是抛异常，但**必须同时打印一行
   `completeness:` 自报**，异常可以是纪律、nan 只能是噪声；
   ③ **凡是「跑完才可能发现问题」的汇总代码，上线前必须对真实产物干跑一次**。
   同族：坑 7/12/13/18/20——全是「校验器没对着真目标」。
25. **「一次采样说空闲」不等于空闲——共享机上必须要求「连续空闲」。**
   （2026-09-11 16:14 现场，代价：一次 boot + 一次险情；16:21 新闸门立刻兑现。）
   等卡闸门第一版是**单次采样判空**：16:14:45 那一poll读到空，两个副本立刻起；
   **35 秒后**邻居起了一个 41 GiB 的新任务，我的 GPU1 副本以
   `Engine core initialization failed`（CUDA OOM）死掉，而在那几十秒里
   **我的 40 GiB 分配和他们的 41 GiB 分配在同一张卡上赛跑**。
   这次是我的任务输——这是无害的方向，但**在这台机器上硬币可以落在另一边，
   而 OOM 掉别人的任务是这条线绝对不能发生的事**。
   **修法**：`FREE_HOLD` 连续 `N` 次采样（默认 10×30s = 5 分钟）为空才放行，
   中途一旦变忙就 `GPUS_FREE_ABORTED` 并把计数清零重来。这不能消灭竞争
   （邻居何时起任务与我们无关），但把暴露窗口从 30 秒压到「恰好撞上」。
   **16:21 的实测**：新闸门 `GPUS_IDLE 2 — holding 10 polls` → 30 秒后
   `GPUS_FREE_ABORTED 2: 2:33398MiB after 1 free polls`——
   **同一台机器、同一种邻居行为，上一版会直接起，这一版挡住了。**
   **第二条修法（同一个坑的另一半）**：`wait_up` 原以为「慢」和「死」是一回事，
   一个 OOM 死掉的 boot 会让它白等满 20 分钟。现在超过 4 个 poll 若
   `pgrep -- "--port N "` 已经找不到进程，就立刻返回 1——
   **死的 server 不是慢的 server**。
26. **`engine_stats_*_s3.txt` 的最后一拍会抓到泄洪段，不能当稳态用**
   （2026-09-12 sweep9 回填时发现）。这一轮第一次读到 `Running 15 / Waiting 45`
   这种数——那是 bench 客户端**已经停发**、引擎正在把队列排空的**下降沿**，
   不是稳态。同一臂的稳态在**启动日志**里（`server_*.log` 的 `loggers.py` 行）：
   `Running 24 / KV 98.2%`。
   **取证口径**：`Running` 一律从 `server_*.log` 取连续同向的稳态拍；
   `engine_stats_*_s3.txt` 只作旁证，用之前先看它前后几拍是不是同向。
   和 25 号是同一个病的两面——**25 是「一次空闲不算空闲」，
   26 是「一次读数不算稳态」**。
27. **「机械构造」的复用假设会在变量定义上断掉，而 `bash -n` 抓不到**
   （2026-09-12 sweep10 第一次启动，07:22:03–07:23:35 一次夭折）：
   新相位块照抄了上一个块的**调用**（`boot_replica 8341 $SP1 ...`），
   却忘了 `SP1` / `SP2` / `SPEC_R2C` 这三行**定义是上一个块自带的**，
   `mtp_bench7.sh` 顶层根本没有。
   语法检查通过——**未定义变量不是语法错**——运行时 `CUDA_VISIBLE_DEVICES=` 为空，
   `No devices were found`，`wait_gpus` 拿到空参数，日志打成 `booting on (,)`，
   **卡在等卡轮询里空转：不报错、不退出、也不测任何东西。**
   发现方式：启动后 40 秒读 `run.log` 第一行的 booting 文案。
   教训两条：
   (a) 追加相位块后，把块里引用的**每个变量**与「块内定义 ∪ bench7 顶层定义」
       对一遍（`grep -oE '\$\{?[A-Za-z_][A-Za-z0-9_]*' | sort -u` 逐个核）；
   (b) **启动后第一件事是核那一行 booting 文案里的参数有没有真填进去**，
       不是核进程在不在——它在，而且非常安静。

## 文件清单

| 文件 | 内容 |
|---|---|
| `mtp_bench.sh` / `mtp_bench2.sh` | 两轮 sweep 脚本（头部 docstring 写明臂定义与基线选择理由） |
| `mtp_bench3.sh` | C2b 臂（显式 sched 8192 排除法） |
| `mtp_bench4.sh` | C4 臂（len 4096 容量墙解锁阴性对照，docstring 含预注册预测） |
| `int4_calib.py` | GPTQ W4A16 混合精度标定（ignore 名单 + 防呆断言 + docstring 写选型理由） |
| `mtp_bench5.sh` | W4 臂 bench（int4 checkpoint + fp8 KV + MTP k=2） |
| `chain_int4.sh` | 串联编排：C4 完成 → 等两卡空闲 → 标定 → W4 bench |
| `ceval_w4.sh` | W4 C-Eval 补跑包装（起 int4 服务 + `<lc-venv>` 跑 200 题；坑 10/11 的现场） |
| `mtp_greedy.py` | 贪心等价探针（8 题固定 prompt） |
| `greedy_diff2.py` / `greedy_diff3.py` | 逐 token 对照（含 within-arm 对照优先的设计） |
| `greedy_diff4.py` | A(sweep1) vs W4 对照（含首翻位置报告） |
| `greedy_diff_c2c4.py` | C2 vs C4 对照（len 数值中性的证据） |
| `sweep{1,2}/summary.txt` | 各臂各并发档的关键指标汇总 |
| `sweep{1,2}/run.log` | 臂启动/完成时间线（含锚点） |
| `sweep{1,2}/bench_*.log` | bench 原始输出 |
| `sweep{1,2}/specmetrics_*.txt` | /metrics 的 spec/accept/draft 指标 |
| `sweep{1,2}/greedy.txt` | 贪心探针原始 JSON（token 序列 + 文本） |
| `sweep{1,2}/server_startup_lines.txt` | 各臂启动配置/池大小/WARNING 摘录（全量 server 日志留测量机） |
| `sweep2/env_2026-09-11.txt` | 环境锁档 |
| `sweep3/summary.txt` + `bench_C2b_*.log` | C2b 臂结果（sched 排除法） |
| `sweep3/engine_stats_{K2,M2,C2b}_c48.txt` | **容量墙实锤**：spec 臂 Running 16-17 / usage 90-96% vs K2 满载 48 |
| `sweep4/`（C4 全套：summary/bench×2/engine_stats×2/specmetrics×2/greedy/startup_lines） | **len 4096 阴性对照**：@48 298.21（<340 判据）、Running 仍 16-17、KV 字节同 9.28 GiB、C2 vs C4 贪心 8/8 |
| `sweep5/` | W4 全套：summary/bench×2/engine_stats×2/specmetrics×2/greedy/startup_lines/**ceval_W4.log**（路径脱敏同上） |
| `mtp_bench6.sh` | sweep6 编排：TP2 锚点 + R2b/R2c/R2a 副本对四相（docstring 含臂设计与邻座条件） |
| `sweep6/` | 2 卡部署矩阵全套：summary（含 nan bug 现场）/bench×10/engine_stats×4/startup_lines（池打印 129,706 vs 历史 136,533 的邻座足迹证据）/gpu_snapshots×5/specmetrics×2 |
| `sweep7/` | 本窗证据：B0 `bench_B0_c16/c48.log`（**含客户端 Namespace 口径字段**，坑 17c）/ engine_stats_B0_c{16,48}_s{1,2,3}（7 份，prefix 恒 0.0%）/ server_startup_lines（四项台账闭合）/ gpu_snapshots / run.log / summary.txt |
| `sweep7/seeds.txt` | **非推断的 tag→seed 表**（24 行，逐条从每个 bench log 自己的 `Namespace(seed=N)` 抽出）。坑 21 的取证与修法依据 |
| `sweep7/loadavg_trace.txt` | 每个引擎采样点的 loadavg（1/5/15m）。修正案 4 证伪「负载解释」的原始依据 |
| `sweep7/gpu_wait.txt` | `wait_gpus` 等卡日志（邻居占用时的轮询记录，含跳过与 deadline） |
| `sweep7/seeds_used.txt` | 运行期 seed 落盘（`tag seed phase` 三列）+ 同相位撞号告警 |
| `sweep7/engine_stats_*.txt` | 每个相位 1–3 次引擎态采样（Running/Waiting/KV usage/prefix hit） |
| `sweep7/bench_{R2c0b,R2cH_c120t,R2cH_c144t,R2cAB70,R2cAB00,SB2048,SB8192,R2b48z,B0ppp}_*.log` | 修正案 4 全部臂的原始 bench 输出（含各自 Namespace 口径行） |
| `sweep7/bench_{R2bZ_c108t,R2bZ_c120t,R2bZ_c132t,R2bZ_c144t}_p{1,2}.log` | 夜跑 k=0 阶梯（同池同 boot，seed 0）——**发现 E 的峰在 @132t** |
| `sweep7/bench_R2cZ_{c48t_a,c48t_b,c48t_c,c120t,c144t}_p{1,2}.log` | 夜跑 k=1（重跑后，两副本均 k=1）——发现 B/D 的位置斜坡与交叉点 |
| `sweep7/bench_FV{0,1,2}_c48.log` | 四因子链的 bf16/fp8/fp8+sched8192 三节点（单副本，各一次 boot） |
| `sweep7/bench_R2cABr{0a,70,0b}_c48t_p{1,2}.log` | 一 boot 内 0a/70/0b —— **发现 C 的池效应与位置效应分离** |
| `sweep7/server_R2bZ_1.log` | **坑 26 的原始铁证**：171659 的 `Errno 98` traceback + 170213 在 16:54:34 服务流量的 pid 前缀 |
| `sweep7/server_{FV0,FV1,FV2,R2cABr_1,R2cABr_2,R2cZ_1,R2cZ_2}.log` | 各相位服务器日志（`R2cZ_{1,2}` 均为重跑后的合法 boot，drafter 命中数 1） |
| `sweep7/port_conflict.txt` | `port_precheck` 的冲突记录（本轮为空 = 重跑时端口干净；文件本身即守卫生效的留痕位） |
| `sweep7/bench_{R2bZw,R2cZw}_{w0,c108t,c120t,c132t,c144t}_p{1,2}.log` | Z2 的两条 **warm** 阶梯（各丢一个预热点，位置对齐）——**发现 F：交叉点 (66,72] 与 k=1 的 @132t 下凹** |
| `sweep7/bench_{R2bZx,R2cZx}_{w0,c120t,c126t,c132t,c132tr,c138t,c144t}_p{1,2}.log` | Z3 的交叉带细扫（28 个）——`@132tr` 是 @132t 的**就地复测**；**P1 证伪：k=1 凹口在 63 lanes/副本而非 66** |
| `sweep7/bench_{R2bZy,R2cZy}_{w0,c132t,c136t,c138t,c138tr,c140t,c144t}_p{1,2}.log` | Z4 的断崖复测（28 个）——**Q1/Q2/Q4 证伪 → 断崖撤回**；`R2bZy_c136t…c140t` 四格是**全账本唯一出现副本劈叉（>2%）的地方**，`p1` 双峰而 `p2` 跨 boot 逐位一致 |
| `sweep7/bench_R2cZv_{w0,c120t,c126t,c126tr,c132t,c126tb}_p{1,2}.log` 等 15 件 | Z5 的凹口独立复测（单臂 k=1，12 个 bench log + 2 个 server log + 1 个 specmetrics）——**R1–R4 全确认：凹口独立复现，且不是劈叉**；`@126t` 三个读数 654.78/653.52/652.84 极差 0.30%，全部 rung 的 \|p1−p2\| ≤0.69%（2% 阈值零命中） |
| `sweep7/bench_R2cZ6_{n1..n6}_p{1,2}.log` 等 16 件 | Z6 的位置效应定向复测（单臂 k=1，两形状交错 `48,120,48,120,48,120`，seed 固定 0）——**@48t 首点 653.57 比同形状的 662.59/662.77 低 1.37%**；@120t 点只低 0.09%；同形状重复差 **0.03% / 0.30%**（噪声底比效应小一个量级）。**交错从 @48t 开始 → 形状归属未判**，Z7 反向交错 |
| `sweep7/bench_R2cZ7_{m1..m6}_p{1,2}.log` 等 16 件 | Z7 的反向交错（单臂 k=1，`120,48,120,48,120,48`，seed 固定 0）——**S1/S2 双双证伪、NEITHER fired**：`m1`（@120t boot 首点）只低 **0.41%**、`m2`（@48t 位置 2）**完全不低（+0.04%）**；同形状非首点在两相之间只差 **0.14% / 0.05%**（@48t 五个 661.03–662.77、@120t 五个 668.66–671.22）→ **`发现 B` 整条作废** |

对照记录：TP2 bf16 基线数字来自部署实验同窗 ABBA（@16 284.9±6.5 / @48 350.2±3.3）。
**⚠️ 这两行跨窗引用前先读坑 17**：本窗重测为 @16 239.61 / @48 375.09，@16 差
−15.9%、@48 差 +7.1% 方向相反，且 09-06 那轮的口径字段（`--num-prompts` /
`--request-rate` / `--random-input-len`）仓库里没有记录 → **跨窗那一行未复现，
不要与 sweep6/sweep7 的数字并列做表**。

## 修正轨迹（归因的自我纠错）

1. **verify 算力假设 → 排除**：初判 @48 MTP 亏损是"批量饱和后 verify 多算
   不划算"；TPOT/ITL 对账推翻——verify 步仅 +4.6% 时间换 2.35 倍 token；
2. **调度节流假设 → 排除**：改判 spec 强制 `scheduled=2048` 的 prefill 节流；
   C2b（显式 8192）@48 不动推翻；
3. **max_len 预留假设 → 开跑前撤销**：C4 臂初始预测"len 减半 → 记账减半"；
   读 `single_type_kv_cache_manager.py` 后发现准入门按实际 token 记账，
   预测改为阴性（见 sweep4/5 预注册节）；
4. **最终定稿**：容量墙 = draft 权重缩池（块数）+ 每请求 2 快照块（驻留），
   引擎统计与源码对账逐位闭合。
5. **W8A8 误判 → 口头汇报后撤销**（2026-09-11）：向用户汇报时把「W8A8
   fp8 dynamic」当成未测牌推荐——实际 A 臂 `--quantization fp8` 就是
   W8A8 dynamic（bf16 checkpoint 在线压 fp8，激活动态量化），整条阶梯
   一直跑在 fp8 GEMM 上，TPOT 46.3ms 已是这套内核栈的 fp8 速度顶。
   教训：给自己开「新牌」前先对一遍已有臂的 serve 标志位。
6. **「@16 条数 = 波数，波数少就被 ramp 主导」→ 实测证伪**（2026-09-11，
   B0 跑完后修）：sweep6 → sweep7 之间，我把 B0 首跑 @48 的 +14.4% 异常归因到
   坑 16（seed 复用前缀缓存）——那部分成立；但顺手把 09-06 的 @16 = 284.9
   归因成"那轮用了 32 条、2 波、被 ramp 主导"，并据此写出坑 17、预注册
   B0-16 ≈ 270-310。实测 96 条版 **239.61**、32 条版 **237.07**，差 **1.1%**
   ——**归因错了**。真相是 @16 为客户端并发受限点，条数只改时长（3 倍），
   不改速率。教训：两个数字对不上时，"找到一个会改变结果的变量"不等于"找到了
   原因"——**这个变量必须能在本机被单独测一次**（96 条 vs 32 条各跑一次的成本
   只有 2 分钟），拿跨窗数字反推原因等于用未知解释未知。已按此重写坑 17。
7. **「台账四项已闭合」→ 用户一问就露馅**（2026-09-11，B1 跑完后修）：我在
   B0 的报告里写了「台账四项闭合」，实际入库的 `server_startup_lines.txt` 里
   **一行台账都没有**——正则匹配的是老版本措辞（坑 18）。用户问「显存占用
   记录了吗」时才回原始 server 日志核对，发现数值一直都在。
   教训有两层：(a) 解析器静默失配是老毛病（坑 7/12/13/18 同族）；
   (b) 更要紧的是**「我核对过了」必须能在已入库的证据上被第三方重算**，
   否则等于把自己脑子里的数当成证据——这正是这条线一直在防的事。
8. **两个「解释」被自己的补测连续推翻**（2026-09-11 晚，修正案 4）：
   对 R2c@48t 600.40 的 2.97% 缺口，我先给了「prompt 集」解释，落空后改给
   「host loadavg」解释。补的两个对照把两个都杀了——**同 boot 背靠背换池只有
   +2.46%**（R2cAB），**loadavg 15.10 下照样跑出 651.60**（R2c0b）。
   教训：两个候选解释互相竞争时，**不要挑一个「更合理」的写下，要去造那个能把
   两者分开的臂**。本轮最后是靠 R2cAB 的「同一 boot 内背靠背、只差一个变量」
   这个形态解决的——**这种形态对共享机上的任何归因都适用，应该成为默认手法**，
   而不是在 n=1 的跨窗相关上编故事。
9. **两处「数量一致性」被当成证据，其实是恒等式**（2026-09-11 晚自查）：
   ① 「steps/s 排序与吞吐完全一致」——两者都是 `常数/wall`，排序必然一致；
   ② 「四因子乘积闭合到 0.001%」——四段是相邻节点的比值，乘积欠项后必然等于
   end/start（telescoping）。**两处都不是第二个证据，只是把同一个数写了两遍。**
   教训（已写进两节正文）：**写下「A 与 B 一致」之前，先问「这两个量是不是由
   同一个量除出来的」**。这条专治「用同一个测量冒充两个独立证据」——
   和 6 号（用未知解释未知）是同一个病的两种形态。
10. **bench_pair 的几何被我自己写反了**（2026-09-11 晚审 R2cAB 时发现）：
    我在脚本注释里写「两个副本取 prompt 池的**不相交两半**」，据此以为
    「两个副本 = 同一池的两半」可省一次采样。实际 `bench_pair TAG conc np p0 n`
    里 **`p0+i` 是端口号，`np` 是每个副本**都跑的全量 prompt 数——
    两个副本服务的是**同一批 48 条 prompt**（`Total input tokens 51668 = 2×25834`
    逐位确认）。所以它们**不是**「同一池的两半」，而是**两个独立服务器各自
    服务同一池**。安全性来自「两个服务器 = 两份 KV 缓存」，不是来自「池被切开了」。
    教训：**注释里的机制说明必须和代码逐行对过一次**，否则它会作为一个
    「已知事实」被我带进后面的推理（这一条正是坑 16 的判据来源，写反了风险很大）。
11. **合取判据的幅度门槛把自己卡掉了**（2026-09-12，sweep8 Z8 跑完后回填）：
    我给 Z8 写死的判据是「(a) 墙内两格 k=2 赢 **且** (b) `@132t` k=2 输 **≥10%**，
    两条都成立才宣布『深度 2 在高并发反超』被证伪」。实测 **(a) 兑现（+4.31%/+4.61%）
    而 (b) 只到 −5.72%**，所以按字面**合取不成立，我不能宣布证伪**——
    尽管方向在 ≥24 路/副本的**每一格**都如预测那样是 k=2 输。
    同时我自己的"证伪器"（`@48t` 及以上出现 k=2 ≥ k=1）**没有触发**。
    结果是一种很别扭但必须照记的状态：**方向全对、判据没兑现**。
    处理方式：**两行并存，门槛不动**，结论里明写"方向性结论照记、判据兑付照记失败"。
    教训：**幅度门槛是对同类效应的先验，不是对机制的先验**——
    当机制（`Running` 钉在 17 vs 24、KV 97.7% vs 98.2%）比幅度结实得多的时候，
    把 (b) 写成 '输且 ≥10%' 就把一个稳健的方向结论押在了一个拍脑袋的数上。
    下次同类的写法：**方向与幅度分两条、分开兑付**，别用 AND 把它们绑成一个判定。
12. **预注册第 3 条拍紧了（窄负）**（2026-09-12，同一次）：我写
    "k=1 四个高格极差 <3%（同一条墙的四个读数）"，实测 **3.24%**，负。
    而 sweep7 Z2 自己那四格是 **3.74%**——**两个独立窗口都落在 3.2–3.7%**，
    说明 3% 这个门槛是我看数之前拍错了，不是这次测脏了。
    **门槛不动，判定记负**；同时把机制证据单独记：这两条阶梯的
    `Running` 分别全程钉在 24 与 17，`Waiting` 单调增长——
    **"同一条墙"由引擎态支撑，不由极差支撑**。
13. **「块粒度是个常数」这个隐含假设被自己的归档日志推翻**（2026-09-12，
    sweep9 回填时）：§5.2 里我写死了 `attention block size = 1,600 tokens`，
    之后好几轮都当常数在用。这一轮为了给 `--mamba-block-size` 定量，把归档日志里
    `Setting attention block size to N tokens` 这行**全部抽出来数了一遍**——
    结果是 **784 / 1568 / 1584 / 1600 四个值**，按 `(KV dtype, k)` 分得干干净净
    （见「sweep9 结果 → 修正」）。1,600 只是 k=2 那一档，本线终态配置是 **1584**。
    **结论没错**：81,920 ÷ 1600 ≈ 51 块这条对账本来就是拿 k=2 的池做的，
    换个块长也还在原来的格子上——错的是**把一个被测出来的值当成了架构常数**，
    而它其实是「这份配置在这台机器上」的输出。
    教训：**凡是日志打印出来的"常数"，都要问一句它是常量还是当前配置的函数。**
    同一行在归档里出现过几个不同的值，答案就是免费的——但得先去数；
    这一轮没去数之前，它一直躺在那儿冒充"已知"。
    顺带还纠正了一个文件名笔误（`kv_cache_interface.py:911` 实为
    `platforms/interface.py:912`，padding 那行在 `:936`）。
