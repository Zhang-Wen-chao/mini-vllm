We need answer in Chinese. User asks: 用贪心策略说明找零钱问题（面额 1,5,10,25）为什么贪心能得到最优解。 Need explain greedy optimal for coin system 1,5,10,25 (US coins). Need likely provide proof via exchange argument / canonical coin system. Need be careful: Greedy: take largest coin <= remaining. Need show minimizes number of coins. We can present structural property: For any amount n, optimal solution uses at most 4 pennies, at most 1 nickel? Actually with 1,5,10,25: greedy optimal. Need rigorous but accessible.

We need think of proof. Several approaches:
1. Show each coin is multiple of previous? 5 multiple of 1, 10 multiple of 5, 25 not multiple of 10. So not simple.
2. Use modular / exchange argument: For any amount, greedy choices can be transformed to optimal without increasing coins.
   - Let greedy use g25 quarters, g10 dimes, g5 nickels, g1 pennies.
   - Need show any optimal has same or more coins.
   Could prove by considering amount modulo 25, 10, 5.
   - Greedy quarters: q = floor(n/25). Need show any optimal uses at most q quarters? Actually if uses fewer than q quarters, remaining amount >= 25 + (n mod 25). To make that with dimes/nickels/pennies requires at least? Need show replacing 25 by smaller coins requires at least 3 coins? 25 can be made by 2 dimes + 1 nickel (3 coins) or 5 dimes (5) etc. So one quarter saves at least 2 coins compared to any combination of smaller coins making 25. But interactions with remainder? We can show if optimal uses q' < q quarters, then it must make at least 25 more value using non-quarter coins. The minimum number of non-quarter coins to make 25 is 3 (10+10+5). Replacing those 3 with one quarter reduces coin count by 2, contradiction to optimal if q' < q? Need ensure those 3 coins can be identified and replaced while preserving total? If optimal has q' quarters and some smaller coins total value n - 25q'. If q' <= q-1, smaller coins value >= 25 + r where r = n mod 25. We need find subset of smaller coins of value at least 25? Not necessarily exactly 25. We can exchange: If smaller coins total value >=25, can we replace some subset with a quarter and not increase coin count? Need subset value 25? If no subset exactly 25, maybe use 30? Replacing 30 by quarter + 5? That's 2 coins vs at least? Need careful.

Alternative: prove by induction on amount using greedy choice property: For coin system canonical, show for all n, greedy(n) <= optimal(n). Induction: Let c be largest coin <= n. Need show there exists optimal solution containing c. Then greedy optimal by induction. For 1,5,10,25, prove: For any n, there is an optimal solution with a quarter if n>=25, with a dime if 10<=n<25? etc.

Can prove via cases modulo 25:
- If n = 25q + r, 0<=r<25. Greedy uses q quarters + greedy(r). Need show optimal uses q quarters. Suppose optimal uses k<q quarters. Then smaller coins make 25(q-k)+r >=25+r. Need show any set of smaller coins making at least 25+r has at least q-k+? Hmm.
Maybe use lower bound: To make amount A with coins <=10, minimum number is ceil(A/10) except? Since max coin 10, at least ceil(A/10). If k<q, smaller amount A = n-25k >= 25(q-k)+r. Number smaller coins >= ceil(A/10). Total coins >= k + ceil((25(q-k)+r)/10). Need show this > q + greedy(r) for k<q? Let's test.
Let d = q-k >=1. Need show k + ceil((25d+r)/10) >= q + G(r) + 1? Actually to prove optimal not less than greedy: q+G(r). If k<q, total >= k + ceil((25d+r)/10). Need >= q+G(r). Since k=q-d, need ceil((25d+r)/10) >= d + G(r). Is this true for d>=1, r 0..24? Let's test d=1: ceil((25+r)/10) >= 1+G(r). r=0: ceil25/10=3 >=1 yes. r=1:3 >=1+1=2. r=4:3>=2. r=5:3>=1+1=2. r=9:3>=1+1? G(9)=1 dime? Wait G(9)=1? 9 uses 1 dime? no largest <=9 is 5? Actually coins 1,5,10,25; G(9)=1 nickel +4 pennies =5 coins? Wait if amount 9, greedy uses 5+1*4 =5 coins. But optimal also 5. So G(9)=5. Then ceil34/10=4 >=1+5=6 false. But if k=q-1, smaller amount 25+r. For r=9, A=34. Minimum coins with <=10 is 4 (10+10+10+4 pennies? 4 coins? 10+10+10+1+1+1+1 =7? Wait ceil(34/10)=4 lower bound not tight; 34 with 10,5,1 min? 10+10+10+1+1+1+1 =7? Actually 34 = 10+10+10+1+1+1+1 (7) or 10+10+5+5+1+1+1+1 (8). Minimum 7? Let's compute: 34 mod 5 =4, use 3 dimes +4 pennies =7. So lower bound ceil(A/10)=4 too weak. Need better lower bound for smaller coins.

Maybe use exact minimum for amounts <25? Since if fewer quarters, the excess over q quarters is at least 25, but can combine with remainder. Could use periodicity: For amounts >=25, optimal(n) = 1 + optimal(n-25)? Is that true? If yes, induction. Need prove: For n>=25, there is optimal solution containing